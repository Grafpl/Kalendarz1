<UserControl x:Class="Kalendarz1.HandlowiecDashboard.Controls.GaugeRealizacji"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             Width="250" Height="200">

    <UserControl.Resources>
        <SolidColorBrush x:Key="GaugeBgBrush" Color="#2D333B"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tytuł -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Tytul, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="14" FontWeight="SemiBold"
                   Foreground="#FFFFFF"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,5"/>

        <!-- Gauge -->
        <lvc:Gauge Grid.Row="1"
                   Value="{Binding Wartosc, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   From="0" To="120"
                   Uses360Mode="False"
                   GaugeActiveFill="{Binding KolorBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   GaugeBackground="{StaticResource GaugeBgBrush}"
                   LabelFormatter="{Binding LabelFormatter, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   HighFontSize="28"
                   InnerRadius="60"
                   AnimationsSpeed="0:0:0.5"/>

        <!-- Wartości tekstowe -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,5,0,0">
            <TextBlock Text="{Binding WartoscTekst, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="12" Foreground="#AAAAAA"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Prognoza -->
        <TextBlock Grid.Row="3"
                   Text="{Binding PrognozaTekst, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="11" Foreground="#888888"
                   FontStyle="Italic"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   Margin="5,5,5,0"/>
    </Grid>
</UserControl>
