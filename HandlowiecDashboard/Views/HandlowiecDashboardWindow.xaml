<Window x:Class="Kalendarz1.HandlowiecDashboard.Views.HandlowiecDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Dashboard Handlowca" Height="850" Width="1300"
        WindowStartupLocation="CenterScreen" WindowState="Maximized" Background="#1A1D21">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundDarkBrush" Color="#1A1D21"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#252A31"/>
        <SolidColorBrush x:Key="CardBackgroundLightBrush" Color="#2D333B"/>
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#F4A261"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#8B949E"/>
        <SolidColorBrush x:Key="BorderColorBrush" Color="#30363D"/>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" Background="{StaticResource CardBackgroundBrush}"
                                Padding="{TemplateBinding Padding}" Margin="0,0,2,0"
                                CornerRadius="8,8,0,0" BorderThickness="1,1,1,0"
                                BorderBrush="{StaticResource BorderColorBrush}">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource CardBackgroundLightBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentOrangeBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2D333B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#000000"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Background="#000000" BorderBrush="#555555" BorderThickness="1"
                                Focusable="false" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="#000000" BorderBrush="#555555" BorderThickness="1" CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="1" Background="#222222" CornerRadius="0,3,3,0">
                                                    <Path Fill="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" Margin="10,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock"><Setter Property="Foreground" Value="#FFFFFF"/></Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#000000" BorderBrush="#555555" BorderThickness="1"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#000000"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="8,4"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentOrangeBrush}"/>
            <Setter Property="Foreground" Value="#1A1D21"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>

        <Style x:Key="DescriptionStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Margin" Value="20,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Ciemny styl dla DefaultTooltip -->
        <Style TargetType="lvc:DefaultTooltip">
            <Setter Property="Background" Value="#2D333B"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Naglowek -->
        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="20,12"
                BorderBrush="{StaticResource BorderColorBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Width="40" Height="40" CornerRadius="8" Background="{StaticResource AccentOrangeBrush}" Margin="0,0,12,0">
                        <TextBlock Text="H" FontSize="20" FontWeight="Bold" Foreground="#1A1D21" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Dashboard Handlowca" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Analiza sprzedazy z faktur" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="btnOdswiez" Content="Odswiez" Margin="12,0,0,0" Style="{StaticResource DarkButtonStyle}" Click="BtnOdswiez_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" x:Name="tabControl" Margin="10" SelectionChanged="TabControl_SelectionChanged">

            <!-- Tab: Sprzedaz miesieczna -->
            <TabItem Header="Sprzedaz miesieczna">
                <Border Background="{StaticResource CardBackgroundLightBrush}" CornerRadius="0,8,8,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="15,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Rok:" Style="{StaticResource FilterLabelStyle}"/>
                                <ComboBox x:Name="cmbRokSprzedaz" Style="{StaticResource DarkComboBoxStyle}" Width="80" SelectionChanged="CmbRokSprzedaz_SelectionChanged"/>
                                <TextBlock Text="Miesiac:" Style="{StaticResource FilterLabelStyle}" Margin="20,0,8,0"/>
                                <ComboBox x:Name="cmbMiesiacSprzedaz" Style="{StaticResource DarkComboBoxStyle}" Width="120" SelectionChanged="CmbMiesiacSprzedaz_SelectionChanged"/>
                                <TextBlock Text="Sprzedaz per handlowiec w wybranym miesiacu. Rozwin handlowca aby zobaczyc jego klientow." Style="{StaticResource DescriptionStyle}"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" Padding="15,10" Margin="0,1,0,0">
                            <TextBlock x:Name="txtSumaSprzedaz" Text="" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}" HorizontalAlignment="Center"/>
                        </Border>
                        <Grid Grid.Row="3" Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="400"/>
                            </Grid.ColumnDefinitions>
                            <lvc:CartesianChart Grid.Column="0" x:Name="chartSprzedaz" Margin="10">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis x:Name="axisXSprzedaz" ShowLabels="True" FontSize="11" Foreground="#FFFFFF" LabelsRotation="0">
                                        <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis x:Name="axisYSprzedaz" Title="Wartosc (zl)" MinValue="0" Foreground="#FFFFFF" FontSize="10">
                                        <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                            <Border Grid.Column="1" Background="#1A1D21" CornerRadius="8" Margin="10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="Szczegoly per handlowiec (kliknij aby rozwinac)" FontWeight="Bold" Foreground="#F4A261" Margin="10,10,10,5"/>
                                    <TreeView Grid.Row="1" x:Name="treeSprzedaz" Background="Transparent" BorderThickness="0" Foreground="#FFFFFF"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab: Top 10 odbiorcy -->
            <TabItem Header="Top 10 odbiorcy">
                <Border Background="{StaticResource CardBackgroundLightBrush}" CornerRadius="0,8,8,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="15,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Rok:" Style="{StaticResource FilterLabelStyle}"/>
                                <ComboBox x:Name="cmbRokTop10" Style="{StaticResource DarkComboBoxStyle}" Width="80" SelectionChanged="CmbTop10_SelectionChanged"/>
                                <TextBlock Text="Miesiac:" Style="{StaticResource FilterLabelStyle}" Margin="20,0,8,0"/>
                                <ComboBox x:Name="cmbMiesiacTop10" Style="{StaticResource DarkComboBoxStyle}" Width="120" SelectionChanged="CmbTop10_SelectionChanged"/>
                                <TextBlock Text="Towar:" Style="{StaticResource FilterLabelStyle}" Margin="20,0,8,0"/>
                                <ComboBox x:Name="cmbTowarTop10" Style="{StaticResource DarkComboBoxStyle}" Width="250" SelectionChanged="CmbTop10_SelectionChanged"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" Padding="10,8" Margin="0,1,0,0">
                            <TextBlock x:Name="txtTop10Info" Text="" FontSize="12" Foreground="#FFFFFF" HorizontalAlignment="Center"/>
                        </Border>
                        <lvc:CartesianChart Grid.Row="2" x:Name="chartTop10" Margin="20">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis x:Name="axisXTop10" Title="Wartosc (zl)" MinValue="0" Foreground="#FFFFFF" FontSize="10">
                                    <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis x:Name="axisYTop10" ShowLabels="True" FontSize="10" Foreground="#FFFFFF">
                                    <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab: Udzial handlowcow -->
            <TabItem Header="Udzial handlowcow">
                <Border Background="{StaticResource CardBackgroundLightBrush}" CornerRadius="0,8,8,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="15,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Od:" Style="{StaticResource FilterLabelStyle}"/>
                                <ComboBox x:Name="cmbRokUdzialOd" Style="{StaticResource DarkComboBoxStyle}" Width="80" SelectionChanged="CmbUdzial_SelectionChanged"/>
                                <ComboBox x:Name="cmbMiesiacUdzialOd" Style="{StaticResource DarkComboBoxStyle}" Width="110" Margin="5,0,0,0" SelectionChanged="CmbUdzial_SelectionChanged"/>
                                <TextBlock Text="Do:" Style="{StaticResource FilterLabelStyle}" Margin="20,0,8,0"/>
                                <ComboBox x:Name="cmbRokUdzialDo" Style="{StaticResource DarkComboBoxStyle}" Width="80" SelectionChanged="CmbUdzial_SelectionChanged"/>
                                <ComboBox x:Name="cmbMiesiacUdzialDo" Style="{StaticResource DarkComboBoxStyle}" Width="110" Margin="5,0,0,0" SelectionChanged="CmbUdzial_SelectionChanged"/>
                                <TextBlock Text="Procentowy udzial kazdego handlowca w sprzedazy w czasie" Style="{StaticResource DescriptionStyle}"/>
                            </StackPanel>
                        </Border>
                        <lvc:CartesianChart Grid.Row="1" x:Name="chartUdzial" Margin="20" LegendLocation="None">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis x:Name="axisXUdzial" ShowLabels="True" FontSize="10" Foreground="#FFFFFF" LabelsRotation="45">
                                    <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis x:Name="axisYUdzial" Title="Udzial (%)" MinValue="0" Foreground="#FFFFFF" FontSize="10">
                                    <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab: Analiza cen -->
            <TabItem Header="Analiza cen">
                <Border Background="{StaticResource CardBackgroundLightBrush}" CornerRadius="0,8,8,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="15,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Rok:" Style="{StaticResource FilterLabelStyle}"/>
                                <ComboBox x:Name="cmbRokCeny" Style="{StaticResource DarkComboBoxStyle}" Width="80" SelectionChanged="CmbCeny_SelectionChanged"/>
                                <TextBlock Text="Miesiac:" Style="{StaticResource FilterLabelStyle}" Margin="20,0,8,0"/>
                                <ComboBox x:Name="cmbMiesiacCeny" Style="{StaticResource DarkComboBoxStyle}" Width="120" SelectionChanged="CmbCeny_SelectionChanged"/>
                                <TextBlock Text="Towar:" Style="{StaticResource FilterLabelStyle}" Margin="20,0,8,0"/>
                                <ComboBox x:Name="cmbTowarCeny" Style="{StaticResource DarkComboBoxStyle}" Width="250" SelectionChanged="CmbCeny_SelectionChanged"/>
                                <TextBlock Text="Kliknij 2x na handlowca aby otworzyc szczegoly" Style="{StaticResource DescriptionStyle}"/>
                            </StackPanel>
                        </Border>
                        <Grid Grid.Row="1" Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <lvc:CartesianChart Grid.Column="0" Grid.Row="0" x:Name="chartCeny" Margin="0,0,10,0">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis x:Name="axisXCeny" ShowLabels="True" FontSize="11" Foreground="#FFFFFF" LabelsRotation="30">
                                        <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis x:Name="axisYCeny" Title="Cena (zl/kg)" MinValue="0" Foreground="#FFFFFF" FontSize="10">
                                        <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                            <lvc:CartesianChart Grid.Column="1" Grid.Row="0" x:Name="chartCenyKg" Margin="10,0,0,0">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis x:Name="axisXCenyKg" ShowLabels="True" FontSize="11" Foreground="#FFFFFF" LabelsRotation="30">
                                        <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis x:Name="axisYCenyKg" Title="Ilosc (kg)" MinValue="0" Foreground="#FFFFFF" FontSize="10">
                                        <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                            <!-- Tabela handlowcow z mozliwoscia klikniecia -->
                            <DataGrid Grid.Row="1" Grid.ColumnSpan="2" x:Name="gridAnalizaCeny" AutoGenerateColumns="False" IsReadOnly="True"
                                      Background="Transparent" BorderThickness="0" Margin="0,10,0,0"
                                      RowBackground="#252A31" AlternatingRowBackground="#2D333B"
                                      Foreground="#FFFFFF" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#30363D"
                                      HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False"
                                      MaxHeight="200" MouseDoubleClick="GridAnalizaCeny_MouseDoubleClick">
                                <DataGrid.ColumnHeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="#1A1D21"/>
                                        <Setter Property="Foreground" Value="#F4A261"/>
                                        <Setter Property="Padding" Value="8,6"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="11"/>
                                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                        <Setter Property="BorderBrush" Value="#30363D"/>
                                    </Style>
                                </DataGrid.ColumnHeaderStyle>
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#3D434B"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Handlowiec" Binding="{Binding Handlowiec}" Width="150"/>
                                    <DataGridTextColumn Header="Suma kg" Binding="{Binding SumaKgTekst}" Width="100">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="#4ECDC4"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Srednia cena" Binding="{Binding SredniaCenaTekst}" Width="100">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Min cena" Binding="{Binding MinCenaTekst}" Width="90">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="#FF6B6B"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Max cena" Binding="{Binding MaxCenaTekst}" Width="90">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="#4ECDC4"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Suma wartosc" Binding="{Binding SumaWartoscTekst}" Width="110">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="#F4A261"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Transakcji" Binding="{Binding LiczbaTransakcji}" Width="80">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Kontrahentow" Binding="{Binding LiczbaKontrahentow}" Width="90">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab: Swieze vs Mrozone -->
            <TabItem Header="Swieze vs Mrozone">
                <Border Background="{StaticResource CardBackgroundLightBrush}" CornerRadius="0,8,8,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="15,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Rok:" Style="{StaticResource FilterLabelStyle}"/>
                                <ComboBox x:Name="cmbRokSM" Style="{StaticResource DarkComboBoxStyle}" Width="80" SelectionChanged="CmbSM_SelectionChanged"/>
                                <TextBlock Text="Miesiac:" Style="{StaticResource FilterLabelStyle}" Margin="20,0,8,0"/>
                                <ComboBox x:Name="cmbMiesiacSM" Style="{StaticResource DarkComboBoxStyle}" Width="120" SelectionChanged="CmbSM_SelectionChanged"/>
                                <TextBlock Text="Porownanie sprzedazy produktow swiezych i mrozonych" Style="{StaticResource DescriptionStyle}"/>
                            </StackPanel>
                        </Border>
                        <lvc:CartesianChart Grid.Row="1" x:Name="chartSwiezeMrozone" Margin="20">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis x:Name="axisXSM" ShowLabels="True" FontSize="14" Foreground="#FFFFFF">
                                    <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis x:Name="axisYSM" Title="Ilosc (kg)" MinValue="0" Foreground="#FFFFFF" FontSize="10">
                                    <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab: Porownanie okresow -->
            <TabItem Header="Porownanie okresow">
                <Border Background="{StaticResource CardBackgroundLightBrush}" CornerRadius="0,8,8,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="15,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Rok 1:" Style="{StaticResource FilterLabelStyle}"/>
                                <ComboBox x:Name="cmbRokPorown1" Style="{StaticResource DarkComboBoxStyle}" Width="80" SelectionChanged="CmbPorown_SelectionChanged"/>
                                <TextBlock Text="Rok 2:" Style="{StaticResource FilterLabelStyle}" Margin="20,0,8,0"/>
                                <ComboBox x:Name="cmbRokPorown2" Style="{StaticResource DarkComboBoxStyle}" Width="80" SelectionChanged="CmbPorown_SelectionChanged"/>
                                <TextBlock Text="Porownanie sprzedazy rok do roku w poszczegolnych miesiacach" Style="{StaticResource DescriptionStyle}"/>
                            </StackPanel>
                        </Border>
                        <lvc:CartesianChart Grid.Row="1" x:Name="chartPorownanie" Margin="20" LegendLocation="Top">
                            <lvc:CartesianChart.ChartLegend>
                                <lvc:DefaultLegend BulletSize="12" Foreground="#FFFFFF"/>
                            </lvc:CartesianChart.ChartLegend>
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis x:Name="axisXPorown" ShowLabels="True" FontSize="11" Foreground="#FFFFFF">
                                    <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis x:Name="axisYPorown" Title="Wartosc (zl)" MinValue="0" Foreground="#FFFFFF" FontSize="10">
                                    <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab: Trend sprzedazy -->
            <TabItem Header="Trend sprzedazy">
                <Border Background="{StaticResource CardBackgroundLightBrush}" CornerRadius="0,8,8,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="15,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Ostatnie miesiecy:" Style="{StaticResource FilterLabelStyle}"/>
                                <ComboBox x:Name="cmbOkres" Style="{StaticResource DarkComboBoxStyle}" Width="80" SelectionChanged="CmbTrend_SelectionChanged"/>
                                <TextBlock Text="Trend wartosci sprzedazy w wybranym okresie - pokazuje jak zmienia sie sprzedaz miesiac do miesiaca" Style="{StaticResource DescriptionStyle}"/>
                            </StackPanel>
                        </Border>
                        <lvc:CartesianChart Grid.Row="1" x:Name="chartTrend" Margin="20">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis x:Name="axisXTrend" ShowLabels="True" FontSize="10" Foreground="#FFFFFF" LabelsRotation="30">
                                    <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis x:Name="axisYTrend" Title="Wartosc (zl)" MinValue="0" Foreground="#FFFFFF" FontSize="10">
                                    <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab: Saldo opakowan - styl Power BI -->
            <TabItem Header="Saldo opakowan">
                <Border Background="{StaticResource CardBackgroundLightBrush}" CornerRadius="0,8,8,8">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="250"/>
                        </Grid.RowDefinitions>

                        <!-- Filtry -->
                        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="15,10" Margin="0,0,0,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Handlowiec:" Style="{StaticResource FilterLabelStyle}"/>
                                <ComboBox x:Name="cmbHandlowiecOpak" Style="{StaticResource DarkComboBoxStyle}" Width="150" SelectionChanged="CmbOpakowania_SelectionChanged"/>
                                <TextBlock Text="Od:" Style="{StaticResource FilterLabelStyle}" Margin="20,0,8,0"/>
                                <DatePicker x:Name="dpOpakOd" Width="120" SelectedDateChanged="DpOpakowania_SelectedDateChanged"/>
                                <TextBlock Text="Do:" Style="{StaticResource FilterLabelStyle}" Margin="15,0,8,0"/>
                                <DatePicker x:Name="dpOpakDo" Width="120" SelectedDateChanged="DpOpakowania_SelectedDateChanged"/>
                                <TextBlock x:Name="txtOpakWybranyKontrahent" Text="" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}" VerticalAlignment="Center" Margin="30,0,0,0"/>
                                <TextBlock Text="Kliknij na wykresie aby zaznaczyc odbiorce" Style="{StaticResource DescriptionStyle}" Margin="20,0,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Gorny panel - dwa wykresy slupkowe poziome -->
                        <Grid Grid.Row="1" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Wykres slupkowy E2 per kontrahent -->
                            <Border x:Name="borderChartE2" Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="10" Margin="0,0,5,0"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Border.RenderTransform>
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1.01" Duration="0:0:0.15"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1.01" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.15"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,0,0,5">
                                        <TextBlock Text="Saldo E2 wg Kontrahenta" FontSize="12" FontWeight="Bold" Foreground="#4ECDC4"/>
                                        <TextBlock x:Name="txtOpakE2Suma" Text="" FontSize="11" Foreground="#8B949E" Margin="15,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <lvc:CartesianChart Grid.Row="1" x:Name="chartOpakowaniaE2" Margin="0" DataClick="ChartOpakowaniaE2_DataClick"
                                                        AnimationsSpeed="0:0:0.5" Hoverable="True" DisableAnimations="False">
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis x:Name="axisXOpakE2" Title="" MinValue="0" Foreground="#FFFFFF" FontSize="9">
                                                <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis x:Name="axisYOpakE2" ShowLabels="True" FontSize="10" Foreground="#FFFFFF">
                                                <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </Grid>
                            </Border>

                            <!-- Wykres slupkowy H1 per kontrahent -->
                            <Border x:Name="borderChartH1" Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="10" Margin="5,0,0,0"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Border.RenderTransform>
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1.01" Duration="0:0:0.15"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1.01" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.15"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,0,0,5">
                                        <TextBlock Text="Saldo H1 wg Kontrahenta" FontSize="12" FontWeight="Bold" Foreground="#F4A261"/>
                                        <TextBlock x:Name="txtOpakH1Suma" Text="" FontSize="11" Foreground="#8B949E" Margin="15,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <lvc:CartesianChart Grid.Row="1" x:Name="chartOpakowaniaH1" Margin="0" DataClick="ChartOpakowaniaH1_DataClick"
                                                        AnimationsSpeed="0:0:0.5" Hoverable="True" DisableAnimations="False">
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis x:Name="axisXOpakH1" Title="" MinValue="0" Foreground="#FFFFFF" FontSize="9">
                                                <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis x:Name="axisYOpakH1" ShowLabels="True" FontSize="10" Foreground="#FFFFFF">
                                                <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- Dolny panel - dwa wykresy liniowe tygodniowe -->
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Wykres liniowy E2 - historia tygodniowa wybranego kontrahenta -->
                            <Border x:Name="borderTrendE2" Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="10" Margin="0,0,5,0"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Border.RenderTransform>
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.2"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,0,0,5">
                                        <TextBlock Text="Ilosc pojemnikow E2 ktore 'wisza' nam odbiorcy w tygodniach" FontSize="10" Foreground="#4ECDC4" FontWeight="Bold"/>
                                        <TextBlock x:Name="txtOpakE2TrendKontrahent" Text="" FontSize="10" Foreground="#F4A261" FontWeight="Bold" Margin="10,0,0,0"/>
                                    </StackPanel>
                                    <lvc:CartesianChart Grid.Row="1" x:Name="chartOpakTrendE2" Margin="0"
                                                        AnimationsSpeed="0:0:0.7" Hoverable="True" DisableAnimations="False">
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis x:Name="axisXOpakTrendE2" ShowLabels="True" FontSize="9" Foreground="#FFFFFF" LabelsRotation="0">
                                                <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis x:Name="axisYOpakTrendE2" Title="" Foreground="#4ECDC4" FontSize="9" MinValue="0">
                                                <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </Grid>
                            </Border>

                            <!-- Wykres liniowy H1 - historia tygodniowa wybranego kontrahenta -->
                            <Border x:Name="borderTrendH1" Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="10" Margin="5,0,0,0"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Border.RenderTransform>
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.2"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,0,0,5">
                                        <TextBlock Text="Ilosc pojemnikow H1 ktore 'wisza' nam odbiorcy w tygodniach" FontSize="10" Foreground="#F4A261" FontWeight="Bold"/>
                                        <TextBlock x:Name="txtOpakH1TrendKontrahent" Text="" FontSize="10" Foreground="#F4A261" FontWeight="Bold" Margin="10,0,0,0"/>
                                    </StackPanel>
                                    <lvc:CartesianChart Grid.Row="1" x:Name="chartOpakTrendH1" Margin="0"
                                                        AnimationsSpeed="0:0:0.7" Hoverable="True" DisableAnimations="False">
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis x:Name="axisXOpakTrendH1" ShowLabels="True" FontSize="9" Foreground="#FFFFFF" LabelsRotation="0">
                                                <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis x:Name="axisYOpakTrendH1" Title="" Foreground="#F4A261" FontSize="9" MinValue="0">
                                                <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                            </lvc:Axis>
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Tab: Platnosci -->
            <TabItem Header="Platnosci">
                <Border Background="{StaticResource CardBackgroundLightBrush}" CornerRadius="0,8,8,8">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Filtry -->
                            <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="15,10" Margin="0,0,0,10">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Handlowiec:" Style="{StaticResource FilterLabelStyle}"/>
                                    <ComboBox x:Name="cmbHandlowiecPlat" Style="{StaticResource DarkComboBoxStyle}" Width="150" SelectionChanged="CmbPlatnosci_SelectionChanged"/>
                                    <TextBlock Text="Kliknij 2x na kontrahenta aby otworzyc szczegoly platnosci" Style="{StaticResource DescriptionStyle}" Margin="20,0,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Glowne karty statystyk - 6 kart -->
                            <Grid Grid.Row="1" Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Karta: Suma do zaplaty -->
                                <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,5,0">
                                    <StackPanel>
                                        <TextBlock Text="SUMA DO ZAPLATY" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                                        <TextBlock x:Name="txtPlatSumaDoZaplaty" Text="0 zl" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtPlatIloscKlientow" Text="0 klientow" FontSize="10" Foreground="#8B949E" Margin="0,3,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Karta: Terminowe -->
                                <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,5,0">
                                    <StackPanel>
                                        <TextBlock Text="TERMINOWE" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                                        <TextBlock x:Name="txtPlatTerminowe" Text="0 zl" FontSize="20" FontWeight="Bold" Foreground="#4ECDC4" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtPlatTerminoweProcent" Text="0%" FontSize="10" Foreground="#4ECDC4" Margin="0,3,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Karta: Przeterminowane -->
                                <Border Grid.Column="2" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,5,0">
                                    <StackPanel>
                                        <TextBlock Text="PRZETERMINOWANE" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                                        <TextBlock x:Name="txtPlatPrzeterminowane" Text="0 zl" FontSize="20" FontWeight="Bold" Foreground="#FF6B6B" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtPlatPrzeterminowaneProcent" Text="0%" FontSize="10" Foreground="#FF6B6B" Margin="0,3,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Karta: Liczba faktur przeterminowanych -->
                                <Border Grid.Column="3" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,5,0">
                                    <StackPanel>
                                        <TextBlock Text="FAKTUR PRZET." FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                                        <TextBlock x:Name="txtPlatLiczbaFakturPrzet" Text="0" FontSize="20" FontWeight="Bold" Foreground="#FF6B6B" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtPlatLiczbaKlientowPrzet" Text="u 0 klientow" FontSize="10" Foreground="#8B949E" Margin="0,3,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Karta: Przekroczony limit -->
                                <Border Grid.Column="4" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,5,0">
                                    <StackPanel>
                                        <TextBlock Text="PRZEKR. LIMIT" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                                        <TextBlock x:Name="txtPlatPrzekroczony" Text="0 zl" FontSize="20" FontWeight="Bold" Foreground="#F4A261" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtPlatPrzekroczonyIlosc" Text="0 klientow" FontSize="10" Foreground="#F4A261" Margin="0,3,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Karta: Max dni przeterminowania -->
                                <Border Grid.Column="5" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,0,0">
                                    <StackPanel>
                                        <TextBlock Text="MAX DNI PRZET." FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                                        <TextBlock x:Name="txtPlatMaxDni" Text="0 dni" FontSize="20" FontWeight="Bold" Foreground="#AA96DA" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtPlatMaxDniKlient" Text="" FontSize="9" Foreground="#8B949E" Margin="0,3,0,0" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Aging Analysis - podzial wg dni z liczba faktur -->
                            <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="ANALIZA WIEKOWA NALEZNOSCI (przeterminowane wg liczby dni)" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold" Margin="0,0,0,10"/>
                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 0-30 dni -->
                                        <Border Grid.Column="0" Background="#1A1D21" CornerRadius="5" Padding="12" Margin="0,0,5,0">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="1-30 DNI" FontSize="12" Foreground="#4ECDC4" FontWeight="Bold" HorizontalAlignment="Center"/>
                                                <TextBlock x:Name="txtAging030" Text="0 zl" FontSize="18" FontWeight="Bold" Foreground="#4ECDC4" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                                <TextBlock x:Name="txtAging030Procent" Text="0% | 0 faktur" FontSize="10" Foreground="#8B949E" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- 31-60 dni -->
                                        <Border Grid.Column="1" Background="#1A1D21" CornerRadius="5" Padding="12" Margin="5,0,5,0">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="31-60 DNI" FontSize="12" Foreground="#FFE66D" FontWeight="Bold" HorizontalAlignment="Center"/>
                                                <TextBlock x:Name="txtAging3160" Text="0 zl" FontSize="18" FontWeight="Bold" Foreground="#FFE66D" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                                <TextBlock x:Name="txtAging3160Procent" Text="0% | 0 faktur" FontSize="10" Foreground="#8B949E" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- 61-90 dni -->
                                        <Border Grid.Column="2" Background="#1A1D21" CornerRadius="5" Padding="12" Margin="5,0,5,0">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="61-90 DNI" FontSize="12" Foreground="#F4A261" FontWeight="Bold" HorizontalAlignment="Center"/>
                                                <TextBlock x:Name="txtAging6190" Text="0 zl" FontSize="18" FontWeight="Bold" Foreground="#F4A261" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                                <TextBlock x:Name="txtAging6190Procent" Text="0% | 0 faktur" FontSize="10" Foreground="#8B949E" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- 90+ dni -->
                                        <Border Grid.Column="3" Background="#1A1D21" CornerRadius="5" Padding="12" Margin="5,0,0,0">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="90+ DNI" FontSize="12" Foreground="#FF6B6B" FontWeight="Bold" HorizontalAlignment="Center"/>
                                                <TextBlock x:Name="txtAging90Plus" Text="0 zl" FontSize="18" FontWeight="Bold" Foreground="#FF6B6B" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                                <TextBlock x:Name="txtAging90PlusProcent" Text="0% | 0 faktur" FontSize="10" Foreground="#8B949E" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Grid>
                            </Border>

                            <!-- Wykres + Top 5 przekroczonych limitow + Top 5 dluznikow -->
                            <Grid Grid.Row="3" Margin="0,0,0,10" Height="200">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="280"/>
                                    <ColumnDefinition Width="280"/>
                                </Grid.ColumnDefinitions>

                                <!-- Wykres slupkowy przeterminowanych -->
                                <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="10" Margin="0,0,5,0">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="TOP 5 PRZETERMINOWANYCH" FontSize="10" Foreground="{StaticResource AccentOrangeBrush}" FontWeight="Bold" Margin="5,0,0,5"/>
                                        <lvc:CartesianChart Grid.Row="1" x:Name="chartTopDluznicy" Margin="0">
                                            <lvc:CartesianChart.AxisX>
                                                <lvc:Axis x:Name="axisXDluznicy" Title="" MinValue="0" Foreground="#FFFFFF" FontSize="9">
                                                    <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                                </lvc:Axis>
                                            </lvc:CartesianChart.AxisX>
                                            <lvc:CartesianChart.AxisY>
                                                <lvc:Axis x:Name="axisYDluznicy" ShowLabels="True" FontSize="9" Foreground="#FFFFFF">
                                                    <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                                </lvc:Axis>
                                            </lvc:CartesianChart.AxisY>
                                        </lvc:CartesianChart>
                                    </Grid>
                                </Border>

                                <!-- Panel Top dluznicy -->
                                <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,5,0">
                                    <StackPanel>
                                        <TextBlock Text="TOP 5 DLUZNICY (przeterminowane)" FontSize="10" Foreground="#FF6B6B" FontWeight="Bold" Margin="0,0,0,8"/>
                                        <StackPanel x:Name="panelTopDluznicy"/>
                                    </StackPanel>
                                </Border>

                                <!-- Panel Top przekroczone limity -->
                                <Border Grid.Column="2" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,0,0">
                                    <StackPanel>
                                        <TextBlock Text="TOP 5 PRZEKROCZONE LIMITY" FontSize="10" Foreground="#F4A261" FontWeight="Bold" Margin="0,0,0,8"/>
                                        <StackPanel x:Name="panelTopPrzekroczone"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Tabela ze szczegolami -->
                            <DataGrid Grid.Row="4" x:Name="gridPlatnosci" AutoGenerateColumns="False" IsReadOnly="True"
                                      Background="Transparent" BorderThickness="0"
                                      RowBackground="#252A31" AlternatingRowBackground="#2D333B"
                                      Foreground="#FFFFFF" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#30363D"
                                      HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False"
                                      MouseDoubleClick="GridPlatnosci_MouseDoubleClick">
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#3D434B"/>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding PrzekroczonyLimitAlert}" Value="True">
                                                <Setter Property="Background" Value="#3D2A2A"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.ColumnHeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="#1A1D21"/>
                                        <Setter Property="Foreground" Value="#F4A261"/>
                                        <Setter Property="Padding" Value="8,6"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="11"/>
                                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                        <Setter Property="BorderBrush" Value="#30363D"/>
                                    </Style>
                                </DataGrid.ColumnHeaderStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Kontrahent" Binding="{Binding Kontrahent}" Width="160"/>
                                    <DataGridTextColumn Header="Handlowiec" Binding="{Binding Handlowiec}" Width="90"/>
                                    <DataGridTextColumn Header="Limit kredytu" Binding="{Binding LimitKredytuTekst}" Width="90">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Do zaplaty" Binding="{Binding DoZaplatyTekst}" Width="90">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Terminowe" Binding="{Binding TerminoweTekst}" Width="85">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Foreground" Value="#4ECDC4"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Przetermin." Binding="{Binding PrzeterminowaneTekst}" Width="85">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PrzeterminowaneAlert}" Value="True">
                                                        <Setter Property="Foreground" Value="#FF6B6B"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Po limicie" Binding="{Binding PrzekroczonyLimitTekst}" Width="80">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PrzekroczonyLimitAlert}" Value="True">
                                                        <Setter Property="Foreground" Value="#F4A261"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="% limitu" Binding="{Binding ProcentLimituTekst}" Width="65">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PrzekroczonyLimitAlert}" Value="True">
                                                        <Setter Property="Foreground" Value="#FF6B6B"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Max dni" Binding="{Binding DniPrzeterminowania}" Width="60">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PrzeterminowaneAlert}" Value="True">
                                                        <Setter Property="Foreground" Value="#FF6B6B"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Faktur" Binding="{Binding LiczbaFaktur}" Width="55">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </TabItem>

        </TabControl>

        <Border Grid.RowSpan="2" Background="#CC1A1D21" x:Name="loadingOverlay" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="{StaticResource AccentOrangeBrush}"/>
                <TextBlock Text="Ladowanie danych..." FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
