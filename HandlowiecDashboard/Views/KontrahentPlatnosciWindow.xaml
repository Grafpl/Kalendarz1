<Window x:Class="Kalendarz1.HandlowiecDashboard.Views.KontrahentPlatnosciWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Platnosci Kontrahenta" Height="750" Width="1100"
        WindowStartupLocation="CenterScreen" WindowState="Maximized" Background="#1A1D21">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundDarkBrush" Color="#1A1D21"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#252A31"/>
        <SolidColorBrush x:Key="CardBackgroundLightBrush" Color="#2D333B"/>
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#F4A261"/>
        <SolidColorBrush x:Key="AccentGreenBrush" Color="#4ECDC4"/>
        <SolidColorBrush x:Key="AccentRedBrush" Color="#FF6B6B"/>
        <SolidColorBrush x:Key="AccentYellowBrush" Color="#FFE66D"/>
        <SolidColorBrush x:Key="AccentPurpleBrush" Color="#AA96DA"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#8B949E"/>
        <SolidColorBrush x:Key="BorderColorBrush" Color="#30363D"/>

        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentOrangeBrush}"/>
            <Setter Property="Foreground" Value="#1A1D21"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Naglowek -->
        <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="20,12"
                BorderBrush="{StaticResource BorderColorBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Width="40" Height="40" CornerRadius="8" Background="{StaticResource AccentRedBrush}" Margin="0,0,12,0">
                        <TextBlock Text="P" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Platnosci: " FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock x:Name="txtKontrahentNazwa" Text="" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}"/>
                        </StackPanel>
                        <TextBlock x:Name="txtHandlowiec" Text="" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>
                <Button Grid.Column="2" x:Name="btnOdswiez" Content="Odswiez" Style="{StaticResource DarkButtonStyle}" Click="BtnOdswiez_Click"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Karty podsumowania -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Limit kredytu -->
                    <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,5,0">
                        <StackPanel>
                            <TextBlock Text="LIMIT KREDYTU" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                            <TextBlock x:Name="txtLimitKredytu" Text="0 zl" FontSize="22" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,3,0,0"/>
                            <TextBlock x:Name="txtWykorzystanieLimit" Text="" FontSize="11" Foreground="#8B949E" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Do zaplaty -->
                    <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,5,0">
                        <StackPanel>
                            <TextBlock Text="DO ZAPLATY" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                            <TextBlock x:Name="txtDoZaplaty" Text="0 zl" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}" Margin="0,3,0,0"/>
                            <TextBlock x:Name="txtIloscFaktur" Text="0 faktur" FontSize="11" Foreground="#8B949E" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Terminowe -->
                    <Border Grid.Column="2" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,5,0">
                        <StackPanel>
                            <TextBlock Text="TERMINOWE" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                            <TextBlock x:Name="txtTerminowe" Text="0 zl" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentGreenBrush}" Margin="0,3,0,0"/>
                            <TextBlock x:Name="txtTerminoweProcent" Text="0%" FontSize="11" Foreground="{StaticResource AccentGreenBrush}" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Przeterminowane -->
                    <Border Grid.Column="3" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,5,0">
                        <StackPanel>
                            <TextBlock Text="PRZETERMINOWANE" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                            <TextBlock x:Name="txtPrzeterminowane" Text="0 zl" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentRedBrush}" Margin="0,3,0,0"/>
                            <TextBlock x:Name="txtPrzeterminowaneProcent" Text="0%" FontSize="11" Foreground="{StaticResource AccentRedBrush}" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Max dni przeterminowania -->
                    <Border Grid.Column="4" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="5,0,0,0">
                        <StackPanel>
                            <TextBlock Text="MAX DNI PRZET." FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                            <TextBlock x:Name="txtMaxDni" Text="0 dni" FontSize="22" FontWeight="Bold" Foreground="{StaticResource AccentPurpleBrush}" Margin="0,3,0,0"/>
                            <TextBlock x:Name="txtPrzekroczonyLimit" Text="" FontSize="11" Foreground="{StaticResource AccentRedBrush}" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Aging Analysis + Przekroczone Limity - dwie kolumny -->
                <Grid Grid.Row="1" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- LEWA STRONA: Analiza wiekowa przeterminowan -->
                    <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="15" Margin="0,0,7,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="ANALIZA WIEKOWA PRZETERMINOWAŃ" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- 1-7 dni -->
                                <Border Grid.Column="0" Background="#1A1D21" CornerRadius="5" Padding="10" Margin="0,0,3,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="1-7 dni" FontSize="11" Foreground="#FCD34D" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="txtAging17" Text="0 zl" FontSize="16" FontWeight="Bold" Foreground="#FCD34D" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtAging17Faktury" Text="0 fakt." FontSize="9" Foreground="#8B949E" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- 8-14 dni -->
                                <Border Grid.Column="1" Background="#1A1D21" CornerRadius="5" Padding="10" Margin="3,0,3,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="8-14 dni" FontSize="11" Foreground="#F97316" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="txtAging814" Text="0 zl" FontSize="16" FontWeight="Bold" Foreground="#F97316" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtAging814Faktury" Text="0 fakt." FontSize="9" Foreground="#8B949E" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- 15-21 dni -->
                                <Border Grid.Column="2" Background="#1A1D21" CornerRadius="5" Padding="10" Margin="3,0,3,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="15-21 dni" FontSize="11" Foreground="#EF4444" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="txtAging1521" Text="0 zl" FontSize="16" FontWeight="Bold" Foreground="#EF4444" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtAging1521Faktury" Text="0 fakt." FontSize="9" Foreground="#8B949E" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- 21+ dni -->
                                <Border Grid.Column="3" Background="#1A1D21" CornerRadius="5" Padding="10" Margin="3,0,0,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="21+ dni" FontSize="11" Foreground="#DC2626" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="txtAging21Plus" Text="0 zl" FontSize="16" FontWeight="Bold" Foreground="#DC2626" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtAging21PlusFaktury" Text="0 fakt." FontSize="9" Foreground="#8B949E" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- PRAWA STRONA: Przekroczone limity wg kwoty -->
                    <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="15" Margin="7,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="PRZEKROCZONE LIMITY WG KWOTY" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- do 100k -->
                                <Border Grid.Column="0" Background="#1A1D21" CornerRadius="5" Padding="10" Margin="0,0,3,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="do 100k" FontSize="11" Foreground="#10B981" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="txtLimit100k" Text="0 zl" FontSize="16" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtLimit100kKlientow" Text="0 fakt." FontSize="9" Foreground="#8B949E" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- 100-300k -->
                                <Border Grid.Column="1" Background="#1A1D21" CornerRadius="5" Padding="10" Margin="3,0,3,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="100-300k" FontSize="11" Foreground="#F97316" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="txtLimit300k" Text="0 zl" FontSize="16" FontWeight="Bold" Foreground="#F97316" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtLimit300kKlientow" Text="0 fakt." FontSize="9" Foreground="#8B949E" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- 300-500k -->
                                <Border Grid.Column="2" Background="#1A1D21" CornerRadius="5" Padding="10" Margin="3,0,3,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="300-500k" FontSize="11" Foreground="#EF4444" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="txtLimit500k" Text="0 zl" FontSize="16" FontWeight="Bold" Foreground="#EF4444" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtLimit500kKlientow" Text="0 fakt." FontSize="9" Foreground="#8B949E" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- 500k+ -->
                                <Border Grid.Column="3" Background="#1A1D21" CornerRadius="5" Padding="10" Margin="3,0,0,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="500k+" FontSize="11" Foreground="#DC2626" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="txtLimit500kPlus" Text="0 zl" FontSize="16" FontWeight="Bold" Foreground="#DC2626" HorizontalAlignment="Center" Margin="0,3,0,0"/>
                                        <TextBlock x:Name="txtLimit500kPlusKlientow" Text="0 fakt." FontSize="9" Foreground="#8B949E" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Trend platnosci i historia -->
                <Grid Grid.Row="2" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="350"/>
                    </Grid.ColumnDefinitions>

                    <!-- Wykres trend platnosci -->
                    <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="15" Margin="0,0,7,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="TREND SALDA (OSTATNIE 6 MIESIECY)" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold" Margin="0,0,0,10"/>
                            <lvc:CartesianChart Grid.Row="1" x:Name="chartTrendSaldo" Height="180">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis x:Name="axisXTrend" ShowLabels="True" FontSize="10" Foreground="#FFFFFF">
                                        <lvc:Axis.Separator><lvc:Separator StrokeThickness="0"/></lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis x:Name="axisYTrend" Title="Saldo (zl)" MinValue="0" Foreground="#FFFFFF" FontSize="10">
                                        <lvc:Axis.Separator><lvc:Separator Stroke="#30363D" StrokeThickness="1"/></lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </Grid>
                    </Border>

                    <!-- Podsumowanie -->
                    <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="15" Margin="7,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="HISTORIA PLATNOSCI (12 MIES.)" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold" Margin="0,0,0,10"/>
                            <StackPanel Grid.Row="1">
                                <Border Background="#1A1D21" CornerRadius="5" Padding="10" Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="Suma zaplacona" FontSize="10" Foreground="#8B949E"/>
                                        <TextBlock x:Name="txtSumaZaplacona" Text="0 zl" FontSize="18" FontWeight="Bold" Foreground="#4ECDC4"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#1A1D21" CornerRadius="5" Padding="10" Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="Sredni czas platnosci" FontSize="10" Foreground="#8B949E"/>
                                        <TextBlock x:Name="txtSredniCzas" Text="0 dni" FontSize="18" FontWeight="Bold" Foreground="#FFE66D"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#1A1D21" CornerRadius="5" Padding="10" Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="Ostatnia platnosc" FontSize="10" Foreground="#8B949E"/>
                                        <TextBlock x:Name="txtOstatniaPlatnosc" Text="-" FontSize="18" FontWeight="Bold" Foreground="#F4A261"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#1A1D21" CornerRadius="5" Padding="10">
                                    <StackPanel>
                                        <TextBlock Text="Liczba opoznien" FontSize="10" Foreground="#8B949E"/>
                                        <TextBlock x:Name="txtLiczbaOpoznien" Text="0" FontSize="18" FontWeight="Bold" Foreground="#FF6B6B"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Tabela faktur -->
                <Border Grid.Row="3" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="SZCZEGOLY DOKUMENTOW" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Bold"/>
                            <TextBlock x:Name="txtLiczbaWynikow" Text="" FontSize="11" Foreground="{StaticResource AccentOrangeBrush}" Margin="15,0,0,0"/>
                        </StackPanel>
                        <DataGrid Grid.Row="1" x:Name="gridFaktury" AutoGenerateColumns="False" IsReadOnly="True"
                                  Background="Transparent" BorderThickness="0"
                                  RowBackground="#252A31" AlternatingRowBackground="#2D333B"
                                  Foreground="#FFFFFF" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#30363D"
                                  HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False"
                                  MaxHeight="250">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#1A1D21"/>
                                    <Setter Property="Foreground" Value="#F4A261"/>
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="11"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                    <Setter Property="BorderBrush" Value="#30363D"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nr dokumentu" Binding="{Binding NrDokumentu}" Width="120"/>
                                <DataGridTextColumn Header="Data" Binding="{Binding DataTekst}" Width="85"/>
                                <DataGridTextColumn Header="Termin" Binding="{Binding TerminTekst}" Width="85"/>
                                <DataGridTextColumn Header="Kwota brutto" Binding="{Binding KwotaBruttoTekst}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Rozliczone" Binding="{Binding RozliczoneTekst}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Foreground" Value="#4ECDC4"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Do zaplaty" Binding="{Binding DoZaplatyTekst}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Foreground" Value="#F4A261"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Dni" Binding="{Binding DniPrzeterminowania}" Width="50">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding JestPrzeterminowana}" Value="True">
                                                    <Setter Property="Foreground" Value="#FF6B6B"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Przeterminowana">
                                                    <Setter Property="Foreground" Value="#FF6B6B"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="W terminie">
                                                    <Setter Property="Foreground" Value="#4ECDC4"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Rozliczona">
                                                    <Setter Property="Foreground" Value="#8B949E"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Loading overlay -->
        <Border Grid.RowSpan="2" Background="#CC1A1D21" x:Name="loadingOverlay" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="{StaticResource AccentOrangeBrush}"/>
                <TextBlock Text="Ladowanie danych..." FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
