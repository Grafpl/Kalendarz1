<Window x:Class="Kalendarz1.Avilog.Views.RozliczeniaAvilogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Rozliczenia Avilog - Dzienne zestawienie uboju"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#ECEFF1">

    <Window.Resources>
        <!-- === PALETA KOLORÓW === -->
        <Color x:Key="PrimaryColor">#388E3C</Color>
        <Color x:Key="PrimaryDarkColor">#1B5E20</Color>
        <Color x:Key="PrimaryLightColor">#C8E6C9</Color>
        <Color x:Key="AccentColor">#FF6F00</Color>
        <Color x:Key="AccentLightColor">#FFE0B2</Color>
        <Color x:Key="TextDarkColor">#212121</Color>
        <Color x:Key="TextGrayColor">#757575</Color>
        <Color x:Key="BackgroundColor">#ECEFF1</Color>
        <Color x:Key="CardColor">#FFFFFF</Color>
        <Color x:Key="BorderColor">#CFD8DC</Color>
        <Color x:Key="ErrorColor">#D32F2F</Color>
        <Color x:Key="WarningColor">#F57C00</Color>
        <Color x:Key="SuccessColor">#388E3C</Color>
        <Color x:Key="InfoColor">#1976D2</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
        <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentLightBrush" Color="{StaticResource AccentLightColor}"/>
        <SolidColorBrush x:Key="TextDarkBrush" Color="{StaticResource TextDarkColor}"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="{StaticResource TextGrayColor}"/>
        <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}"/>

        <!-- === STYLE PRZYCISKÓW === -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#145214"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#546E7A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#37474F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E65100"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextGrayBrush}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- === STYL TEXTBOX === -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- === STYL DATEPICKER === -->
        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- === STYL KARTY === -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" BlurRadius="8" Opacity="0.12" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- === STYL NAGŁÓWKA GRUPY === -->
        <Style x:Key="GroupHeaderStyle" TargetType="Border">
            <Setter Property="Background" Value="#37474F"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>

        <!-- === STYLE DATAGRID === -->
        <Style x:Key="ModernDataGridColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#455A64"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#546E7A"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernDataGridRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#ECEFF1"/>
            <Setter Property="MinHeight" Value="44"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
                <DataTrigger Binding="{Binding JestSuma}" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontSize" Value="13"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernDataGridCell" TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- === STYL STATYSTYKI === -->
        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource TextGrayBrush}"/>
            <Setter Property="TextTransform" Value="Uppercase"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
        </Style>

        <Style x:Key="StatValueSmall" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <!-- NAGŁÓWEK -->
        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <Border Grid.Row="0" Padding="24,16">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#1B5E20" Offset="0"/>
                    <GradientStop Color="#388E3C" Offset="0.5"/>
                    <GradientStop Color="#4CAF50" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo i tytuł -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#FFFFFF20" CornerRadius="8" Padding="12,8" Margin="0,0,16,0">
                        <TextBlock Text="🚛" FontSize="28" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="DZIENNE ZESTAWIENIE UBOJU - AVILOG"
                                   FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="txtOkres" Text="Okres: --"
                                   FontSize="13" Foreground="#C8E6C9" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- Status i progress -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="txtStatus" Text="" Foreground="#C8E6C9" FontSize="12"
                               VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <ProgressBar x:Name="progressBar" Width="120" Height="4" IsIndeterminate="True"
                                 Visibility="Collapsed" Background="#FFFFFF30" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <!-- TOOLBAR / FILTRY -->
        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="16,16,16,0" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Wybór okresu -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,24,0">
                    <TextBlock Text="OKRES:" VerticalAlignment="Center" FontWeight="SemiBold"
                               Foreground="{StaticResource TextGrayBrush}" FontSize="11" Margin="0,0,12,0"/>
                    <DatePicker x:Name="datePickerOd" Width="130" Style="{StaticResource ModernDatePicker}"/>
                    <TextBlock Text="—" VerticalAlignment="Center" Margin="8,0" Foreground="{StaticResource TextGrayBrush}"/>
                    <DatePicker x:Name="datePickerDo" Width="130" Style="{StaticResource ModernDatePicker}"/>
                </StackPanel>

                <!-- Przyciski szybkiego wyboru -->
                <Button Grid.Column="1" Content="🔍 Załaduj" Style="{StaticResource PrimaryButton}"
                        Click="BtnLoad_Click" Margin="0,0,8,0" ToolTip="Załaduj dane (F5)"/>
                <Button Grid.Column="2" Content="Ten tydzień" Style="{StaticResource SecondaryButton}"
                        Click="BtnThisWeek_Click" Margin="0,0,8,0" ToolTip="Bieżący tydzień roboczy"/>
                <Button Grid.Column="3" Content="◀ Poprzedni" Style="{StaticResource SecondaryButton}"
                        Click="BtnPrevWeek_Click" Margin="0,0,8,0" ToolTip="Poprzedni tydzień"/>
                <Button Grid.Column="4" Content="Następny ▶" Style="{StaticResource SecondaryButton}"
                        Click="BtnNextWeek_Click" ToolTip="Następny tydzień"/>

                <!-- Separator -->
                <Border Grid.Column="5" Width="1" Background="{StaticResource BorderBrush}" Margin="24,4"/>

                <!-- Stawka -->
                <StackPanel Grid.Column="6" Orientation="Horizontal" Margin="0,0,12,0">
                    <TextBlock Text="STAWKA:" VerticalAlignment="Center" FontWeight="SemiBold"
                               Foreground="{StaticResource TextGrayBrush}" FontSize="11" Margin="0,0,12,0"/>
                    <TextBox x:Name="txtStawka" Width="80" Style="{StaticResource ModernTextBox}"
                             TextAlignment="Right" FontWeight="Bold" FontSize="14" Text="0.119"/>
                    <TextBlock Text="zł/kg" VerticalAlignment="Center" Foreground="{StaticResource TextGrayBrush}"
                               Margin="8,0,0,0"/>
                </StackPanel>

                <Button Grid.Column="7" Content="💾 Zapisz stawkę" Style="{StaticResource AccentButton}"
                        Click="BtnSaveStawka_Click" Margin="0,0,8,0"/>
                <Button Grid.Column="8" Content="📜" Style="{StaticResource IconButton}"
                        Click="BtnHistoriaStawek_Click" ToolTip="Historia stawek" FontSize="18"/>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <!-- GŁÓWNA ZAWARTOŚĆ - DATAGRID -->
        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="16" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Nagłówek tabeli z grupami kolumn -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50"/>  <!-- LP -->
                        <ColumnDefinition Width="180"/> <!-- Hodowca -->
                        <ColumnDefinition Width="80"/>  <!-- Ilość sztuk -->
                        <ColumnDefinition Width="270"/> <!-- ILOŚĆ KG (BRUTTO, TARA, NETTO) -->
                        <ColumnDefinition Width="160"/> <!-- ILOŚĆ UPADKÓW (SZT, KG) -->
                        <ColumnDefinition Width="100"/> <!-- RÓŻNICA KG -->
                        <ColumnDefinition Width="70"/>  <!-- KM -->
                        <ColumnDefinition Width="80"/>  <!-- Początek -->
                        <ColumnDefinition Width="80"/>  <!-- Koniec -->
                        <ColumnDefinition Width="80"/>  <!-- Godziny -->
                        <ColumnDefinition Width="100"/> <!-- Dzień -->
                        <ColumnDefinition Width="*"/>   <!-- Padding -->
                    </Grid.ColumnDefinitions>

                    <!-- Nagłówki grup -->
                    <Border Grid.Column="0" Grid.ColumnSpan="3" Background="#37474F" Padding="8,6">
                        <TextBlock Text="" Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                    </Border>
                    <Border Grid.Column="3" Background="#1565C0" Padding="8,6">
                        <TextBlock Text="ILOŚĆ KG" Foreground="White" FontWeight="Bold" FontSize="11"
                                   HorizontalAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="4" Background="#E65100" Padding="8,6">
                        <TextBlock Text="ILOŚĆ UPADKÓW" Foreground="White" FontWeight="Bold" FontSize="11"
                                   HorizontalAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="5" Background="#2E7D32" Padding="8,6">
                        <TextBlock Text="RÓŻNICA" Foreground="White" FontWeight="Bold" FontSize="11"
                                   HorizontalAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="6" Grid.ColumnSpan="5" Background="#37474F" Padding="8,6">
                        <TextBlock Text="" Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                    </Border>
                    <Border Grid.Column="11" Background="#37474F"/>
                </Grid>

                <!-- DataGrid -->
                <DataGrid x:Name="dataGridKursy" Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#ECEFF1"
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                          RowStyle="{StaticResource ModernDataGridRow}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          BorderThickness="0"
                          CanUserSortColumns="True"
                          EnableRowVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          MouseDoubleClick="DataGridKursy_MouseDoubleClick">
                    <DataGrid.Columns>
                        <!-- LP -->
                        <DataGridTextColumn Header="LP" Binding="{Binding LP}" Width="50">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Hodowca -->
                        <DataGridTextColumn Header="Hodowca" Binding="{Binding HodowcaNazwa}" Width="180">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Ilość sztuk -->
                        <DataGridTextColumn Header="Ilość sztuk" Binding="{Binding SztukiRazem, StringFormat=N0}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- ILOŚĆ KG - BRUTTO -->
                        <DataGridTextColumn Header="BRUTTO" Binding="{Binding BruttoUbojni, StringFormat=N0}" Width="90">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}">
                                    <Setter Property="Background" Value="#1976D2"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- ILOŚĆ KG - TARA -->
                        <DataGridTextColumn Header="TARA" Binding="{Binding TaraUbojni, StringFormat=N0}" Width="90">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}">
                                    <Setter Property="Background" Value="#1976D2"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- ILOŚĆ KG - NETTO -->
                        <DataGridTextColumn Header="NETTO" Binding="{Binding NettoUbojni, StringFormat=N0}" Width="90">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}">
                                    <Setter Property="Background" Value="#1976D2"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#1565C0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- ILOŚĆ UPADKÓW - SZT -->
                        <DataGridTextColumn Header="SZT" Binding="{Binding SztukiPadle}" Width="70">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}">
                                    <Setter Property="Background" Value="#F57C00"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Foreground" Value="#E65100"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- ILOŚĆ UPADKÓW - KG -->
                        <DataGridTextColumn Header="KG" Binding="{Binding UpadkiKg, StringFormat=N0}" Width="90">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}">
                                    <Setter Property="Background" Value="#F57C00"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="#E65100"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- RÓŻNICA KG -->
                        <DataGridTextColumn Header="KG" Binding="{Binding RoznicaKg, StringFormat=N0}" Width="100">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}">
                                    <Setter Property="Background" Value="#43A047"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- KM -->
                        <DataGridTextColumn Header="KM" Binding="{Binding DystansKM, StringFormat=N0}" Width="70">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Początek Usługi -->
                        <DataGridTextColumn Header="Początek" Binding="{Binding PoczatekUslugiFormatowany}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Koniec Usługi -->
                        <DataGridTextColumn Header="Koniec" Binding="{Binding KoniecUslugiFormatowany}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Godziny -->
                        <DataGridTextColumn Header="Godziny" Binding="{Binding CzasUslugiGodziny, StringFormat=N2}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Dzień -->
                        <DataGridTextColumn Header="Dzień" Binding="{Binding DataFormatowana}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Kierowca -->
                        <DataGridTextColumn Header="Kierowca" Binding="{Binding KierowcaNazwa}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextGrayBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Auto -->
                        <DataGridTextColumn Header="Auto" Binding="{Binding CarID}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextGrayBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <!-- FOOTER - PODSUMOWANIE I ROZLICZENIE -->
        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="16,0,16,16" Padding="24,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Podsumowanie statystyk -->
                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Suma sztuk -->
                    <StackPanel Grid.Column="0" Margin="0,0,32,0">
                        <TextBlock Text="SUMA SZTUK" Style="{StaticResource StatLabel}"/>
                        <TextBlock x:Name="txtSumaSztuk" Text="0" Style="{StaticResource StatValueSmall}"/>
                    </StackPanel>

                    <!-- Suma Brutto -->
                    <StackPanel Grid.Column="1" Margin="0,0,32,0">
                        <TextBlock Text="SUMA BRUTTO" Style="{StaticResource StatLabel}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="txtSumaBrutto" Text="0" Style="{StaticResource StatValueSmall}"/>
                            <TextBlock Text=" kg" Style="{StaticResource StatValueSmall}" Foreground="{StaticResource TextGrayBrush}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Suma Tara -->
                    <StackPanel Grid.Column="2" Margin="0,0,32,0">
                        <TextBlock Text="SUMA TARA" Style="{StaticResource StatLabel}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="txtSumaTara" Text="0" Style="{StaticResource StatValueSmall}"/>
                            <TextBlock Text=" kg" Style="{StaticResource StatValueSmall}" Foreground="{StaticResource TextGrayBrush}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Suma Netto -->
                    <StackPanel Grid.Column="3" Margin="0,0,32,0">
                        <TextBlock Text="SUMA NETTO" Style="{StaticResource StatLabel}" Foreground="#1565C0"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="txtSumaNetto" Text="0" Style="{StaticResource StatValueSmall}" Foreground="#1565C0"/>
                            <TextBlock Text=" kg" Style="{StaticResource StatValueSmall}" Foreground="#1565C0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Suma Upadków -->
                    <StackPanel Grid.Column="4" Margin="0,0,32,0">
                        <TextBlock Text="SUMA UPADKÓW" Style="{StaticResource StatLabel}" Foreground="#E65100"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="txtSumaUpadkow" Text="0" Style="{StaticResource StatValueSmall}" Foreground="#E65100"/>
                            <TextBlock Text=" kg" Style="{StaticResource StatValueSmall}" Foreground="#E65100"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Różnica KG -->
                    <StackPanel Grid.Column="5" Margin="0,0,32,0">
                        <TextBlock Text="RÓŻNICA KG" Style="{StaticResource StatLabel}" Foreground="#2E7D32"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="txtSumaRoznica" Text="0" Style="{StaticResource StatValueSmall}" Foreground="#2E7D32" FontWeight="Bold"/>
                            <TextBlock Text=" kg" Style="{StaticResource StatValueSmall}" Foreground="#2E7D32"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Suma KM -->
                    <StackPanel Grid.Column="6" Margin="0,0,32,0">
                        <TextBlock Text="SUMA KM" Style="{StaticResource StatLabel}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="txtSumaKM" Text="0" Style="{StaticResource StatValueSmall}"/>
                            <TextBlock Text=" km" Style="{StaticResource StatValueSmall}" Foreground="{StaticResource TextGrayBrush}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Suma godzin -->
                    <StackPanel Grid.Column="7">
                        <TextBlock Text="SUMA GODZIN" Style="{StaticResource StatLabel}"/>
                        <TextBlock x:Name="txtSumaGodzin" Text="0:00" Style="{StaticResource StatValueSmall}"/>
                    </StackPanel>
                </Grid>

                <!-- Separator -->
                <Border Grid.Column="1" Width="1" Background="{StaticResource BorderBrush}" Margin="32,0"/>

                <!-- Panel rozliczenia -->
                <Border Grid.Column="2" Background="#E8F5E9" CornerRadius="8" Padding="24,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Formuła -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                            <TextBlock Text="Cena:" FontSize="12" Foreground="{StaticResource TextGrayBrush}" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtFormulaCena" Text="0 zł" FontSize="12" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextDarkBrush}" Margin="4,0,16,0" VerticalAlignment="Center"/>
                            <TextBlock Text="×" FontSize="14" Foreground="{StaticResource TextGrayBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="Różnica KG:" FontSize="12" Foreground="{StaticResource TextGrayBrush}"
                                       Margin="16,0,0,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtFormulaRoznica" Text="0 kg" FontSize="12" FontWeight="SemiBold"
                                       Foreground="#2E7D32" Margin="4,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Label -->
                        <TextBlock Grid.Row="1" Text="DO ZAPŁATY ZA OKRES" FontSize="11" FontWeight="SemiBold"
                                   Foreground="#2E7D32" HorizontalAlignment="Center" Margin="0,0,0,4"/>

                        <!-- Kwota -->
                        <TextBlock x:Name="txtDoZaplaty" Grid.Row="2" Text="0,00 zł" FontSize="32" FontWeight="Bold"
                                   Foreground="#1B5E20" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <!-- FLOATING ACTION BUTTONS -->
        <!-- ═══════════════════════════════════════════════════════════════════ -->
        <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                    Margin="0,0,32,24">
            <Button x:Name="btnExportExcel" Content="📊 Excel" Style="{StaticResource PrimaryButton}"
                    Click="BtnExportExcel_Click" Margin="0,0,0,8" ToolTip="Eksport do Excel (Ctrl+E)"/>
            <Button Content="🖨️ Drukuj" Style="{StaticResource SecondaryButton}"
                    Click="BtnPrint_Click" Margin="0,0,0,8" ToolTip="Drukuj raport (Ctrl+P)"/>
            <Button Content="📋 Kopiuj" Style="{StaticResource SecondaryButton}"
                    Click="BtnCopy_Click" ToolTip="Kopiuj do schowka (Ctrl+C)"/>
        </StackPanel>
    </Grid>
</Window>
