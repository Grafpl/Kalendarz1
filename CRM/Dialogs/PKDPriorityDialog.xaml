<Window x:Class="Kalendarz1.CRM.Dialogs.PKDPriorityDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Kalendarz1.CRM.Dialogs"
        Title="Priorytetowe branże PKD"
        Width="500" Height="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        MinWidth="400" MinHeight="400">

    <Window.Resources>
        <local:InverseLengthToVisibilityConverter x:Key="InverseLengthToVisibility"/>
        <SolidColorBrush x:Key="BgPrimaryBrush" Color="#0d1117"/>
        <SolidColorBrush x:Key="BgSecondaryBrush" Color="#161b22"/>
        <SolidColorBrush x:Key="BgTertiaryBrush" Color="#21262d"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#e6edf3"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#8b949e"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#30363d"/>
        <SolidColorBrush x:Key="AccentRed" Color="#EF4444"/>

        <Style x:Key="PKDCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e6edf3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="bd" Background="#161b22" CornerRadius="4" Padding="10,8"
                                BorderThickness="1" BorderBrush="#30363d">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="checkBorder" Width="18" Height="18" CornerRadius="3"
                                        Background="#21262d" BorderBrush="#30363d" BorderThickness="1" Margin="0,0,10,0">
                                    <Path x:Name="checkMark" Visibility="Collapsed"
                                          Data="M3,7 L6,10 L12,4" Stroke="#EF4444" StrokeThickness="2"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Stretch="Uniform" Width="10" Height="10"/>
                                </Border>
                                <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="checkBorder" Property="Background" Value="#2d1f1f"/>
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#EF4444"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#EF4444"/>
                                <Setter Property="Foreground" Value="#EF4444"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#21262d"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2ea043"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#21262d"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#30363d"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SearchTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0d1117"/>
            <Setter Property="Foreground" Value="#e6edf3"/>
            <Setter Property="BorderBrush" Value="#30363d"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="#e6edf3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BgPrimaryBrush}" CornerRadius="10" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" Color="Black" Opacity="0.5" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#EF4444" CornerRadius="10,10,0,0" Padding="16,12" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Priorytetowe branże PKD" FontSize="15" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Zaznaczone branże będą wyświetlane na czerwono i pierwsze w CRM" FontSize="11" Foreground="#FFCDD2" Margin="0,2,0,0"/>
                    </StackPanel>
                    <Button Content="X" HorizontalAlignment="Right" VerticalAlignment="Top"
                            Background="Transparent" Foreground="White" BorderThickness="0"
                            FontSize="14" FontWeight="Bold" Cursor="Hand" Padding="8,2"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- Search -->
            <Border Grid.Row="1" Background="{StaticResource BgSecondaryBrush}" Padding="16,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtSearch" Style="{StaticResource SearchTextBox}"
                             TextChanged="TxtSearch_TextChanged"/>
                    <TextBlock Grid.Column="0" Text="Szukaj branży..." FontSize="12" Foreground="#484f58"
                               VerticalAlignment="Center" Margin="12,0,0,0" IsHitTestVisible="False"
                               Visibility="{Binding Text.Length, ElementName=txtSearch, Converter={StaticResource InverseLengthToVisibility}}"/>
                    <TextBlock x:Name="txtCount" Grid.Column="1" Text="0 zaznaczonych" FontSize="11"
                               Foreground="#EF4444" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </Grid>
            </Border>

            <!-- PKD List -->
            <ScrollViewer Grid.Row="2" Padding="16,8" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="pnlPKDList">
                    <!-- PKD items will be added dynamically -->
                </StackPanel>
            </ScrollViewer>

            <!-- Buttons -->
            <Border Grid.Row="3" Background="{StaticResource BgSecondaryBrush}" CornerRadius="0,0,10,10" Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Odznacz wszystkie" Style="{StaticResource CancelButton}" Click="BtnClearAll_Click"/>
                    <Button Grid.Column="2" Content="Anuluj" Style="{StaticResource CancelButton}" Click="BtnClose_Click" Margin="0,0,8,0"/>
                    <Button Grid.Column="3" Content="Zapisz" Style="{StaticResource ActionButton}" Click="BtnSave_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
