<Window x:Class="Kalendarz1.CRM.Dialogs.CallResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wynik rozmowy"
        Width="380" Height="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="BgPrimaryBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="BgSecondaryBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="BgTertiaryBrush" Color="#334155"/>
        <SolidColorBrush x:Key="AccentGreenBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#94A3B8"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#334155"/>

        <Style x:Key="RadioStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" Background="#1E293B" CornerRadius="6" Padding="12,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse x:Name="circle" Width="18" Height="18" Stroke="#64748B" StrokeThickness="2" Fill="Transparent"/>
                                <Ellipse x:Name="dot" Width="10" Height="10" Fill="#4CAF50" Visibility="Collapsed"
                                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="circle" Property="Stroke" Value="#4CAF50"/>
                                <Setter TargetName="dot" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="bd" Property="Background" Value="#1E3A2F"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#334155"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#66BB6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#475569"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BgPrimaryBrush}" CornerRadius="10" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" Color="Black" Opacity="0.4" BlurRadius="15"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#166534" CornerRadius="10,10,0,0" Padding="16,14">
                <StackPanel>
                    <TextBlock Text="📞 Wynik rozmowy" FontSize="16" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="txtContactName" Text="Nazwa firmy" FontSize="12" Foreground="#86EFAC" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>

            <!-- Options -->
            <ScrollViewer Grid.Row="1" Padding="16,12" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Wybierz wynik:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,8"/>

                    <RadioButton x:Name="rbNieOdebrano" Style="{StaticResource RadioStyle}" Content="📵 Nie odebrano" Tag="Próba kontaktu"/>
                    <RadioButton x:Name="rbZajete" Style="{StaticResource RadioStyle}" Content="⏳ Zajęte / Niedostępny" Tag="Próba kontaktu"/>
                    <RadioButton x:Name="rbZainteresowany" Style="{StaticResource RadioStyle}" Content="✅ Rozmowa - zainteresowany" Tag="Zgoda na dalszy kontakt"/>
                    <RadioButton x:Name="rbNiezainteresowany" Style="{StaticResource RadioStyle}" Content="❌ Rozmowa - niezainteresowany" Tag="Nie zainteresowany"/>
                    <RadioButton x:Name="rbUmowiono" Style="{StaticResource RadioStyle}" Content="📅 Rozmowa - umówiono kontakt" Tag="Zgoda na dalszy kontakt"/>
                    <RadioButton x:Name="rbOferta" Style="{StaticResource RadioStyle}" Content="📄 Do wysłania oferta" Tag="Do wysłania oferta"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Note -->
            <Border Grid.Row="2" Background="{StaticResource BgSecondaryBrush}" Padding="16,12">
                <StackPanel>
                    <TextBlock Text="📝 Krótka notatka (opcjonalnie):" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtNote" Background="{StaticResource BgTertiaryBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="10,8" FontSize="12"
                             TextWrapping="Wrap" AcceptsReturn="True" Height="60" CaretBrush="White"/>
                </StackPanel>
            </Border>

            <!-- Buttons -->
            <Border Grid.Row="3" Background="{StaticResource BgSecondaryBrush}" CornerRadius="0,0,10,10" Padding="16,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Anuluj" Style="{StaticResource CancelButton}" Click="BtnCancel_Click" Margin="0,0,6,0"/>
                    <Button Grid.Column="1" Content="Zapisz" Style="{StaticResource ActionButton}" Click="BtnSave_Click" Margin="6,0,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
