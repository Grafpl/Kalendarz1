<Window x:Class="Kalendarz1.CRM.CRMWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Kalendarz1.CRM"
        Title="CRM Manager Pro"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        x:Name="mainWindow"
        Background="#0F172A"
        FontFamily="Segoe UI">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:PhoneFormatConverter x:Key="PhoneFormatConverter"/>
        <local:HandlowiecAvatarConverter x:Key="HandlowiecAvatarConverter"/>
        <local:DateWithDayConverter x:Key="DateWithDayConverter"/>
        <local:MedalColorConverter x:Key="MedalColorConverter"/>
        <local:MedalTextConverter x:Key="MedalTextConverter"/>
        <local:NoteTypeToPathConverter x:Key="NoteTypeToPathConverter"/>
        <local:ValidationWarningConverter x:Key="ValidationWarningConverter"/>
        <local:ValidationWarningVisibilityConverter x:Key="ValidationWarningVisibilityConverter"/>

        <!-- DARK CORPORATE THEME -->
        <SolidColorBrush x:Key="BgPrimary" Color="#0F172A"/>
        <SolidColorBrush x:Key="BgSecondary" Color="#1E293B"/>
        <SolidColorBrush x:Key="BgTertiary" Color="#334155"/>
        <SolidColorBrush x:Key="BgElevated" Color="#475569"/>

        <SolidColorBrush x:Key="AccentPrimary" Color="#6366F1"/>
        <SolidColorBrush x:Key="AccentLight" Color="#A5B4FC"/>
        <SolidColorBrush x:Key="AccentBg" Color="#312E81"/>

        <SolidColorBrush x:Key="TextPrimary" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#94A3B8"/>
        <SolidColorBrush x:Key="TextMuted" Color="#64748B"/>

        <SolidColorBrush x:Key="BorderDefault" Color="#334155"/>
        <SolidColorBrush x:Key="BorderLight" Color="#475569"/>
        <SolidColorBrush x:Key="BorderAccent" Color="#6366F1"/>

        <!-- Statusy z ciemnym tłem -->
        <SolidColorBrush x:Key="StatusSuccess" Color="#22C55E"/>
        <SolidColorBrush x:Key="StatusSuccessBg" Color="#14532D"/>
        <SolidColorBrush x:Key="StatusWarning" Color="#F59E0B"/>
        <SolidColorBrush x:Key="StatusWarningBg" Color="#78350F"/>
        <SolidColorBrush x:Key="StatusInfo" Color="#3B82F6"/>
        <SolidColorBrush x:Key="StatusInfoBg" Color="#1E3A8A"/>
        <SolidColorBrush x:Key="StatusDanger" Color="#EF4444"/>
        <SolidColorBrush x:Key="StatusDangerBg" Color="#7F1D1D"/>
        <SolidColorBrush x:Key="StatusNeutral" Color="#94A3B8"/>
        <SolidColorBrush x:Key="StatusNeutralBg" Color="#374151"/>
        <SolidColorBrush x:Key="StatusTeal" Color="#14B8A6"/>
        <SolidColorBrush x:Key="StatusTealBg" Color="#134E4A"/>

        <!-- Stare nazwy dla kompatybilności -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#6366F1"/>
        <SolidColorBrush x:Key="PrimaryLight" Color="#312E81"/>
        <SolidColorBrush x:Key="TextDark" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextGray" Color="#94A3B8"/>
        <SolidColorBrush x:Key="BorderColor" Color="#334155"/>
        <SolidColorBrush x:Key="AccentRed" Color="#EF4444"/>
        <SolidColorBrush x:Key="AccentRedLight" Color="#7F1D1D"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.25" BlurRadius="15" ShadowDepth="0" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="KpiCard" TargetType="Border" BasedOn="{StaticResource CardStyle}">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="MinWidth" Value="150"/>
        </Style>

        <Style x:Key="BtnIcon" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#334155"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                            To="#7F1D1D" Duration="0:0:1.5"/>
        </Storyboard>

        <!-- Dark ComboBox Style -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"/>
                                                <Path Grid.Column="1" x:Name="Arrow" Fill="#94A3B8"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#475569"/>
                                                <Setter TargetName="Border" Property="BorderBrush" Value="#6366F1"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,4,24,4"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#1E293B" BorderBrush="#475569"
                                            BorderThickness="1" CornerRadius="6" Margin="0,2,0,0">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
                                        </Border.Effect>
                                        <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="2,1">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#334155"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#6366F1"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" CornerRadius="12" Margin="0,0,0,16" Padding="12" Background="#1E293B">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.25" BlurRadius="15" ShadowDepth="0" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo firmowe w nagłówku -->
                <Border Grid.Column="0" Margin="0,0,20,0" Padding="10,0" VerticalAlignment="Center">
                    <Border x:Name="borderLogo" MinWidth="80" MinHeight="60" MaxWidth="280" MaxHeight="80" CornerRadius="6" Background="#334155"
                            BorderBrush="#475569" BorderThickness="1" ToolTip="Prawy przycisk myszy aby zmienić logo" Cursor="Hand">
                        <Border.ContextMenu>
                            <ContextMenu Background="#1E293B" BorderBrush="#475569">
                                <MenuItem Header="Zmień logo z dysku sieciowego" Click="MenuZmienLogo_Click">
                                    <MenuItem.Icon>
                                        <Path Fill="#6366F1" Stretch="Uniform" Width="14" Height="14"
                                              Data="M19,13h-6v6h-2v-6H5v-2h6V5h2v6h6V13z"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="Usuń logo" Click="MenuUsunLogo_Click">
                                    <MenuItem.Icon>
                                        <Path Fill="#EF4444" Stretch="Uniform" Width="14" Height="14"
                                              Data="M6,19c0,1.1,0.9,2,2,2h8c1.1,0,2-0.9,2-2V7H6V19z M19,4h-3.5l-1-1h-5l-1,1H5v2h14V4z"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </Border.ContextMenu>
                        <Grid>
                            <Image x:Name="imgLogo" Stretch="Uniform" Margin="6"/>
                            <TextBlock x:Name="txtLogoPlaceholder" Text="LOGO" FontSize="14" FontWeight="Bold"
                                       Foreground="#64748B" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </Border>

                <!-- Hidden fields for compatibility -->
                <TextBlock x:Name="txtHeaderKlient" Visibility="Collapsed"/>
                <TextBlock x:Name="txtHeaderMiasto" Visibility="Collapsed"/>
                <TextBlock x:Name="txtHeaderTelefon" Visibility="Collapsed"/>
                <Border x:Name="panelNastepnyKontakt" Visibility="Collapsed">
                    <TextBlock x:Name="txtKlientNastepnyKontakt"/>
                </Border>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- Avatar i nazwa zalogowanego użytkownika z celami -->
                    <Border Background="#334155" CornerRadius="8" Padding="12,8" Margin="0,0,15,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="userAvatarBorder" Width="56" Height="56" CornerRadius="28" ClipToBounds="True" Margin="0,0,12,0" Background="#312E81"
                                    BorderBrush="#6366F1" BorderThickness="2">
                                <Image x:Name="imgUserAvatar" Stretch="UniformToFill"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                                <TextBlock x:Name="txtUserName" Text="Użytkownik" FontSize="16" FontWeight="Bold" Foreground="#E2E8F0"/>
                            </StackPanel>
                            <!-- Cel dzienny z pozycją -->
                            <Border x:Name="borderCelDzienny" Background="#14532D" CornerRadius="6" Padding="12,6" Margin="0,0,8,0">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock x:Name="txtPozycjaDzienna" Text="" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="txtCelDziennyLabel" Text="CEL DNIA" FontSize="9" FontWeight="Bold" Foreground="#22C55E" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock x:Name="txtCelDziennyWykonane" Text="0" FontSize="22" FontWeight="Black" Foreground="#22C55E"/>
                                        <TextBlock Text="/" FontSize="18" FontWeight="Bold" Foreground="#64748B" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="txtCelDziennyCel" Text="15" FontSize="18" FontWeight="Bold" Foreground="#64748B" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            <!-- Cel tygodniowy z pozycją -->
                            <Border x:Name="borderCelTygodniowy" Background="#1E3A8A" CornerRadius="6" Padding="12,6">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock x:Name="txtPozycjaTygodniowa" Text="" FontSize="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="txtCelTygodniowyLabel" Text="CEL TYGODNIA" FontSize="9" FontWeight="Bold" Foreground="#60A5FA" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock x:Name="txtCelTygodniowyWykonane" Text="0" FontSize="22" FontWeight="Black" Foreground="#60A5FA"/>
                                        <TextBlock Text="/" FontSize="18" FontWeight="Bold" Foreground="#64748B" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="txtCelTygodniowyCel" Text="120" FontSize="18" FontWeight="Bold" Foreground="#64748B" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <StackPanel VerticalAlignment="Center" Margin="0,0,15,0" Width="120" Visibility="Collapsed">
                        <Grid Margin="0,0,0,4">
                            <TextBlock x:Name="txtTargetLabel" Text="CEL DNIA (15)" FontSize="9" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="txtTargetInfo" Text="0/15" FontSize="10" FontWeight="Bold" Foreground="#6366F1" HorizontalAlignment="Right"/>
                        </Grid>
                        <ProgressBar x:Name="pbTarget" Height="6" BorderThickness="0" Background="#334155" Foreground="#6366F1" Maximum="15" Value="0">
                            <ProgressBar.Clip>
                                <RectangleGeometry Rect="0,0,120,6" RadiusX="3" RadiusY="3"/>
                            </ProgressBar.Clip>
                        </ProgressBar>
                    </StackPanel>

                    <Button x:Name="btnReminder" Content="🔔 Przypomnienia" Style="{StaticResource BtnIcon}" Click="BtnReminder_Click" ToolTip="Otwórz okno przypomnień" Foreground="#22C55E" FontWeight="SemiBold"/>
                    <Button Content="📊 Kanban" Style="{StaticResource BtnIcon}" Click="BtnKanban_Click"/>
                    <Button Content="📈 Dashboard" Style="{StaticResource BtnIcon}" Click="BtnDashboard_Click" Foreground="#EF4444" FontWeight="Bold"/>
                    <Button x:Name="btnManager" Content="👔 Szef" Style="{StaticResource BtnIcon}" Click="BtnManager_Click" Foreground="#EF4444" FontWeight="Bold"/>
                    <Button Content="🗺️ Mapa" Style="{StaticResource BtnIcon}" Click="BtnMapa_Click"/>
                    <Button Content="📥 Import" Style="{StaticResource BtnIcon}" Click="BtnImportFirm_Click"/>
                    <Button Content="👥 Duplikaty" Style="{StaticResource BtnIcon}" Click="BtnDuplicates_Click" ToolTip="Wykrywanie duplikatow"/>
                    <Button Content="📧 Email" Style="{StaticResource BtnIcon}" Click="BtnEmailTemplate_Click" ToolTip="Szablony email"/>
                    <Button x:Name="btnAdmin" Content="⚙️ Admin" Style="{StaticResource BtnIcon}" Click="BtnAdmin_Click"/>
                    <Button x:Name="btnToggleTheme" Content="☀️ Jasny" Style="{StaticResource BtnIcon}" Click="BtnToggleTheme_Click" ToolTip="Przełącz tryb jasny/ciemny"/>

                    <Button Content="+ Nowy" Click="BtnDodaj_Click" Margin="10,0,0,0" Padding="15,8" BorderThickness="0" Cursor="Hand" Foreground="White" FontWeight="Bold">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" CornerRadius="6" Background="#6366F1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#4F46E5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="460"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1E293B" CornerRadius="8" BorderBrush="#334155" BorderThickness="1" Margin="0,0,16,0" ClipToBounds="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Margin="12,10" Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Pierwszy wiersz: Tytuł, Szukaj, Liczba wyników -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Baza Odbiorców" FontWeight="Bold" FontSize="14" Foreground="#E2E8F0" VerticalAlignment="Center"/>
                            <Border Grid.Column="1" Background="#0F172A" CornerRadius="6" VerticalAlignment="Center" Height="30" Margin="12,0,0,0" BorderBrush="#334155" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#64748B" FontSize="12"/>
                                    <TextBox x:Name="txtSzukaj" Grid.Column="1" Background="Transparent" BorderThickness="0"
                                             VerticalContentAlignment="Center" FontSize="12" Padding="0,0,8,0" Foreground="#E2E8F0" CaretBrush="#E2E8F0"
                                             TextChanged="TxtSzukaj_TextChanged" Tag="Szukaj..."/>
                                </Grid>
                            </Border>

                            <!-- Filtry obok wyszukiwarki -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                                <Border x:Name="chipVIP" Background="#78350F" CornerRadius="10" Padding="6,3" Margin="0,0,4,0" Cursor="Hand" MouseLeftButtonDown="Chip_Click" Tag="VIP">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="⭐" FontSize="9" VerticalAlignment="Center"/>
                                        <TextBlock Text="VIP" FontSize="8" FontWeight="Bold" Foreground="#FBBF24" Margin="2,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <Border x:Name="chipPilne" Background="#7F1D1D" CornerRadius="10" Padding="6,3" Margin="0,0,4,0" Cursor="Hand" MouseLeftButtonDown="Chip_Click" Tag="Pilne">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🔥" FontSize="9" VerticalAlignment="Center"/>
                                        <TextBlock Text="Pilne" FontSize="8" FontWeight="Bold" Foreground="#FCA5A5" Margin="2,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <Border x:Name="chipPremium" Background="#312E81" CornerRadius="10" Padding="6,3" Margin="0,0,4,0" Cursor="Hand" MouseLeftButtonDown="Chip_Click" Tag="Premium">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="💎" FontSize="9" VerticalAlignment="Center"/>
                                        <TextBlock Text="Premi" FontSize="8" FontWeight="Bold" Foreground="#A5B4FC" Margin="2,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <Border x:Name="chipZalegle" Background="#7F1D1D" CornerRadius="10" Padding="6,3" Margin="0,0,8,0" Cursor="Hand" MouseLeftButtonDown="ChipZalegle_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="⚠️" FontSize="9" VerticalAlignment="Center"/>
                                        <TextBlock Text="Zaleg" FontSize="8" FontWeight="Bold" Foreground="#FCA5A5" Margin="2,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <TextBlock Text="Status:" FontSize="9" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                <ComboBox x:Name="cmbStatus" Style="{StaticResource DarkComboBox}" Width="100" Margin="0,0,6,0" SelectionChanged="CmbStatus_SelectionChanged" ItemContainerStyle="{StaticResource DarkComboBoxItem}"/>
                                <TextBlock Text="Woj:" FontSize="9" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                <ComboBox x:Name="cmbWojewodztwo" Style="{StaticResource DarkComboBox}" Width="90" Margin="0,0,6,0" SelectionChanged="CmbWojewodztwo_SelectionChanged" ItemContainerStyle="{StaticResource DarkComboBoxItem}"/>
                                <TextBlock Text="Branża:" FontSize="9" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                <ComboBox x:Name="cmbBranza" Style="{StaticResource DarkComboBox}" Width="110" Margin="0,0,6,0" SelectionChanged="CmbBranza_SelectionChanged" ItemContainerStyle="{StaticResource DarkComboBoxItem}"/>
                                <TextBlock Text="Handlowiec:" FontSize="9" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,3,0"/>
                                <ComboBox x:Name="cmbHandlowiec" Style="{StaticResource DarkComboBox}" Width="120" Margin="0,0,6,0" SelectionChanged="CmbHandlowiec_SelectionChanged" ItemContainerStyle="{StaticResource DarkComboBoxItem}"/>
                                <CheckBox x:Name="chkTylkoMoje" Content="Moi" FontSize="9" Foreground="#6366F1" FontWeight="SemiBold" VerticalContentAlignment="Center" Margin="2,0,0,0" Checked="ChkTylkoMoje_Changed" Unchecked="ChkTylkoMoje_Changed"/>
                            </StackPanel>

                            <TextBlock x:Name="txtLiczbaWynikow" Grid.Column="3" Text="0 klientów" HorizontalAlignment="Right" FontSize="11" Foreground="#64748B" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>

                    <DataGrid x:Name="dgKontakty" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="None"
                              BorderThickness="0" Background="#1E293B" RowBackground="#1E293B" AlternatingRowBackground="#273548" RowHeaderWidth="0"
                              SelectionMode="Single" SelectionUnit="FullRow"
                              SelectionChanged="DgKontakty_SelectionChanged"
                              MouseDoubleClick="DgKontakty_MouseDoubleClick">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#0F172A"/>
                                <Setter Property="Foreground" Value="#94A3B8"/>
                                <Setter Property="Padding" Value="12,14"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                <Setter Property="BorderBrush" Value="#334155"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Padding" Value="8,4"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#E2E8F0"/>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Height" Value="60"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Background" Value="#1E293B"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="Margin" Value="2,1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#334155"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#1E3A5F"/>
                                        <Setter Property="BorderBrush" Value="#60A5FA"/>
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#60A5FA" BlurRadius="15" ShadowDepth="0" Opacity="0.7"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                                <Style.Resources>
                                    <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever" AutoReverse="True">
                                        <DoubleAnimation Storyboard.TargetProperty="(Effect).(DropShadowEffect.BlurRadius)"
                                                         From="10" To="25" Duration="0:0:0.8"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(Effect).(DropShadowEffect.Opacity)"
                                                         From="0.5" To="0.9" Duration="0:0:0.8"/>
                                    </Storyboard>
                                </Style.Resources>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <!-- Kolumna walidacji - ikona ostrzeżenia przy brakujących danych -->
                            <DataGridTemplateColumn Header="⚠️" Width="35" CanUserResize="False">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="⚠️" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Foreground="#F59E0B" Cursor="Help">
                                            <TextBlock.Visibility>
                                                <MultiBinding Converter="{StaticResource ValidationWarningVisibilityConverter}">
                                                    <Binding Path="TELEFON_K"/>
                                                    <Binding Path="EMAIL"/>
                                                    <Binding Path="OstatniaZmiana"/>
                                                </MultiBinding>
                                            </TextBlock.Visibility>
                                            <TextBlock.ToolTip>
                                                <MultiBinding Converter="{StaticResource ValidationWarningConverter}">
                                                    <Binding Path="TELEFON_K"/>
                                                    <Binding Path="EMAIL"/>
                                                    <Binding Path="OstatniaZmiana"/>
                                                </MultiBinding>
                                            </TextBlock.ToolTip>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Status" Width="155" SortMemberPath="Status">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="6,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#374151"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Do zadzwonienia">
                                                            <Setter Property="Background" Value="#374151"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Próba kontaktu">
                                                            <Setter Property="Background" Value="#78350F"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Nawiązano kontakt">
                                                            <Setter Property="Background" Value="#14532D"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Zgoda na dalszy kontakt">
                                                            <Setter Property="Background" Value="#134E4A"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Do wysłania oferta">
                                                            <Setter Property="Background" Value="#1E3A8A"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Nie zainteresowany">
                                                            <Setter Property="Background" Value="#7F1D1D"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock VerticalAlignment="Center" FontSize="14" Margin="0,0,6,0">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="📋"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Do zadzwonienia">
                                                                    <Setter Property="Text" Value="📞"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Próba kontaktu">
                                                                    <Setter Property="Text" Value="⏳"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Nawiązano kontakt">
                                                                    <Setter Property="Text" Value="✅"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Zgoda na dalszy kontakt">
                                                                    <Setter Property="Text" Value="🤝"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Do wysłania oferta">
                                                                    <Setter Property="Text" Value="📄"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Nie zainteresowany">
                                                                    <Setter Property="Text" Value="❌"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#94A3B8"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Do zadzwonienia">
                                                                    <Setter Property="Foreground" Value="#94A3B8"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Próba kontaktu">
                                                                    <Setter Property="Foreground" Value="#FCD34D"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Nawiązano kontakt">
                                                                    <Setter Property="Foreground" Value="#4ADE80"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Zgoda na dalszy kontakt">
                                                                    <Setter Property="Foreground" Value="#2DD4BF"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Do wysłania oferta">
                                                                    <Setter Property="Foreground" Value="#60A5FA"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Nie zainteresowany">
                                                                    <Setter Property="Foreground" Value="#FCA5A5"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Firma" Width="240" SortMemberPath="NAZWA">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4,0">
                                            <!-- Tag na poczatku -->
                                            <Border Background="#78350F" CornerRadius="4" Padding="5,2" Margin="0,0,6,0"
                                                    Visibility="{Binding MaTagi, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Text="{Binding Tagi}" FontSize="9" Foreground="#FBBF24" FontWeight="Bold"/>
                                            </Border>
                                            <!-- Nazwa firmy -->
                                            <TextBlock Text="{Binding NAZWA}" FontWeight="SemiBold" FontSize="13"
                                                       TextWrapping="Wrap" TextAlignment="Left" VerticalAlignment="Center"
                                                       MaxWidth="240" MaxHeight="50">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#E2E8F0"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Handl." Width="70" SortMemberPath="OstatniHandlowiec">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center"
                                                    ToolTip="{Binding OstatniHandlowiec}">
                                            <Border Width="28" Height="28" CornerRadius="14" ClipToBounds="True"
                                                    HorizontalAlignment="Center" Margin="0,0,0,2">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#312E81"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding OstatniHandlowiec}" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding OstatniHandlowiec}" Value="">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Image x:Name="imgHandlowiecAvatar" Stretch="UniformToFill"
                                                       Source="{Binding OstatniHandlowiec, Converter={StaticResource HandlowiecAvatarConverter}}"/>
                                            </Border>
                                            <TextBlock Text="{Binding OstatniHandlowiec}" FontSize="9" FontWeight="SemiBold"
                                                       TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center"
                                                       MaxWidth="80" MaxHeight="28" TextTrimming="CharacterEllipsis">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#A5B4FC"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Miasto" Width="100" SortMemberPath="MIASTO">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding MIASTO}" TextWrapping="Wrap" TextAlignment="Center"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#E2E8F0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Woj." Width="90" SortMemberPath="Wojewodztwo">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Wojewodztwo}" TextWrapping="Wrap" TextAlignment="Center"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#E2E8F0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Branża" Width="140" SortMemberPath="PKD_Opis">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding PKD_Opis}" FontSize="10" TextWrapping="Wrap" TextAlignment="Center"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="{Binding PKD_Opis}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#E2E8F0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Telefon" Width="150" SortMemberPath="TELEFON_K">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding TELEFON_K, Converter={StaticResource PhoneFormatConverter}}" FontWeight="Bold" FontSize="15" TextAlignment="Center"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#E2E8F0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Setter Property="FontSize" Value="18"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Km" Width="50" SortMemberPath="Km">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Km}" FontWeight="SemiBold" TextAlignment="Center"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#60A5FA"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Ost. Zmiana" Width="115" SortMemberPath="OstatniaZmiana">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding OstatniaZmiana, Converter={StaticResource DateWithDayConverter}}" TextAlignment="Center"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#E2E8F0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Ost. notatka" Width="180" SortMemberPath="OstatniNotatka">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding OstatniNotatka}" FontSize="10" Foreground="#94A3B8"
                                                   TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center" Margin="4,0"
                                                   ToolTip="{Binding OstatniNotatka}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>

                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="📞 Zadzwoń" Click="MenuZadzwon_Click"/>
                                <MenuItem Header="✏️ Edytuj" Click="MenuEdytuj_Click"/>
                                <MenuItem Header="📅 Ustaw datę kontaktu" Click="MenuUstawDate_Click"/>
                                <Separator/>
                                <MenuItem Header="🔄 Zmień status">
                                    <MenuItem Header="📞 Do zadzwonienia" Click="MenuZmienStatus_Click" Tag="Do zadzwonienia"/>
                                    <MenuItem Header="⏳ Próba kontaktu" Click="MenuZmienStatus_Click" Tag="Próba kontaktu"/>
                                    <MenuItem Header="✅ Nawiązano kontakt" Click="MenuZmienStatus_Click" Tag="Nawiązano kontakt"/>
                                    <MenuItem Header="🤝 Zgoda na dalszy kontakt" Click="MenuZmienStatus_Click" Tag="Zgoda na dalszy kontakt"/>
                                    <MenuItem Header="📄 Do wysłania oferta" Click="MenuZmienStatus_Click" Tag="Do wysłania oferta"/>
                                    <Separator/>
                                    <MenuItem Header="❌ Nie zainteresowany" Click="MenuZmienStatus_Click" Tag="Nie zainteresowany"/>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="🏷️ Otaguj">
                                    <MenuItem Header="⭐ VIP" Click="MenuTag_Click" Tag="VIP"/>
                                    <MenuItem Header="🔥 Pilne" Click="MenuTag_Click" Tag="Pilne"/>
                                    <MenuItem Header="💎 Premium" Click="MenuTag_Click" Tag="Premium"/>
                                    <Separator/>
                                    <MenuItem Header="🗑️ Wyczyść tagi" Click="MenuTag_Click" Tag="CLEAR"/>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="🔍 Szukaj w Google" Click="MenuGoogle_Click"/>
                                <MenuItem Header="🗺️ Pokaż trasę" Click="MenuTrasa_Click"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="Transparent">
                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                    <StackPanel>

                        <!-- Klient - kompaktowy z telefonem po prawej i ikonkami -->
                        <Border Style="{StaticResource CardStyle}" Padding="12,8" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock x:Name="txtPanelKlientNazwa" Text="Wybierz klienta z listy..." FontSize="13" FontWeight="Bold" Foreground="#E2E8F0" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock x:Name="txtPanelKlientMiasto" Text="" FontSize="11" Foreground="#94A3B8" Margin="0,2,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" x:Name="txtPanelKlientTelefon" Text="" FontSize="18" FontWeight="Black" Foreground="#22C55E"
                                           VerticalAlignment="Center" Margin="10,0,10,0" Cursor="Hand" MouseLeftButtonDown="TxtKlientTelefon_Click" ToolTip="Kliknij aby zadzwonić"/>

                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button x:Name="btnHeaderCall" Style="{StaticResource BtnIcon}" ToolTip="Zadzwoń" Click="BtnKlientZadzwon_Click" Padding="6,4">
                                        <Path Data="M6.62,10.79C8.06,13.62 10.38,15.94 13.21,17.38L15.41,15.18C15.69,14.9 16.08,14.82 16.43,14.93C17.55,15.3 18.75,15.5 20,15.5A1,1 0 0,1 21,16.5V20A1,1 0 0,1 20,21A17,17 0 0,1 3,4A1,1 0 0,1 4,3H7.5A1,1 0 0,1 8.5,4C8.5,5.25 8.7,6.45 9.07,7.57C9.18,7.92 9.1,8.31 8.82,8.59L6.62,10.79Z"
                                              Fill="#22C55E" Stretch="Uniform" Width="16" Height="16"/>
                                    </Button>
                                    <Button x:Name="btnHeaderEdit" Style="{StaticResource BtnIcon}" ToolTip="Edytuj" Click="BtnKlientEdytuj_Click" Padding="6,4">
                                        <Path Data="M3,17.25V21h3.75L17.81,9.94l-3.75-3.75L3,17.25zM20.71,7.04c0.39-0.39,0.39-1.02,0-1.41l-2.34-2.34c-0.39-0.39-1.02-0.39-1.41,0l-1.83,1.83l3.75,3.75L20.71,7.04z"
                                              Fill="#60A5FA" Stretch="Uniform" Width="16" Height="16"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- SZYBKIE PRZYCISKI STATUSU -->
                        <Border Style="{StaticResource CardStyle}" Padding="10,8" Margin="0,0,0,10" x:Name="panelQuickStatus">
                            <StackPanel>
                                <TextBlock Text="SZYBKA ZMIANA STATUSU" FontSize="10" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,6"/>
                                <WrapPanel>
                                    <Button Style="{StaticResource BtnIcon}" Padding="8,4" Margin="2" Tag="Do zadzwonienia" Click="BtnQuickStatus_Click" ToolTip="Do zadzwonienia">
                                        <StackPanel Orientation="Horizontal">
                                            <Path Fill="#22C55E" Stretch="Uniform" Width="12" Height="12" Margin="0,0,4,0"
                                                  Data="M6.62,10.79C8.06,13.62 10.38,15.94 13.21,17.38L15.41,15.18C15.69,14.9 16.08,14.82 16.43,14.93C17.55,15.3 18.75,15.5 20,15.5A1,1 0 0,1 21,16.5V20A1,1 0 0,1 20,21A17,17 0 0,1 3,4A1,1 0 0,1 4,3H7.5A1,1 0 0,1 8.5,4C8.5,5.25 8.7,6.45 9.07,7.57C9.18,7.92 9.1,8.31 8.82,8.59L6.62,10.79Z"/>
                                            <TextBlock Text="Zadzwoń" FontSize="10" Foreground="#E2E8F0"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource BtnIcon}" Padding="8,4" Margin="2" Tag="Próba kontaktu" Click="BtnQuickStatus_Click" ToolTip="Próba kontaktu">
                                        <StackPanel Orientation="Horizontal">
                                            <Path Fill="#F59E0B" Stretch="Uniform" Width="12" Height="12" Margin="0,0,4,0"
                                                  Data="M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4M12,6A6,6 0 0,0 6,12A6,6 0 0,0 12,18A6,6 0 0,0 18,12H12V6Z"/>
                                            <TextBlock Text="Próba" FontSize="10" Foreground="#E2E8F0"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource BtnIcon}" Padding="8,4" Margin="2" Tag="Nawiązano kontakt" Click="BtnQuickStatus_Click" ToolTip="Nawiązano kontakt">
                                        <StackPanel Orientation="Horizontal">
                                            <Path Fill="#3B82F6" Stretch="Uniform" Width="12" Height="12" Margin="0,0,4,0"
                                                  Data="M21,19V20H3V19L5,17V11C5,7.9 7.03,5.17 10,4.29C10,4.19 10,4.1 10,4A2,2 0 0,1 12,2A2,2 0 0,1 14,4C14,4.1 14,4.19 14,4.29C16.97,5.17 19,7.9 19,11V17L21,19M14,21A2,2 0 0,1 12,23A2,2 0 0,1 10,21"/>
                                            <TextBlock Text="Kontakt" FontSize="10" Foreground="#E2E8F0"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource BtnIcon}" Padding="8,4" Margin="2" Tag="Zgoda na dalszy kontakt" Click="BtnQuickStatus_Click" ToolTip="Zgoda na dalszy kontakt">
                                        <StackPanel Orientation="Horizontal">
                                            <Path Fill="#8B5CF6" Stretch="Uniform" Width="12" Height="12" Margin="0,0,4,0"
                                                  Data="M12 2C6.5 2 2 6.5 2 12S6.5 22 12 22 22 17.5 22 12 17.5 2 12 2M10 17L5 12L6.41 10.59L10 14.17L17.59 6.58L19 8L10 17Z"/>
                                            <TextBlock Text="Zgoda" FontSize="10" Foreground="#E2E8F0"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource BtnIcon}" Padding="8,4" Margin="2" Tag="Do wysłania oferta" Click="BtnQuickStatus_Click" ToolTip="Do wysłania oferta">
                                        <StackPanel Orientation="Horizontal">
                                            <Path Fill="#06B6D4" Stretch="Uniform" Width="12" Height="12" Margin="0,0,4,0"
                                                  Data="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M18,20H6V4H13V9H18V20Z"/>
                                            <TextBlock Text="Oferta" FontSize="10" Foreground="#E2E8F0"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource BtnIcon}" Padding="8,4" Margin="2" Tag="Nie zainteresowany" Click="BtnQuickStatus_Click" ToolTip="Nie zainteresowany">
                                        <StackPanel Orientation="Horizontal">
                                            <Path Fill="#EF4444" Stretch="Uniform" Width="12" Height="12" Margin="0,0,4,0"
                                                  Data="M12,2C17.53,2 22,6.47 22,12C22,17.53 17.53,22 12,22C6.47,22 2,17.53 2,12C2,6.47 6.47,2 12,2M15.59,7L12,10.59L8.41,7L7,8.41L10.59,12L7,15.59L8.41,17L12,13.41L15.59,17L17,15.59L13.41,12L17,8.41L15.59,7Z"/>
                                            <TextBlock Text="Nie" FontSize="10" Foreground="#E2E8F0"/>
                                        </StackPanel>
                                    </Button>
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Padding="12,10" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="NOTATKI / HISTORIA" FontSize="12" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,8"/>
                                <Border Background="#0F172A" CornerRadius="6" Height="350" BorderBrush="#334155" BorderThickness="1" Padding="6">
                                    <ScrollViewer>
                                        <ItemsControl x:Name="listaNotatek">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="#1E293B" CornerRadius="6" Padding="10,8" Margin="2,2,2,6" BorderBrush="#334155" BorderThickness="1">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <!-- Avatar - większy -->
                                                            <Border Width="40" Height="40" CornerRadius="20" ClipToBounds="True"
                                                                    VerticalAlignment="Top" Margin="0,2,10,0" Background="#312E81">
                                                                <Image x:Name="imgNotatkaAvatar" Stretch="UniformToFill"/>
                                                            </Border>
                                                            <StackPanel Grid.Column="1">
                                                                <StackPanel Orientation="Horizontal" Cursor="Hand" MouseLeftButtonUp="NotatkaText_Click" Tag="{Binding}">
                                                                    <Path Data="{Binding Typ, Converter={StaticResource NoteTypeToPathConverter}}"
                                                                          Fill="White" Stretch="Uniform" Width="14" Height="14"
                                                                          Margin="0,2,8,0" VerticalAlignment="Top"/>
                                                                    <TextBlock Text="{Binding Tresc}" TextWrapping="Wrap" FontSize="14" Foreground="#E2E8F0"
                                                                               MaxHeight="{Binding MaxHeight}" TextTrimming="CharacterEllipsis" ToolTip="Kliknij aby rozwinąć/zwinąć"/>
                                                                </StackPanel>
                                                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                                    <TextBlock Text="{Binding Operator}" FontSize="12" FontWeight="Bold" Foreground="#6366F1"/>
                                                                    <TextBlock Text=" • " FontSize="12" Foreground="#64748B"/>
                                                                    <TextBlock Text="{Binding DataUtworzenia, StringFormat='dd.MM HH:mm'}" FontSize="12" Foreground="#64748B"/>
                                                                </StackPanel>
                                                            </StackPanel>
                                                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top"
                                                                        Visibility="{Binding CzyNotatka, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                <Button Padding="6,3" Margin="0,0,5,0"
                                                                        Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                                        Tag="{Binding}" Click="BtnEdytujNotatke_Click" ToolTip="Edytuj">
                                                                    <Path Data="M3,17.25V21h3.75L17.81,9.94l-3.75-3.75L3,17.25zM20.71,7.04c0.39-0.39,0.39-1.02,0-1.41l-2.34-2.34c-0.39-0.39-1.02-0.39-1.41,0l-1.83,1.83l3.75,3.75L20.71,7.04z"
                                                                          Fill="White" Stretch="Uniform" Width="14" Height="14"/>
                                                                </Button>
                                                                <Button Padding="6,3"
                                                                        Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                                        Tag="{Binding}" Click="BtnUsunNotatke_Click" ToolTip="Usuń">
                                                                    <Path Data="M6,19c0,1.1,0.9,2,2,2h8c1.1,0,2-0.9,2-2V7H6V19zM19,4h-3.5l-1-1h-5l-1,1H5v2h14V4z"
                                                                          Fill="White" Stretch="Uniform" Width="14" Height="14"/>
                                                                </Button>
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Border>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="txtNowaNotatka" Background="#334155" Foreground="#E2E8F0" BorderThickness="0" Padding="6" VerticalContentAlignment="Center" FontSize="11" CaretBrush="#E2E8F0" Tag="Wpisz notatkę..."/>
                                    <Button x:Name="btnDodajNotatke" Grid.Column="1" Content="+" Style="{StaticResource BtnPrimary}" Margin="6,0,0,0" Padding="10,4" Click="BtnDodajNotatke_Click"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Ranking dzienny i tygodniowy obok siebie -->
                        <Border Style="{StaticResource CardStyle}" Padding="12,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- RANKING DZIENNY -->
                                <StackPanel Grid.Column="0" Margin="0,0,6,0">
                                    <TextBlock x:Name="txtRankingTytul" Text="RANKING DZIENNY" FontSize="12" FontWeight="Bold" Foreground="#22C55E" Margin="0,0,0,8"/>
                                    <ItemsControl x:Name="listaRankingDzienny" BorderThickness="0" Background="Transparent">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#0F172A" CornerRadius="6" Padding="8,6" Margin="0,0,0,4" BorderBrush="#334155" BorderThickness="1">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="24"/>
                                                            <ColumnDefinition Width="32"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <!-- Pozycja z medalem - kolorowa -->
                                                        <TextBlock Text="{Binding Medal, Converter={StaticResource MedalTextConverter}}"
                                                                   Foreground="{Binding Medal, Converter={StaticResource MedalColorConverter}}"
                                                                   HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" FontWeight="Bold"/>

                                                        <!-- Avatar -->
                                                        <Border Grid.Column="1" Width="26" Height="26" CornerRadius="13" ClipToBounds="True"
                                                                VerticalAlignment="Center" Margin="4,0,0,0" Background="#312E81">
                                                            <Image Stretch="UniformToFill" Source="{Binding Operator, Converter={StaticResource HandlowiecAvatarConverter}}"/>
                                                        </Border>

                                                        <!-- Nazwa -->
                                                        <TextBlock Grid.Column="2" Text="{Binding Operator}" FontWeight="SemiBold" FontSize="11" Foreground="#E2E8F0"
                                                                   VerticalAlignment="Center" Margin="6,0,0,0" TextTrimming="CharacterEllipsis"/>

                                                        <!-- Procent -->
                                                        <TextBlock Grid.Column="3" Text="{Binding ProcentTekst}" FontSize="14" FontWeight="Bold" Foreground="#22C55E" VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- RANKING TYGODNIOWY -->
                                <StackPanel Grid.Column="1" Margin="6,0,0,0">
                                    <TextBlock Text="RANKING TYGODNIOWY" FontSize="12" FontWeight="Bold" Foreground="#60A5FA" Margin="0,0,0,8"/>
                                    <ItemsControl x:Name="listaRankingTygodniowy" BorderThickness="0" Background="Transparent">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#0F172A" CornerRadius="6" Padding="8,6" Margin="0,0,0,4" BorderBrush="#334155" BorderThickness="1">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="24"/>
                                                            <ColumnDefinition Width="32"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <!-- Pozycja z medalem - kolorowa -->
                                                        <TextBlock Text="{Binding Medal, Converter={StaticResource MedalTextConverter}}"
                                                                   Foreground="{Binding Medal, Converter={StaticResource MedalColorConverter}}"
                                                                   HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" FontWeight="Bold"/>

                                                        <!-- Avatar -->
                                                        <Border Grid.Column="1" Width="26" Height="26" CornerRadius="13" ClipToBounds="True"
                                                                VerticalAlignment="Center" Margin="4,0,0,0" Background="#312E81">
                                                            <Image Stretch="UniformToFill" Source="{Binding Operator, Converter={StaticResource HandlowiecAvatarConverter}}"/>
                                                        </Border>

                                                        <!-- Nazwa -->
                                                        <TextBlock Grid.Column="2" Text="{Binding Operator}" FontWeight="SemiBold" FontSize="11" Foreground="#E2E8F0"
                                                                   VerticalAlignment="Center" Margin="6,0,0,0" TextTrimming="CharacterEllipsis"/>

                                                        <!-- Procent -->
                                                        <TextBlock Grid.Column="3" Text="{Binding ProcentTekst}" FontSize="14" FontWeight="Bold" Foreground="#60A5FA" VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- Ukryte elementy dla kompatybilności -->
                                <RadioButton x:Name="rbDzienny" Visibility="Collapsed" IsChecked="True" GroupName="OkresRankingu" Checked="RbOkresRankingu_Checked"/>
                                <RadioButton x:Name="rbTygodniowy" Visibility="Collapsed" GroupName="OkresRankingu" Checked="RbOkresRankingu_Checked"/>
                                <ListBox x:Name="listaRanking" Visibility="Collapsed" MouseDoubleClick="ListaRanking_MouseDoubleClick"/>
                            </Grid>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border x:Name="toastPopup" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,40"
                Background="#1E293B" CornerRadius="25" Padding="25,12" Opacity="0" IsHitTestVisible="False" BorderBrush="#334155" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="10"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="✅ " Foreground="#22C55E" FontSize="14"/>
                <TextBlock x:Name="toastText" Text="Wykonano" Foreground="#E2E8F0" FontWeight="SemiBold" FontSize="14"/>
            </StackPanel>
        </Border>


        <Border x:Name="loadingOverlay" Grid.RowSpan="2" Background="#E60F172A" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="#6366F1" Background="#334155" BorderThickness="0"/>
                <TextBlock Text="Wczytywanie bazy..." Foreground="#94A3B8" Margin="0,15,0,0" HorizontalAlignment="Center" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
