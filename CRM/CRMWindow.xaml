<Window x:Class="Kalendarz1.CRM.CRMWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CRM Manager Pro - Edycja Green"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#F9FAFB"
        FontFamily="Segoe UI">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <SolidColorBrush x:Key="PrimaryColor" Color="#16A34A"/>
        <SolidColorBrush x:Key="PrimaryLight" Color="#DCFCE7"/>
        <SolidColorBrush x:Key="DangerColor" Color="#DC2626"/>
        <SolidColorBrush x:Key="TextDark" Color="#111827"/>
        <SolidColorBrush x:Key="TextGray" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.04" BlurRadius="10" ShadowDepth="2" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="KpiCard" TargetType="Border" BasedOn="{StaticResource CardStyle}">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="MinWidth" Value="150"/>
        </Style>

        <Style x:Key="BtnIcon" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                            To="#FEF2F2" Duration="0:0:1.5"/>
        </Storyboard>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Image Source="logo-2-green.png" Height="40" Margin="5,0,15,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="CRM" FontWeight="Black" FontSize="20" Foreground="{StaticResource PrimaryColor}"/>
                        <TextBlock Text="Manager" FontSize="10" Foreground="{StaticResource TextGray}"/>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Column="1" Background="#F3F4F6" CornerRadius="8" VerticalAlignment="Center" Height="42">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🔍" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource TextGray}"/>
                        <TextBox x:Name="txtSzukaj" Grid.Column="1" Background="Transparent" BorderThickness="0" 
                                 VerticalContentAlignment="Center" FontSize="14" Padding="0,0,10,0"
                                 TextChanged="TxtSzukaj_TextChanged" Tag="Szukaj firmy..."/>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Margin="20,0" Padding="20,0" BorderBrush="{StaticResource BorderColor}" BorderThickness="1,0,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="AKTYWNY:" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextGray}" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtHeaderKlient" Text="Wybierz z listy..." FontWeight="Bold" FontSize="15" Foreground="{StaticResource TextDark}" TextTrimming="CharacterEllipsis" MaxWidth="200"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock x:Name="txtHeaderMiasto" Text="" FontSize="12" Foreground="{StaticResource TextGray}" Margin="0,0,15,0"/>
                                <TextBlock x:Name="txtHeaderTelefon" Text="" FontSize="13" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Planowany kontakt -->
                        <Border x:Name="panelNastepnyKontakt" Grid.Column="1" Background="#F3F4F6" CornerRadius="6" Padding="10,6" Margin="15,0" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📅" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <StackPanel>
                                    <TextBlock Text="KONTAKT" FontSize="8" FontWeight="Bold" Foreground="{StaticResource TextGray}"/>
                                    <TextBlock x:Name="txtKlientNastepnyKontakt" Text="Brak" FontWeight="Bold" FontSize="11" Foreground="{StaticResource TextDark}"/>
                                </StackPanel>
                                <Button Content="📅" Style="{StaticResource BtnIcon}" ToolTip="Zmień datę kontaktu" Click="MenuUstawDate_Click" Padding="4" Margin="6,0,0,0"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button x:Name="btnHeaderCall" Content="📞" Style="{StaticResource BtnIcon}" ToolTip="Zadzwoń" Click="BtnKlientZadzwon_Click"/>
                            <Button x:Name="btnHeaderEdit" Content="✏️" Style="{StaticResource BtnIcon}" ToolTip="Edytuj" Click="BtnKlientEdytuj_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">

                    <StackPanel VerticalAlignment="Center" Margin="0,0,20,0" Width="120">
                        <Grid Margin="0,0,0,4">
                            <TextBlock Text="CEL DNIA (15)" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextGray}"/>
                            <TextBlock x:Name="txtTargetInfo" Text="0/15" FontSize="10" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Right"/>
                        </Grid>
                        <ProgressBar x:Name="pbTarget" Height="6" BorderThickness="0" Background="#F3F4F6" Foreground="{StaticResource PrimaryColor}" Maximum="15" Value="0">
                            <ProgressBar.Clip>
                                <RectangleGeometry Rect="0,0,120,6" RadiusX="3" RadiusY="3"/>
                            </ProgressBar.Clip>
                        </ProgressBar>
                    </StackPanel>

                    <Button Content="📊 Kanban" Style="{StaticResource BtnIcon}" Click="BtnKanban_Click"/>
                    <Button x:Name="btnManager" Content="📈 Szef" Style="{StaticResource BtnIcon}" Click="BtnManager_Click" Foreground="{StaticResource DangerColor}" FontWeight="Bold"/>
                    <Button Content="🗺️ Mapa" Style="{StaticResource BtnIcon}" Click="BtnMapa_Click"/>
                    <Button x:Name="btnAdmin" Content="⚙️" Style="{StaticResource BtnIcon}" Click="BtnAdmin_Click" Visibility="Collapsed"/>

                    <Button Content="+ Nowy" Style="{StaticResource BtnPrimary}" Click="BtnDodaj_Click" Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Border Style="{StaticResource KpiCard}">
                    <StackPanel>
                        <TextBlock Text="DO ZADZWONIENIA" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextGray}"/>
                        <Grid Margin="0,5,0,0">
                            <TextBlock Text="📞" FontSize="18" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtKpiDzisiaj" Text="0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCard}">
                    <StackPanel>
                        <TextBlock Text="ZALEGŁE" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextGray}"/>
                        <Grid Margin="0,5,0,0">
                            <TextBlock Text="⚠️" FontSize="18" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtKpiZalegle" Text="0" FontSize="22" FontWeight="Bold" Foreground="{StaticResource DangerColor}" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCard}">
                    <StackPanel>
                        <TextBlock Text="OTWARTE OFERTY" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextGray}"/>
                        <Grid Margin="0,5,0,0">
                            <TextBlock Text="📄" FontSize="18" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtKpiOferty" Text="0" FontSize="22" FontWeight="Bold" Foreground="#2563EB" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <ComboBox x:Name="cmbStatus" Width="150" Margin="5,0" Padding="6" SelectionChanged="CmbStatus_SelectionChanged"/>
                <ComboBox x:Name="cmbWojewodztwo" Width="130" Margin="5,0" Padding="6" SelectionChanged="CmbWojewodztwo_SelectionChanged"/>
                <ComboBox x:Name="cmbBranza" Width="160" Margin="5,0" Padding="6" SelectionChanged="CmbBranza_SelectionChanged"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="460"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Margin="0,0,16,0" ClipToBounds="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Margin="16,12" Grid.Row="0">
                        <TextBlock Text="Baza Odbiorców" FontWeight="Bold" FontSize="16" Foreground="{StaticResource TextDark}"/>
                        <TextBlock x:Name="txtLiczbaWynikow" Text="0 klientów" HorizontalAlignment="Right" FontSize="12" Foreground="{StaticResource TextGray}" VerticalAlignment="Center"/>
                    </Grid>

                    <DataGrid x:Name="dgKontakty" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#F3F4F6" VerticalGridLinesBrush="Transparent"
                              BorderThickness="0" Background="White" RowHeaderWidth="0"
                              SelectionMode="Single" SelectionUnit="FullRow"
                              SelectionChanged="DgKontakty_SelectionChanged"
                              MouseDoubleClick="DgKontakty_MouseDoubleClick">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F9FAFB"/>
                                <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
                                <Setter Property="Padding" Value="10,12"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Padding" Value="0,6"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource PrimaryLight}"/>
                                        <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding CzyZaniedbany}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard Storyboard="{StaticResource PulseAnimation}"/>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Status" Width="155">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="6,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#F1F5F9"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Do zadzwonienia">
                                                            <Setter Property="Background" Value="#F1F5F9"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Próba kontaktu">
                                                            <Setter Property="Background" Value="#FFEDD5"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Nawiązano kontakt">
                                                            <Setter Property="Background" Value="#DCFCE7"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Zgoda na dalszy kontakt">
                                                            <Setter Property="Background" Value="#CCFBF1"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Do wysłania oferta">
                                                            <Setter Property="Background" Value="#DBEAFE"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Nie zainteresowany">
                                                            <Setter Property="Background" Value="#FEE2E2"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock VerticalAlignment="Center" FontSize="11" Margin="0,0,4,0">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="📋"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Do zadzwonienia">
                                                                    <Setter Property="Text" Value="📞"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Próba kontaktu">
                                                                    <Setter Property="Text" Value="⏳"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Nawiązano kontakt">
                                                                    <Setter Property="Text" Value="✅"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Zgoda na dalszy kontakt">
                                                                    <Setter Property="Text" Value="🤝"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Do wysłania oferta">
                                                                    <Setter Property="Text" Value="📄"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Nie zainteresowany">
                                                                    <Setter Property="Text" Value="❌"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBlock Text="{Binding Status}" FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#64748B"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Do zadzwonienia">
                                                                    <Setter Property="Foreground" Value="#64748B"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Próba kontaktu">
                                                                    <Setter Property="Foreground" Value="#9A3412"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Nawiązano kontakt">
                                                                    <Setter Property="Foreground" Value="#166534"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Zgoda na dalszy kontakt">
                                                                    <Setter Property="Foreground" Value="#0D9488"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Do wysłania oferta">
                                                                    <Setter Property="Foreground" Value="#1E40AF"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Nie zainteresowany">
                                                                    <Setter Property="Foreground" Value="#991B1B"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Firma" Binding="{Binding NAZWA}" Width="*" FontWeight="SemiBold" Foreground="{StaticResource TextDark}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="Handlowiec" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding OstatniHandlowiec}" FontSize="10" Foreground="#6366F1" FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                                                   ToolTip="{Binding OstatniHandlowiec}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Tagi" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#FEF3C7" CornerRadius="4" Padding="6,2" Margin="0,0,10,0" HorizontalAlignment="Left" Visibility="{Binding MaTagi, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Text="{Binding Tagi}" FontSize="10" Foreground="#92400E" FontWeight="Bold"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Miasto" Binding="{Binding MIASTO}" Width="100" Foreground="{StaticResource TextGray}"/>

                            <DataGridTextColumn Header="Woj." Binding="{Binding Wojewodztwo}" Width="90" Foreground="{StaticResource TextGray}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="Branża" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding PKD_Opis}" FontSize="10" Foreground="#6B7280"
                                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                                                   ToolTip="{Binding PKD_Opis}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Telefon" Binding="{Binding TELEFON_K}" Width="120" FontWeight="Bold" FontSize="13" Foreground="{StaticResource PrimaryColor}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150" Foreground="{StaticResource TextGray}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Ost. Zmiana" Binding="{Binding OstatniaZmiana, StringFormat=dd.MM.yyyy}" Width="90"/>
                        </DataGrid.Columns>

                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="📞 Zadzwoń" Click="MenuZadzwon_Click"/>
                                <MenuItem Header="✏️ Edytuj" Click="MenuEdytuj_Click"/>
                                <MenuItem Header="📅 Ustaw datę kontaktu" Click="MenuUstawDate_Click"/>
                                <Separator/>
                                <MenuItem Header="🔄 Zmień status">
                                    <MenuItem Header="📞 Do zadzwonienia" Click="MenuZmienStatus_Click" Tag="Do zadzwonienia"/>
                                    <MenuItem Header="⏳ Próba kontaktu" Click="MenuZmienStatus_Click" Tag="Próba kontaktu"/>
                                    <MenuItem Header="✅ Nawiązano kontakt" Click="MenuZmienStatus_Click" Tag="Nawiązano kontakt"/>
                                    <MenuItem Header="🤝 Zgoda na dalszy kontakt" Click="MenuZmienStatus_Click" Tag="Zgoda na dalszy kontakt"/>
                                    <MenuItem Header="📄 Do wysłania oferta" Click="MenuZmienStatus_Click" Tag="Do wysłania oferta"/>
                                    <Separator/>
                                    <MenuItem Header="❌ Nie zainteresowany" Click="MenuZmienStatus_Click" Tag="Nie zainteresowany"/>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="🏷️ Otaguj">
                                    <MenuItem Header="⭐ VIP" Click="MenuTag_Click" Tag="VIP"/>
                                    <MenuItem Header="🔥 Pilne" Click="MenuTag_Click" Tag="Pilne"/>
                                    <MenuItem Header="💎 Premium" Click="MenuTag_Click" Tag="Premium"/>
                                    <Separator/>
                                    <MenuItem Header="🗑️ Wyczyść tagi" Click="MenuTag_Click" Tag="CLEAR"/>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="🔍 Szukaj w Google" Click="MenuGoogle_Click"/>
                                <MenuItem Header="🗺️ Pokaż trasę" Click="MenuTrasa_Click"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="Transparent">
                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                    <StackPanel>

                        <Border Style="{StaticResource CardStyle}" Padding="20" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="NOTATKI / HISTORIA" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextGray}" Margin="0,0,0,12"/>
                                <Border Background="#F9FAFB" CornerRadius="8" Height="250" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Padding="5">
                                    <ScrollViewer>
                                        <ItemsControl x:Name="listaNotatek">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="White" CornerRadius="6" Padding="10" Margin="5" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                                                        <StackPanel>
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="{Binding Typ}" FontSize="14" Margin="0,0,8,0"/>
                                                                <TextBlock Text="{Binding Tresc}" TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextDark}"/>
                                                            </StackPanel>
                                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
                                                                <TextBlock Text="{Binding Operator}" FontSize="10" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                                                                <TextBlock Text=" • " FontSize="10" Foreground="{StaticResource TextGray}"/>
                                                                <TextBlock Text="{Binding DataUtworzenia, StringFormat='dd.MM.yyyy HH:mm'}" FontSize="10" Foreground="{StaticResource TextGray}"/>
                                                            </StackPanel>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Border>
                                <Grid Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="txtNowaNotatka" Background="#F3F4F6" BorderThickness="0" Padding="10" VerticalContentAlignment="Center" FontSize="13" Tag="Wpisz notatkę..."/>
                                    <Button x:Name="btnDodajNotatke" Grid.Column="1" Content="Dodaj" Style="{StaticResource BtnPrimary}" Margin="10,0,0,0" Click="BtnDodajNotatke_Click"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Padding="20">
                            <StackPanel>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock x:Name="txtRankingTytul" Text="RANKING AKTYWNOŚCI (Ostatnie 30 dni)" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextGray}" VerticalAlignment="Center"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <RadioButton x:Name="rb30Dni" Content="30 dni" IsChecked="True" GroupName="OkresRankingu" Margin="0,0,10,0" FontSize="10" Checked="RbOkresRankingu_Checked"/>
                                        <RadioButton x:Name="rbWszystkie" Content="Wszystkie" GroupName="OkresRankingu" FontSize="10" Checked="RbOkresRankingu_Checked"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Legenda z opisami -->
                                <Border Background="#F9FAFB" CornerRadius="6" Padding="10,8" Margin="0,0,0,12">
                                    <WrapPanel HorizontalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,12,2">
                                            <TextBlock Text="📞" FontSize="10" VerticalAlignment="Center"/>
                                            <TextBlock Text="Do zadz." FontSize="9" Foreground="#64748B" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,12,2">
                                            <TextBlock Text="⏳" FontSize="10" VerticalAlignment="Center"/>
                                            <TextBlock Text="Próba" FontSize="9" Foreground="#9A3412" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,12,2">
                                            <TextBlock Text="✅" FontSize="10" VerticalAlignment="Center"/>
                                            <TextBlock Text="Nawiązano" FontSize="9" Foreground="#166534" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,12,2">
                                            <TextBlock Text="🤝" FontSize="10" VerticalAlignment="Center"/>
                                            <TextBlock Text="Zgoda" FontSize="9" Foreground="#0D9488" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,12,2">
                                            <TextBlock Text="📄" FontSize="10" VerticalAlignment="Center"/>
                                            <TextBlock Text="Oferta" FontSize="9" Foreground="#1E40AF" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                            <TextBlock Text="❌" FontSize="10" VerticalAlignment="Center"/>
                                            <TextBlock Text="Odmowa" FontSize="9" Foreground="#991B1B" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </Border>

                                <TextBlock Text="Kliknij 2x aby zobaczyć szczegóły" FontSize="9" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center" Margin="0,0,0,8" FontStyle="Italic"/>

                                <ListBox x:Name="listaRanking" BorderThickness="0" Background="Transparent" MouseDoubleClick="ListaRanking_MouseDoubleClick">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#FAFAFA" CornerRadius="8" Padding="10" Margin="0,0,0,8" BorderBrush="#E5E7EB" BorderThickness="1">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="28"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Pozycja -->
                                                    <Border Grid.RowSpan="2" Background="{StaticResource PrimaryLight}" Width="24" Height="24" CornerRadius="12" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Pozycja}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                                                    </Border>

                                                    <!-- Nazwa handlowca -->
                                                    <TextBlock Grid.Column="1" Text="{Binding Operator}" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextDark}" Margin="6,0,0,4"/>

                                                    <!-- Suma -->
                                                    <StackPanel Grid.Column="2" Grid.RowSpan="2" VerticalAlignment="Center" Margin="10,0,0,0">
                                                        <TextBlock Text="{Binding Suma}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="SUMA" FontSize="8" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center" FontWeight="Bold"/>
                                                    </StackPanel>

                                                    <!-- Statystyki statusów -->
                                                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="6,2,0,0">
                                                        <!-- Do zadzwonienia -->
                                                        <Border Background="#F1F5F9" CornerRadius="4" Padding="5,2" Margin="0,0,4,0" ToolTip="Do zadzwonienia">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="📞" FontSize="9" VerticalAlignment="Center"/>
                                                                <TextBlock Text="{Binding DoZadzwonienia}" FontSize="10" FontWeight="Bold" Foreground="#64748B" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>

                                                        <!-- Próba kontaktu -->
                                                        <Border Background="#FFEDD5" CornerRadius="4" Padding="5,2" Margin="0,0,4,0" ToolTip="Próba kontaktu">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="⏳" FontSize="9" VerticalAlignment="Center"/>
                                                                <TextBlock Text="{Binding Proby}" FontSize="10" FontWeight="Bold" Foreground="#9A3412" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>

                                                        <!-- Nawiązano kontakt -->
                                                        <Border Background="#DCFCE7" CornerRadius="4" Padding="5,2" Margin="0,0,4,0" ToolTip="Nawiązano kontakt">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="✅" FontSize="9" VerticalAlignment="Center"/>
                                                                <TextBlock Text="{Binding Nawiazano}" FontSize="10" FontWeight="Bold" Foreground="#166534" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>

                                                        <!-- Zgoda na dalszy kontakt -->
                                                        <Border Background="#CCFBF1" CornerRadius="4" Padding="5,2" Margin="0,0,4,0" ToolTip="Zgoda na dalszy kontakt">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="🤝" FontSize="9" VerticalAlignment="Center"/>
                                                                <TextBlock Text="{Binding Zgoda}" FontSize="10" FontWeight="Bold" Foreground="#0D9488" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>

                                                        <!-- Do wysłania oferta -->
                                                        <Border Background="#DBEAFE" CornerRadius="4" Padding="5,2" Margin="0,0,4,0" ToolTip="Do wysłania oferta">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="📄" FontSize="9" VerticalAlignment="Center"/>
                                                                <TextBlock Text="{Binding Oferty}" FontSize="10" FontWeight="Bold" Foreground="#1E40AF" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>

                                                        <!-- Nie zainteresowany -->
                                                        <Border Background="#FEE2E2" CornerRadius="4" Padding="5,2" ToolTip="Nie zainteresowany">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="❌" FontSize="9" VerticalAlignment="Center"/>
                                                                <TextBlock Text="{Binding NieZainteresowany}" FontSize="10" FontWeight="Bold" Foreground="#991B1B" Margin="3,0,0,0" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <ContentPresenter/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                </ListBox>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border x:Name="toastPopup" Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,40" 
                Background="#1F2937" CornerRadius="25" Padding="25,12" Opacity="0" IsHitTestVisible="False">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="10"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="✅ " Foreground="{StaticResource PrimaryColor}" FontSize="14"/>
                <TextBlock x:Name="toastText" Text="Wykonano" Foreground="White" FontWeight="SemiBold" FontSize="14"/>
            </StackPanel>
        </Border>

        <Border x:Name="loadingOverlay" Grid.RowSpan="3" Background="#95FFFFFF" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="4" Foreground="{StaticResource PrimaryColor}" Background="#E5E7EB" BorderThickness="0"/>
                <TextBlock Text="Wczytywanie bazy..." Foreground="{StaticResource TextGray}" Margin="0,15,0,0" HorizontalAlignment="Center" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>