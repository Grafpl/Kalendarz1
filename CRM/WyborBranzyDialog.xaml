<Window x:Class="Kalendarz1.CRM.WyborBranzyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wybór priorytetowych branż"
        Width="700" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="#0F172A"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- DARK CORPORATE THEME -->
        <SolidColorBrush x:Key="ColorPrimary" Color="#6366F1"/>
        <SolidColorBrush x:Key="ColorDanger" Color="#EF4444"/>
        <SolidColorBrush x:Key="ColorText" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="ColorTextMuted" Color="#64748B"/>
        <SolidColorBrush x:Key="ColorBorder" Color="#334155"/>

        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#312E81" Offset="0"/>
            <GradientStop Color="#6366F1" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="ButtonPrimary" TargetType="Button">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ButtonSecondary" TargetType="Button">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#475569"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="CaretBrush" Value="#E2E8F0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <Border Grid.Row="0" Background="{StaticResource HeaderGradient}" Padding="20,16">
            <StackPanel>
                <TextBlock Text="Wybór priorytetowych branż" FontSize="18" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Zaznacz branże, które mają być wyróżnione na czerwono i pojawiać się pierwsze na liście"
                           FontSize="11" Foreground="#A5B4FC" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Zawartość -->
        <Grid Grid.Row="1" Margin="20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Wyszukiwanie -->
            <Border Grid.Row="0" Background="#1E293B" CornerRadius="8" Padding="4" Margin="0,0,0,12" BorderBrush="#334155" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" FontSize="14" VerticalAlignment="Center" Margin="10,0,8,0" Foreground="#64748B"/>
                    <TextBox x:Name="txtSzukaj" Grid.Column="1" BorderThickness="0" Padding="4,10"
                             FontSize="13" Background="Transparent" Foreground="#E2E8F0" CaretBrush="#E2E8F0" VerticalAlignment="Center"
                             TextChanged="TxtSzukaj_TextChanged"/>
                </Grid>
            </Border>

            <!-- Statystyki -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Border Background="#14532D" CornerRadius="6" Padding="10,6" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Zaznaczone: " FontSize="11" Foreground="#4ADE80"/>
                        <TextBlock x:Name="txtZaznaczone" Text="0" FontSize="11" FontWeight="Bold" Foreground="#4ADE80"/>
                    </StackPanel>
                </Border>
                <Border Background="#7F1D1D" CornerRadius="6" Padding="10,6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Priorytetowe branże będą " FontSize="11" Foreground="#FCA5A5"/>
                        <TextBlock Text="CZERWONE" FontSize="11" FontWeight="Bold" Foreground="#EF4444"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Lista branż -->
            <Border Grid.Row="2" Background="#1E293B" CornerRadius="12" BorderBrush="#334155" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4">
                    <ItemsControl x:Name="listaBranzy">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="itemBorder" Background="#1E293B" CornerRadius="8" Padding="12,10" Margin="4,2"
                                        Cursor="Hand" MouseLeftButtonUp="BranzaItem_Click">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <CheckBox x:Name="chkBranza" Grid.Column="0" IsChecked="{Binding CzyPriorytetowa, Mode=TwoWay}"
                                                  VerticalAlignment="Center" Margin="0,0,12,0"
                                                  Checked="ChkBranza_Changed" Unchecked="ChkBranza_Changed"/>

                                        <TextBlock x:Name="txtNazwa" Grid.Column="1" Text="{Binding Nazwa}" FontSize="12"
                                                   VerticalAlignment="Center" TextWrapping="Wrap" Foreground="#E2E8F0"/>

                                        <Border x:Name="badgeLiczba" Grid.Column="2" Background="#334155" CornerRadius="4"
                                                Padding="8,4" Margin="8,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding LiczbaFirm}" FontSize="10" Foreground="#94A3B8"/>
                                        </Border>
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding CzyPriorytetowa}" Value="True">
                                        <Setter TargetName="itemBorder" Property="Background" Value="#7F1D1D"/>
                                        <Setter TargetName="itemBorder" Property="BorderBrush" Value="#EF4444"/>
                                        <Setter TargetName="itemBorder" Property="BorderThickness" Value="2"/>
                                        <Setter TargetName="txtNazwa" Property="Foreground" Value="#FCA5A5"/>
                                        <Setter TargetName="txtNazwa" Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding CzyPriorytetowa}" Value="False">
                                        <Setter TargetName="txtNazwa" Property="Foreground" Value="#E2E8F0"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Przyciski -->
        <Border Grid.Row="2" Background="#1E293B" Padding="20,16" BorderBrush="#334155" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="btnZaznaczWszystkie" Content="Zaznacz wszystkie" Style="{StaticResource ButtonSecondary}"
                        Grid.Column="0" Click="BtnZaznaczWszystkie_Click" Padding="14,10" FontSize="11"/>

                <Button x:Name="btnOdznaczWszystkie" Content="Odznacz wszystkie" Style="{StaticResource ButtonSecondary}"
                        Grid.Column="1" HorizontalAlignment="Left" Margin="8,0,0,0" Click="BtnOdznaczWszystkie_Click" Padding="14,10" FontSize="11"/>

                <Button x:Name="btnAnuluj" Content="Anuluj" Style="{StaticResource ButtonSecondary}"
                        Grid.Column="2" Click="BtnAnuluj_Click" Padding="16,10"/>

                <Button x:Name="btnZapisz" Content="Zapisz zmiany" Style="{StaticResource ButtonPrimary}"
                        Grid.Column="3" Margin="10,0,0,0" Click="BtnZapisz_Click" Padding="20,10"/>
            </Grid>
        </Border>
    </Grid>
</Window>
