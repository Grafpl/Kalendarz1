<Window x:Class="Kalendarz1.CRM.CallReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Czas na telefony!"
        Width="520" Height="680"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Colors -->
        <Color x:Key="BgPrimary">#0F172A</Color>
        <Color x:Key="BgSecondary">#1E293B</Color>
        <Color x:Key="BgTertiary">#334155</Color>
        <Color x:Key="AccentGreen">#4CAF50</Color>
        <Color x:Key="AccentGreenHover">#66BB6A</Color>
        <Color x:Key="AccentOrange">#F59E0B</Color>
        <Color x:Key="AccentRed">#EF4444</Color>
        <Color x:Key="TextPrimary">#E2E8F0</Color>
        <Color x:Key="TextSecondary">#94A3B8</Color>
        <Color x:Key="BorderColor">#334155</Color>

        <SolidColorBrush x:Key="BgPrimaryBrush" Color="{StaticResource BgPrimary}"/>
        <SolidColorBrush x:Key="BgSecondaryBrush" Color="{StaticResource BgSecondary}"/>
        <SolidColorBrush x:Key="BgTertiaryBrush" Color="{StaticResource BgTertiary}"/>
        <SolidColorBrush x:Key="AccentGreenBrush" Color="{StaticResource AccentGreen}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>

        <!-- Action Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentGreenBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#66BB6A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#475569"/>
                                <Setter Property="Foreground" Value="#64748B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small Button Style -->
        <Style x:Key="SmallButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BgTertiaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#475569"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Skip Button Style -->
        <Style x:Key="SkipButton" TargetType="Button" BasedOn="{StaticResource SmallButton}">
            <Setter Property="Background" Value="#7F1D1D"/>
            <Setter Property="Foreground" Value="#FCA5A5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#991B1B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BgTertiaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.3"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border Background="{StaticResource BgPrimaryBrush}" CornerRadius="12" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" Color="Black" Opacity="0.5" BlurRadius="20"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#166534" CornerRadius="12,12,0,0" Padding="20,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="📞 Czas na telefony!" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="txtSubtitle" Text="Zadzwoń do 5 losowych kontaktów" FontSize="12" Foreground="#86EFAC" Margin="0,4,0,0"/>
                    </StackPanel>

                    <TextBlock x:Name="txtTime" Grid.Column="1" Text="10:00" FontSize="28" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Progress Bar -->
            <Border Grid.Row="1" Background="{StaticResource BgSecondaryBrush}" Padding="20,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ProgressBar x:Name="progressBar" Height="8" Background="#475569" Foreground="#4CAF50" Value="0" Maximum="100">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <Border x:Name="PART_Track" CornerRadius="4">
                                        <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}"
                                                HorizontalAlignment="Left" CornerRadius="4"/>
                                    </Border>
                                </Border>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>

                    <TextBlock x:Name="txtProgress" Grid.Column="1" Text="0 / 5 obsłużonych"
                               Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="12,0,0,0" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Contacts List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="12,8">
                <ItemsControl x:Name="contactsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BgSecondaryBrush}" CornerRadius="10" Margin="0,6" Padding="16,14">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                <Setter Property="Opacity" Value="0.6"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Header Row -->
                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="{Binding Nazwa}" FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>

                                        <Border Grid.Column="1" Background="{Binding StatusBackground}" CornerRadius="4" Padding="8,3">
                                            <TextBlock Text="{Binding StatusShort}" FontSize="10" FontWeight="SemiBold"
                                                       Foreground="{Binding StatusColor}"/>
                                        </Border>
                                    </Grid>

                                    <!-- Location -->
                                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,6,0,0">
                                        <TextBlock Text="📍" FontSize="12"/>
                                        <TextBlock Text="{Binding Miasto}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="4,0,0,0"/>
                                        <TextBlock Text=", " Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text="{Binding Wojewodztwo}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>

                                    <!-- Phone -->
                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="📞" FontSize="12"/>
                                        <TextBlock Text="{Binding Telefon}" Foreground="#4ADE80" FontSize="13" FontWeight="SemiBold" Margin="4,0,0,0"/>
                                    </StackPanel>

                                    <!-- Action Buttons -->
                                    <WrapPanel Grid.Row="3" Margin="0,12,0,0" HorizontalAlignment="Left">
                                        <Button Content="📞 Zadzwoń" Style="{StaticResource ActionButton}"
                                                Tag="{Binding}" Click="BtnCall_Click" Margin="0,0,6,4"/>
                                        <Button Content="📝 Notatka" Style="{StaticResource SmallButton}"
                                                Tag="{Binding}" Click="BtnNote_Click" Margin="0,0,6,4"/>
                                        <Button Content="🔄 Status" Style="{StaticResource SmallButton}"
                                                Tag="{Binding}" Click="BtnStatus_Click" Margin="0,0,6,4"/>
                                        <Button Content="🔍 Google" Style="{StaticResource SmallButton}"
                                                Tag="{Binding}" Click="BtnGoogle_Click" Margin="0,0,6,4"/>
                                    </WrapPanel>

                                    <!-- Completed Indicator -->
                                    <TextBlock Grid.Row="0" Grid.RowSpan="4" Text="✓" FontSize="40" Foreground="#4CAF50"
                                               Opacity="0.3" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"
                                               Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVisibility}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Skip Reason -->
            <Border Grid.Row="3" Background="{StaticResource BgSecondaryBrush}" Padding="16,12"
                    Visibility="Collapsed" x:Name="skipReasonPanel">
                <StackPanel>
                    <TextBlock Text="Powód pominięcia:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtSkipReason" Style="{StaticResource DarkTextBox}"
                             TextWrapping="Wrap" AcceptsReturn="False" MaxLength="200"/>
                </StackPanel>
            </Border>

            <!-- Footer Buttons -->
            <Border Grid.Row="4" Background="{StaticResource BgSecondaryBrush}" CornerRadius="0,0,12,12" Padding="16,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnSkip" Content="⏭ Pomiń" Style="{StaticResource SkipButton}" Click="BtnSkip_Click"/>

                    <Button x:Name="btnClose" Grid.Column="2" Content="✓ Zamknij" Style="{StaticResource ActionButton}"
                            Click="BtnClose_Click" IsEnabled="False" MinWidth="100"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
