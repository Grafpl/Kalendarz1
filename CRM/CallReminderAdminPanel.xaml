<Window x:Class="Kalendarz1.CRM.CallReminderAdminPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Panel administracyjny - Przypomnienia telefonów"
        Width="1000" Height="700"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        MinWidth="900" MinHeight="600">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/GlassStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        </ResourceDictionary>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="20" BorderThickness="1" BorderBrush="#14FFFFFF">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0d1117" Offset="0"/>
                <GradientStop Color="#161b22" Offset="0.5"/>
                <GradientStop Color="#0d1117" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" Color="Black" Opacity="0.7" BlurRadius="40"/>
        </Border.Effect>

        <Grid Margin="0">
            <!-- Decorative Background Blobs -->
            <Ellipse Width="300" Height="300" HorizontalAlignment="Right" VerticalAlignment="Top"
                     Margin="0,-80,-80,0" Opacity="0.06" IsHitTestVisible="False">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#58a6ff" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
                <Ellipse.Effect>
                    <BlurEffect Radius="80"/>
                </Ellipse.Effect>
            </Ellipse>

            <Ellipse Width="250" Height="250" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                     Margin="-60,0,0,-60" Opacity="0.05" IsHitTestVisible="False">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#3fb950" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
                <Ellipse.Effect>
                    <BlurEffect Radius="80"/>
                </Ellipse.Effect>
            </Ellipse>

            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ============================================ -->
                <!-- HEADER                                       -->
                <!-- ============================================ -->
                <Border Grid.Row="0" Background="#08FFFFFF" BorderBrush="#14FFFFFF"
                        BorderThickness="1" CornerRadius="20" Padding="24" Margin="0,0,0,20"
                        MouseLeftButtonDown="Header_MouseLeftButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" FontSize="24" VerticalAlignment="Center"/>
                                <TextBlock Text="Panel administracyjny" FontSize="24" FontWeight="Bold"
                                           Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="Zarządzaj harmonogramem telefonów zespołu" FontSize="14"
                                       Foreground="#66FFFFFF" Margin="0,6,0,0"/>
                        </StackPanel>

                        <Button Grid.Column="1" Content="✕" Style="{StaticResource GlassButton}"
                                Width="40" Height="40" Padding="0" FontSize="16"
                                Click="BtnClose_Click"/>
                    </Grid>
                </Border>

                <!-- ============================================ -->
                <!-- STATS CARDS                                  -->
                <!-- ============================================ -->
                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Stat 1 - Calls Today -->
                    <Border Grid.Column="0" Background="#18181f" BorderBrush="#252530"
                            BorderThickness="1" CornerRadius="16" Padding="20" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="📞" FontSize="24"/>
                            <TextBlock x:Name="txtStatsDzis" Text="0" FontSize="28" FontWeight="Bold"
                                       Foreground="#3fb950" Margin="0,8,0,0"/>
                            <Border CornerRadius="10" Padding="6,2" HorizontalAlignment="Left" Margin="0,6,0,8">
                                <Border.Background>
                                    <SolidColorBrush Color="#263fb950"/>
                                </Border.Background>
                                <TextBlock x:Name="txtStatsCallsChange" Text="+0%" FontSize="11"
                                           Foreground="#3fb950" FontWeight="SemiBold"/>
                            </Border>
                            <TextBlock Text="Telefonów dziś" FontSize="13" Foreground="#66FFFFFF"/>
                        </StackPanel>
                    </Border>

                    <!-- Stat 2 - Active Agents -->
                    <Border Grid.Column="1" Background="#18181f" BorderBrush="#252530"
                            BorderThickness="1" CornerRadius="16" Padding="20" Margin="8,0">
                        <StackPanel>
                            <TextBlock Text="👥" FontSize="24"/>
                            <TextBlock x:Name="txtStatsTelefony" Text="0/0" FontSize="28" FontWeight="Bold"
                                       Foreground="#58a6ff" Margin="0,8,0,0"/>
                            <Border CornerRadius="10" Padding="6,2" HorizontalAlignment="Left" Margin="0,6,0,8">
                                <Border.Background>
                                    <SolidColorBrush Color="#2658a6ff"/>
                                </Border.Background>
                                <TextBlock x:Name="txtStatsAgentsPercent" Text="0%" FontSize="11"
                                           Foreground="#58a6ff" FontWeight="SemiBold"/>
                            </Border>
                            <TextBlock Text="Aktywni agenci" FontSize="13" Foreground="#66FFFFFF"/>
                        </StackPanel>
                    </Border>

                    <!-- Stat 3 - Week -->
                    <Border Grid.Column="2" Background="#18181f" BorderBrush="#252530"
                            BorderThickness="1" CornerRadius="16" Padding="20" Margin="8,0">
                        <StackPanel>
                            <TextBlock Text="📊" FontSize="24"/>
                            <TextBlock x:Name="txtStatsTydzien" Text="0" FontSize="28" FontWeight="Bold"
                                       Foreground="#f59e0b" Margin="0,8,0,0"/>
                            <Border CornerRadius="10" Padding="6,2" HorizontalAlignment="Left" Margin="0,6,0,8">
                                <Border.Background>
                                    <SolidColorBrush Color="#26f59e0b"/>
                                </Border.Background>
                                <TextBlock x:Name="txtStatsWeekChange" Text="+0%" FontSize="11"
                                           Foreground="#f59e0b" FontWeight="SemiBold"/>
                            </Border>
                            <TextBlock Text="Ten tydzień" FontSize="13" Foreground="#66FFFFFF"/>
                        </StackPanel>
                    </Border>

                    <!-- Stat 4 - Month -->
                    <Border Grid.Column="3" Background="#18181f" BorderBrush="#252530"
                            BorderThickness="1" CornerRadius="16" Padding="20" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="📈" FontSize="24"/>
                            <TextBlock x:Name="txtStatsMiesiac" Text="0" FontSize="28" FontWeight="Bold"
                                       Foreground="#a78bfa" Margin="0,8,0,0"/>
                            <Border CornerRadius="10" Padding="6,2" HorizontalAlignment="Left" Margin="0,6,0,8">
                                <Border.Background>
                                    <SolidColorBrush Color="#26a78bfa"/>
                                </Border.Background>
                                <TextBlock x:Name="txtStatsMonthChange" Text="+0%" FontSize="11"
                                           Foreground="#a78bfa" FontWeight="SemiBold"/>
                            </Border>
                            <TextBlock Text="Ten miesiąc" FontSize="13" Foreground="#66FFFFFF"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- ============================================ -->
                <!-- MAIN TABLE                                   -->
                <!-- ============================================ -->
                <Border Grid.Row="2" Background="#08FFFFFF" BorderBrush="#14FFFFFF"
                        BorderThickness="1" CornerRadius="16" ClipToBounds="True">
                    <DataGrid x:Name="dgHandlowcy" AutoGenerateColumns="False"
                              Background="Transparent" BorderThickness="0"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#14FFFFFF"
                              RowBackground="Transparent"
                              AlternatingRowBackground="#03FFFFFF"
                              HeadersVisibility="Column"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserReorderColumns="False"
                              CanUserResizeRows="False"
                              RowHeight="60"
                              ColumnHeaderHeight="50"
                              Foreground="White"
                              FontSize="13">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#0f0f13"/>
                                <Setter Property="Foreground" Value="#66FFFFFF"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderBrush" Value="#14FFFFFF"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="16,0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#2658a6ff"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#14FFFFFF"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#2658a6ff"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <!-- Enabled Checkbox -->
                            <DataGridTemplateColumn Header="✓" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}"
                                                  HorizontalAlignment="Center" Style="{StaticResource GlassCheckBox}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Handlowiec Name -->
                            <DataGridTemplateColumn Header="HANDLOWIEC" Width="*" MinWidth="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Width="36" Height="36" CornerRadius="18">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="#58a6ff" Offset="0"/>
                                                        <GradientStop Color="#a78bfa" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <TextBlock Text="{Binding Initials}" FontSize="14" FontWeight="Bold"
                                                           Foreground="White" HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="{Binding UserName}" FontSize="14" FontWeight="Medium"
                                                       Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Time 1 -->
                            <DataGridTemplateColumn Header="GODZ. 1" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox SelectedValue="{Binding Time1String, UpdateSourceTrigger=PropertyChanged}"
                                                  ItemsSource="{Binding AvailableTimes}"
                                                  Style="{StaticResource GlassComboBox}"
                                                  Width="80"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Time 2 -->
                            <DataGridTemplateColumn Header="GODZ. 2" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox SelectedValue="{Binding Time2String, UpdateSourceTrigger=PropertyChanged}"
                                                  ItemsSource="{Binding AvailableTimes}"
                                                  Style="{StaticResource GlassComboBox}"
                                                  Width="80"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Contacts Count -->
                            <DataGridTemplateColumn Header="KONTAKTÓW" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox SelectedValue="{Binding ContactsPerReminder, UpdateSourceTrigger=PropertyChanged}"
                                                  ItemsSource="{Binding AvailableCounts}"
                                                  Style="{StaticResource GlassComboBox}"
                                                  Width="60"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Only New -->
                            <DataGridTemplateColumn Header="TYLKO NOWI" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding ShowOnlyNewContacts, UpdateSourceTrigger=PropertyChanged}"
                                                  HorizontalAlignment="Center" Style="{StaticResource GlassCheckBox}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Only Assigned -->
                            <DataGridTemplateColumn Header="PRZYPISANI" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding ShowOnlyAssigned, UpdateSourceTrigger=PropertyChanged}"
                                                  HorizontalAlignment="Center" Style="{StaticResource GlassCheckBox}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Today Count -->
                            <DataGridTemplateColumn Header="DZIŚ" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="10" Padding="8,4" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#26f59e0b"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding TodayCalls}" Value="0">
                                                            <Setter Property="Background" Value="#26ef4444"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding TodayCalls}" FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#f59e0b"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding TodayCalls}" Value="0">
                                                                <Setter Property="Foreground" Value="#ef4444"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Week Count -->
                            <DataGridTextColumn Header="TYDZIEŃ" Binding="{Binding WeekCalls}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#B3FFFFFF"/>
                                        <Setter Property="FontWeight" Value="Medium"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- ============================================ -->
                <!-- BOTTOM PANEL                                 -->
                <!-- ============================================ -->
                <Grid Grid.Row="3" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>

                    <!-- Weekly Activity Chart -->
                    <Border Grid.Column="0" Background="#08FFFFFF" BorderBrush="#14FFFFFF"
                            BorderThickness="1" CornerRadius="16" Padding="20" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="📊 Aktywność tygodnia" FontSize="14" FontWeight="SemiBold"
                                       Foreground="White" Margin="0,0,0,16"/>

                            <Grid Height="120">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Monday -->
                                <StackPanel Grid.Column="0" VerticalAlignment="Bottom" Margin="4,0">
                                    <Border x:Name="barMon" Height="40" CornerRadius="6,6,0,0" Background="#252530"/>
                                    <TextBlock Text="Pon" FontSize="11" Foreground="#66FFFFFF"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                </StackPanel>

                                <!-- Tuesday -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Margin="4,0">
                                    <Border x:Name="barTue" Height="60" CornerRadius="6,6,0,0" Background="#252530"/>
                                    <TextBlock Text="Wt" FontSize="11" Foreground="#66FFFFFF"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                </StackPanel>

                                <!-- Wednesday -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Margin="4,0">
                                    <Border x:Name="barWed" Height="50" CornerRadius="6,6,0,0" Background="#252530"/>
                                    <TextBlock Text="Śr" FontSize="11" Foreground="#66FFFFFF"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                </StackPanel>

                                <!-- Thursday (highlighted) -->
                                <StackPanel Grid.Column="3" VerticalAlignment="Bottom" Margin="4,0">
                                    <Border x:Name="barThu" Height="90" CornerRadius="6,6,0,0">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#3fb950" Offset="0"/>
                                                <GradientStop Color="#22c55e" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>
                                    <TextBlock Text="Czw" FontSize="11" Foreground="#3fb950" FontWeight="SemiBold"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                </StackPanel>

                                <!-- Friday -->
                                <StackPanel Grid.Column="4" VerticalAlignment="Bottom" Margin="4,0">
                                    <Border x:Name="barFri" Height="70" CornerRadius="6,6,0,0" Background="#252530"/>
                                    <TextBlock Text="Pt" FontSize="11" Foreground="#66FFFFFF"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Quick Actions -->
                    <Border Grid.Column="1" Background="#08FFFFFF" BorderBrush="#14FFFFFF"
                            BorderThickness="1" CornerRadius="16" Padding="20">
                        <StackPanel>
                            <TextBlock Text="⚡ Szybkie akcje" FontSize="14" FontWeight="SemiBold"
                                       Foreground="White" Margin="0,0,0,16"/>

                            <Button Style="{StaticResource GlassButton}" Margin="0,0,0,8"
                                    Click="BtnTest_Click" HorizontalContentAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🧪" FontSize="14" Margin="0,0,12,0"/>
                                    <TextBlock Text="Testuj przypomnienie" FontSize="13"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource GlassButton}" Margin="0,0,0,8"
                                    Click="BtnApplyToAll_Click" HorizontalContentAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📋" FontSize="14" Margin="0,0,12,0"/>
                                    <TextBlock Text="Zastosuj do wszystkich" FontSize="13"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource GlassButton}" Margin="0,0,0,16"
                                    Click="BtnRefresh_Click" HorizontalContentAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🔄" FontSize="14" Margin="0,0,12,0"/>
                                    <TextBlock Text="Odśwież dane" FontSize="13"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource SuccessButton}" Click="BtnSave_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💾" FontSize="16" Margin="0,0,10,0"/>
                                    <TextBlock Text="Zapisz wszystkie zmiany" FontSize="14" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
