<Window x:Class="Kalendarz1.CRM.DashboardCRMWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dashboard Motywacyjny - Aktywność Handlowców"
        Width="1400" Height="900"
        WindowStartupLocation="CenterScreen"
        Background="#F1F5F9"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#16A34A"/>
        <SolidColorBrush x:Key="PhoneColor" Color="#2563EB"/>
        <SolidColorBrush x:Key="StatusColor" Color="#7C3AED"/>
        <SolidColorBrush x:Key="NoteColor" Color="#EA580C"/>
        <SolidColorBrush x:Key="TextDark" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextGray" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E2E8F0"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="20" ShadowDepth="4" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🏆" FontSize="32" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <StackPanel>
                    <TextBlock Text="Dashboard Motywacyjny" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                    <TextBlock x:Name="txtPodtytul" Text="Zmotywuj zespół do działania!" FontSize="13" Foreground="{StaticResource TextGray}"/>
                </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="Okres:" FontSize="12" Foreground="{StaticResource TextGray}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="cmbOkres" Width="150" Padding="10,8" FontSize="12" SelectionChanged="CmbOkres_SelectionChanged">
                    <ComboBoxItem Content="Dzisiaj" IsSelected="True"/>
                    <ComboBoxItem Content="Ostatnie 7 dni"/>
                    <ComboBoxItem Content="Ostatnie 14 dni"/>
                    <ComboBoxItem Content="Ostatnie 30 dni"/>
                    <ComboBoxItem Content="Ten miesiąc"/>
                    <ComboBoxItem Content="Poprzedni miesiąc"/>
                </ComboBox>
                <Button Content="🔄 Odśwież" Padding="16,10" Margin="12,0,0,0" Background="{StaticResource PrimaryColor}"
                        Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Click="BtnOdswiez_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>

        <!-- STATYSTYKI GLOBALNE -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Telefony -->
            <Border Grid.Column="0" Style="{StaticResource StatBadge}" Background="#DBEAFE">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📞" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <StackPanel>
                            <TextBlock Text="TELEFONY" FontSize="10" FontWeight="Bold" Foreground="#1E40AF"/>
                            <TextBlock x:Name="txtTelefonyTotal" Text="0" FontSize="36" FontWeight="Bold" Foreground="#1E40AF"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock x:Name="txtTelefonyInfo" Text="wykonanych połączeń" FontSize="11" Foreground="#3B82F6" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Zmiany statusów -->
            <Border Grid.Column="1" Style="{StaticResource StatBadge}" Background="#EDE9FE">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔄" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <StackPanel>
                            <TextBlock Text="ZMIANY STATUSÓW" FontSize="10" FontWeight="Bold" Foreground="#5B21B6"/>
                            <TextBlock x:Name="txtStatusyTotal" Text="0" FontSize="36" FontWeight="Bold" Foreground="#5B21B6"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock x:Name="txtStatusyInfo" Text="zmian statusów" FontSize="11" Foreground="#7C3AED" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Notatki -->
            <Border Grid.Column="2" Style="{StaticResource StatBadge}" Background="#FFEDD5">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📝" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <StackPanel>
                            <TextBlock Text="NOTATKI" FontSize="10" FontWeight="Bold" Foreground="#9A3412"/>
                            <TextBlock x:Name="txtNotatkiTotal" Text="0" FontSize="36" FontWeight="Bold" Foreground="#9A3412"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock x:Name="txtNotatkiInfo" Text="dodanych notatek" FontSize="11" Foreground="#EA580C" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Razem aktywność -->
            <Border Grid.Column="3" Style="{StaticResource StatBadge}" Background="#DCFCE7" Margin="0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚡" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <StackPanel>
                            <TextBlock Text="ŁĄCZNA AKTYWNOŚĆ" FontSize="10" FontWeight="Bold" Foreground="#166534"/>
                            <TextBlock x:Name="txtAktywnoscTotal" Text="0" FontSize="36" FontWeight="Bold" Foreground="#166534"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock x:Name="txtAktywnoscInfo" Text="wszystkich akcji" FontSize="11" Foreground="#16A34A" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- GŁÓWNA ZAWARTOŚĆ -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- LEWY PANEL - WYKRESY -->
            <Grid Grid.Column="0" Margin="0,0,16,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- WYKRES AKTYWNOŚCI HANDLOWCÓW -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="📊 Aktywność handlowców" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextDark}"/>
                            <TextBlock x:Name="txtWykresOkres" Text=" (Dzisiaj)" FontSize="14" Foreground="{StaticResource TextGray}" VerticalAlignment="Bottom" Margin="4,0,0,1"/>
                        </StackPanel>

                        <!-- Lista handlowców z wykresem słupkowym -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="listaHandlowcyWykres">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="30"/>
                                                <ColumnDefinition Width="140"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="80"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Pozycja -->
                                            <Border Grid.Column="0" Background="{Binding KolorPozycji}" Width="26" Height="26" CornerRadius="13" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Pozycja}" FontSize="11" FontWeight="Bold" Foreground="White"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>

                                            <!-- Nazwa -->
                                            <TextBlock Grid.Column="1" Text="{Binding Nazwa}" FontSize="13" FontWeight="SemiBold"
                                                       Foreground="{StaticResource TextDark}" VerticalAlignment="Center" Margin="8,0,0,0"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <!-- Słupki -->
                                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                                                <!-- Telefony -->
                                                <Border Background="#3B82F6" Height="20" Width="{Binding SzerokoscTelefony}" CornerRadius="3,0,0,3" VerticalAlignment="Center">
                                                    <Border.ToolTip>
                                                        <TextBlock Text="{Binding TooltipTelefony}"/>
                                                    </Border.ToolTip>
                                                </Border>
                                                <!-- Statusy -->
                                                <Border Background="#7C3AED" Height="20" Width="{Binding SzerokoscStatusy}" VerticalAlignment="Center">
                                                    <Border.ToolTip>
                                                        <TextBlock Text="{Binding TooltipStatusy}"/>
                                                    </Border.ToolTip>
                                                </Border>
                                                <!-- Notatki -->
                                                <Border Background="#EA580C" Height="20" Width="{Binding SzerokoscNotatki}" CornerRadius="0,3,3,0" VerticalAlignment="Center">
                                                    <Border.ToolTip>
                                                        <TextBlock Text="{Binding TooltipNotatki}"/>
                                                    </Border.ToolTip>
                                                </Border>
                                            </StackPanel>

                                            <!-- Suma -->
                                            <StackPanel Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Suma}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Right"/>
                                                <TextBlock Text="akcji" FontSize="9" Foreground="{StaticResource TextGray}" HorizontalAlignment="Right"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- LEGENDA -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="16,12">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                            <Border Background="#3B82F6" Width="20" Height="12" CornerRadius="2" VerticalAlignment="Center"/>
                            <TextBlock Text="Telefony" FontSize="11" Foreground="{StaticResource TextDark}" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                            <Border Background="#7C3AED" Width="20" Height="12" CornerRadius="2" VerticalAlignment="Center"/>
                            <TextBlock Text="Zmiany statusów" FontSize="11" Foreground="{StaticResource TextDark}" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#EA580C" Width="20" Height="12" CornerRadius="2" VerticalAlignment="Center"/>
                            <TextBlock Text="Notatki" FontSize="11" Foreground="{StaticResource TextDark}" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- PRAWY PANEL - SZCZEGÓŁY -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- AKTYWNOŚĆ PO DNIACH -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="📅 Aktywność po dniach" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,12"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="listaAktywnoscDni">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding TloKarty}" CornerRadius="8" Padding="12,8" Margin="0,0,0,6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding DzienNazwa}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextDark}"/>
                                                    <TextBlock Text="{Binding Data}" FontSize="10" Foreground="{StaticResource TextGray}"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Border Background="#DBEAFE" CornerRadius="4" Padding="6,2" Margin="0,0,4,0">
                                                        <TextBlock Text="{Binding Telefony}" FontSize="10" FontWeight="Bold" Foreground="#1E40AF"/>
                                                    </Border>
                                                    <Border Background="#EDE9FE" CornerRadius="4" Padding="6,2" Margin="0,0,4,0">
                                                        <TextBlock Text="{Binding Statusy}" FontSize="10" FontWeight="Bold" Foreground="#5B21B6"/>
                                                    </Border>
                                                    <Border Background="#FFEDD5" CornerRadius="4" Padding="6,2">
                                                        <TextBlock Text="{Binding Notatki}" FontSize="10" FontWeight="Bold" Foreground="#9A3412"/>
                                                    </Border>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- TOP 3 DNIA -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="🥇 Podium dnia" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,12"/>
                        <ItemsControl Grid.Row="1" x:Name="listaTopDnia">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding TloPozycji}" CornerRadius="10" Padding="16,12" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Medal}" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Nazwa}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <TextBlock Text="📞" FontSize="10"/>
                                                    <TextBlock Text="{Binding Telefony}" FontSize="10" Foreground="#3B82F6" FontWeight="SemiBold" Margin="2,0,8,0"/>
                                                    <TextBlock Text="🔄" FontSize="10"/>
                                                    <TextBlock Text="{Binding Statusy}" FontSize="10" Foreground="#7C3AED" FontWeight="SemiBold" Margin="2,0,8,0"/>
                                                    <TextBlock Text="📝" FontSize="10"/>
                                                    <TextBlock Text="{Binding Notatki}" FontSize="10" Foreground="#EA580C" FontWeight="SemiBold" Margin="2,0,0,0"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding Suma}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Right"/>
                                                <TextBlock Text="pkt" FontSize="10" Foreground="{StaticResource TextGray}" HorizontalAlignment="Right"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
