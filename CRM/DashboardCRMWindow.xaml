<Window x:Class="Kalendarz1.CRM.DashboardCRMWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dashboard Motywacyjny - Aktywnosc Zespolu"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#10B981"/>
        <SolidColorBrush x:Key="TextLight" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="TextGray" Color="#94A3B8"/>
        <SolidColorBrush x:Key="BorderColor" Color="#334155"/>
        <SolidColorBrush x:Key="CardBackground" Color="#1E293B"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="FilterButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#10B981"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#334155"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style dla DataGrid -->
        <Style x:Key="DarkDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F1F5F9"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#1E293B"/>
            <Setter Property="AlternatingRowBackground" Value="#273548"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#334155"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="RowHeight" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="DarkColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
        </Style>

        <Style x:Key="DarkDataGridCell" TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#F1F5F9"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#334155"/>
                    <Setter Property="Foreground" Value="#10B981"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkDataGridRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="#1E293B"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D3A4F"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#334155"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" CornerRadius="12" Padding="24,16" Margin="0,0,0,16">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#065F46" Offset="0"/>
                    <GradientStop Color="#047857" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="DASHBOARD AKTYWNOSCI" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="txtPodtytul" Text=" - Statystyki zespolu" FontSize="16" Foreground="#A7F3D0" VerticalAlignment="Bottom" Margin="8,0,0,2"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <!-- Filtry czasowe -->
                    <TextBlock Text="Rok:" FontSize="12" Foreground="#A7F3D0" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <Border Background="#1E293B" CornerRadius="6" Padding="2">
                        <ComboBox x:Name="cmbRok" Width="70" Padding="6,4" FontSize="11" BorderThickness="0" SelectionChanged="CmbZakres_SelectionChanged"/>
                    </Border>

                    <TextBlock Text="od:" FontSize="12" Foreground="#A7F3D0" VerticalAlignment="Center" Margin="12,0,6,0"/>
                    <Border Background="#1E293B" CornerRadius="6" Padding="2">
                        <ComboBox x:Name="cmbMiesiacOd" Width="90" Padding="6,4" FontSize="11" BorderThickness="0" SelectionChanged="CmbZakres_SelectionChanged">
                            <ComboBoxItem Content="Styczen" Tag="1"/>
                            <ComboBoxItem Content="Luty" Tag="2"/>
                            <ComboBoxItem Content="Marzec" Tag="3"/>
                            <ComboBoxItem Content="Kwiecien" Tag="4"/>
                            <ComboBoxItem Content="Maj" Tag="5"/>
                            <ComboBoxItem Content="Czerwiec" Tag="6"/>
                            <ComboBoxItem Content="Lipiec" Tag="7"/>
                            <ComboBoxItem Content="Sierpien" Tag="8"/>
                            <ComboBoxItem Content="Wrzesien" Tag="9"/>
                            <ComboBoxItem Content="Pazdziernik" Tag="10"/>
                            <ComboBoxItem Content="Listopad" Tag="11"/>
                            <ComboBoxItem Content="Grudzien" Tag="12"/>
                        </ComboBox>
                    </Border>

                    <TextBlock Text="do:" FontSize="12" Foreground="#A7F3D0" VerticalAlignment="Center" Margin="6,0,6,0"/>
                    <Border Background="#1E293B" CornerRadius="6" Padding="2">
                        <ComboBox x:Name="cmbMiesiacDo" Width="90" Padding="6,4" FontSize="11" BorderThickness="0" SelectionChanged="CmbZakres_SelectionChanged">
                            <ComboBoxItem Content="Styczen" Tag="1"/>
                            <ComboBoxItem Content="Luty" Tag="2"/>
                            <ComboBoxItem Content="Marzec" Tag="3"/>
                            <ComboBoxItem Content="Kwiecien" Tag="4"/>
                            <ComboBoxItem Content="Maj" Tag="5"/>
                            <ComboBoxItem Content="Czerwiec" Tag="6"/>
                            <ComboBoxItem Content="Lipiec" Tag="7"/>
                            <ComboBoxItem Content="Sierpien" Tag="8"/>
                            <ComboBoxItem Content="Wrzesien" Tag="9"/>
                            <ComboBoxItem Content="Pazdziernik" Tag="10"/>
                            <ComboBoxItem Content="Listopad" Tag="11"/>
                            <ComboBoxItem Content="Grudzien" Tag="12"/>
                        </ComboBox>
                    </Border>

                    <!-- Typ danych -->
                    <Border Background="#1E293B" CornerRadius="6" Padding="4" Margin="16,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="rbWszystko" Content="Wszystko" Style="{StaticResource FilterButton}" IsChecked="True" GroupName="TypDanych" Checked="RbTypDanych_Changed"/>
                            <RadioButton x:Name="rbTelefony" Content="Telefony" Style="{StaticResource FilterButton}" GroupName="TypDanych" Checked="RbTypDanych_Changed"/>
                            <RadioButton x:Name="rbNotatki" Content="Notatki" Style="{StaticResource FilterButton}" GroupName="TypDanych" Checked="RbTypDanych_Changed"/>
                        </StackPanel>
                    </Border>

                    <!-- Widok -->
                    <Border Background="#1E293B" CornerRadius="6" Padding="4" Margin="12,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="rbMiesiace" Content="Miesiace" Style="{StaticResource FilterButton}" IsChecked="True" GroupName="Widok" Checked="RbWidok_Changed"/>
                            <RadioButton x:Name="rbTygodnie" Content="Tygodnie" Style="{StaticResource FilterButton}" GroupName="Widok" Checked="RbWidok_Changed"/>
                        </StackPanel>
                    </Border>

                    <Button Content="Odswiez" Padding="14,6" Margin="12,0,0,0" Background="#10B981"
                            Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnOdswiez_Click" FontSize="12">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- WYKRES SLUPKOWY -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16" MaxHeight="320">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="AKTYWNOSC HANDLOWCOW" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextLight}"/>
                    <TextBlock Text=" (kliknij 2x aby zobaczyc wykres)" FontSize="11" Foreground="{StaticResource TextGray}" VerticalAlignment="Bottom" Margin="8,0,0,2"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="wykresSlupkowy">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,3" Cursor="Hand" Background="Transparent" MouseLeftButtonDown="Slupek_MouseLeftButtonDown">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="140"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="70"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding Nazwa}" FontSize="13" FontWeight="SemiBold"
                                                   Foreground="#E2E8F0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                                        <Grid Grid.Column="1" Margin="10,0">
                                            <Border Background="#334155" CornerRadius="4" Height="24"/>
                                            <Border Background="{Binding Kolor}" CornerRadius="4" Height="24"
                                                    HorizontalAlignment="Left" Width="{Binding SzerokoscSlupka}">
                                                <Border.Effect>
                                                    <DropShadowEffect Color="{Binding KolorCien}" BlurRadius="6" ShadowDepth="0" Opacity="0.4"/>
                                                </Border.Effect>
                                            </Border>
                                        </Grid>

                                        <TextBlock Grid.Column="2" Text="{Binding Suma}" FontSize="14" FontWeight="Bold"
                                                   Foreground="{Binding Kolor}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- TABELA DANYCH -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock x:Name="txtTabelaTytul" Text="SZCZEGOLOWE DANE" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextLight}"/>
                    <TextBlock Text=" (kliknij 2x na wiersz aby zobaczyc wykres)" FontSize="11" Foreground="{StaticResource TextGray}" VerticalAlignment="Bottom" Margin="8,0,0,2"/>
                </StackPanel>

                <DataGrid x:Name="tabelaDane" Grid.Row="1"
                          Style="{StaticResource DarkDataGrid}"
                          ColumnHeaderStyle="{StaticResource DarkColumnHeader}"
                          CellStyle="{StaticResource DarkDataGridCell}"
                          RowStyle="{StaticResource DarkDataGridRow}"
                          MouseDoubleClick="TabelaDane_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Handlowiec" Binding="{Binding Nazwa}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
