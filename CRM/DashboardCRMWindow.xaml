<Window x:Class="Kalendarz1.CRM.DashboardCRMWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dashboard CRM"
        Width="1400" Height="850"
        WindowStartupLocation="CenterScreen"
        Background="#F1F5F9"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#2563EB"/>
        <SolidColorBrush x:Key="PhoneColor" Color="#3B82F6"/>
        <SolidColorBrush x:Key="StatusColor" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="NoteColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="TextDark" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextGray" Color="#64748B"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="16" ShadowDepth="2" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER - prosty i czytelny -->
        <Border Grid.Row="0" Background="#2563EB" CornerRadius="12" Padding="20,16" Margin="0,0,0,16">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="DASHBOARD CRM" FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text=" - Aktywność handlowców" FontSize="16" Foreground="#93C5FD" VerticalAlignment="Bottom" Margin="8,0,0,2"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <!-- FILTR HANDLOWCA -->
                    <Border Background="#1D4ED8" CornerRadius="8" Padding="12,6" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Handlowiec:" FontSize="12" Foreground="#BFDBFE" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox x:Name="cmbHandlowiec" Width="150" Padding="8,4" FontSize="12" BorderThickness="0" Background="White" SelectionChanged="CmbHandlowiec_SelectionChanged">
                                <ComboBoxItem Content="Wszyscy" Tag="" IsSelected="True"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <!-- WYBÓR OKRESU -->
                    <Border Background="#1D4ED8" CornerRadius="8" Padding="12,6" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Okres:" FontSize="12" Foreground="#BFDBFE" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox x:Name="cmbOkres" Width="130" Padding="8,4" FontSize="12" BorderThickness="0" Background="White" SelectionChanged="CmbOkres_SelectionChanged">
                                <ComboBoxItem Content="Dzisiaj" IsSelected="True"/>
                                <ComboBoxItem Content="7 dni"/>
                                <ComboBoxItem Content="14 dni"/>
                                <ComboBoxItem Content="30 dni"/>
                                <ComboBoxItem Content="Ten miesiąc"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <Button Content="Odśwież" Padding="16,8" Background="White" Foreground="#2563EB"
                            FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Click="BtnOdswiez_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- GŁÓWNA ZAWARTOŚĆ -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEWA KOLUMNA - STATYSTYKI + RANKING -->
            <Grid Grid.Column="0" Margin="0,0,16,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- KARTY KPI - 2x2 -->
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Telefony -->
                    <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,8,8">
                        <StackPanel>
                            <TextBlock Text="TELEFONY" FontSize="10" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="txtTelefonyTotal" Text="0" FontSize="28" FontWeight="Bold" Foreground="#3B82F6" Margin="0,4,0,0"/>
                            <TextBlock x:Name="txtTelefonyInfo" Text="połączeń" FontSize="10" Foreground="#94A3B8"/>
                        </StackPanel>
                    </Border>

                    <!-- Statusy -->
                    <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource CardStyle}" Margin="8,0,0,8">
                        <StackPanel>
                            <TextBlock Text="STATUSY" FontSize="10" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="txtStatusyTotal" Text="0" FontSize="28" FontWeight="Bold" Foreground="#8B5CF6" Margin="0,4,0,0"/>
                            <TextBlock x:Name="txtStatusyInfo" Text="zmian" FontSize="10" Foreground="#94A3B8"/>
                        </StackPanel>
                    </Border>

                    <!-- Notatki -->
                    <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,8,8,0">
                        <StackPanel>
                            <TextBlock Text="NOTATKI" FontSize="10" FontWeight="Bold" Foreground="#64748B"/>
                            <TextBlock x:Name="txtNotatkiTotal" Text="0" FontSize="28" FontWeight="Bold" Foreground="#F59E0B" Margin="0,4,0,0"/>
                            <TextBlock x:Name="txtNotatkiInfo" Text="dodanych" FontSize="10" Foreground="#94A3B8"/>
                        </StackPanel>
                    </Border>

                    <!-- Suma -->
                    <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}" Margin="8,8,0,0" Background="#2563EB">
                        <StackPanel>
                            <TextBlock Text="RAZEM" FontSize="10" FontWeight="Bold" Foreground="#BFDBFE"/>
                            <TextBlock x:Name="txtAktywnoscTotal" Text="0" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                            <TextBlock x:Name="txtAktywnoscInfo" Text="akcji" FontSize="10" Foreground="#93C5FD"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- RANKING HANDLOWCÓW -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="RANKING" FontSize="12" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,12"/>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="listaHandlowcy">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding TloKarty}" CornerRadius="8" Padding="10,8" Margin="0,0,0,6"
                                                Cursor="Hand" MouseLeftButtonUp="Handlowiec_Click" Tag="{Binding Nazwa}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="28"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Background="{Binding KolorPozycji}" Width="24" Height="24" CornerRadius="12">
                                                    <TextBlock Text="{Binding Pozycja}" FontSize="11" FontWeight="Bold" Foreground="White"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>

                                                <StackPanel Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Nazwa}" FontSize="12" FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextDark}" TextTrimming="CharacterEllipsis"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                        <TextBlock Text="{Binding TelefonyTekst}" FontSize="9" Foreground="#3B82F6" Margin="0,0,6,0"/>
                                                        <TextBlock Text="{Binding StatusyTekst}" FontSize="9" Foreground="#8B5CF6" Margin="0,0,6,0"/>
                                                        <TextBlock Text="{Binding NotatkiTekst}" FontSize="9" Foreground="#F59E0B"/>
                                                    </StackPanel>
                                                </StackPanel>

                                                <TextBlock Grid.Column="2" Text="{Binding Suma}" FontSize="18" FontWeight="Bold"
                                                           Foreground="#2563EB" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- PRAWA KOLUMNA - WYKRES -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="180"/>
                </Grid.RowDefinitions>

                <!-- WYKRES LINIOWY -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="txtWykresTytul" Text="AKTYWNOŚĆ DZIENNA" FontSize="12" FontWeight="Bold" Foreground="#64748B"/>
                                <TextBlock x:Name="txtWykresHandlowiec" Text="" FontSize="12" FontWeight="Bold" Foreground="#2563EB" Margin="8,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Border Background="#F1F5F9" CornerRadius="6" Padding="8,4" Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Miesiąc:" FontSize="10" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <ComboBox x:Name="cmbMiesiac" Width="90" Padding="6,2" FontSize="11" BorderThickness="0" SelectionChanged="CmbWykresOkres_SelectionChanged">
                                            <ComboBoxItem Content="Styczeń" Tag="1"/>
                                            <ComboBoxItem Content="Luty" Tag="2"/>
                                            <ComboBoxItem Content="Marzec" Tag="3"/>
                                            <ComboBoxItem Content="Kwiecień" Tag="4"/>
                                            <ComboBoxItem Content="Maj" Tag="5"/>
                                            <ComboBoxItem Content="Czerwiec" Tag="6"/>
                                            <ComboBoxItem Content="Lipiec" Tag="7"/>
                                            <ComboBoxItem Content="Sierpień" Tag="8"/>
                                            <ComboBoxItem Content="Wrzesień" Tag="9"/>
                                            <ComboBoxItem Content="Październik" Tag="10"/>
                                            <ComboBoxItem Content="Listopad" Tag="11"/>
                                            <ComboBoxItem Content="Grudzień" Tag="12"/>
                                        </ComboBox>
                                    </StackPanel>
                                </Border>
                                <Border Background="#F1F5F9" CornerRadius="6" Padding="8,4">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Rok:" FontSize="10" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <ComboBox x:Name="cmbRok" Width="60" Padding="6,2" FontSize="11" BorderThickness="0" SelectionChanged="CmbWykresOkres_SelectionChanged"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <!-- Canvas wykresu -->
                        <Grid Grid.Row="1" Margin="40,8,16,30">
                            <Canvas x:Name="canvasWykres" Background="Transparent" ClipToBounds="True"/>
                            <StackPanel x:Name="panelOsY" Orientation="Vertical" HorizontalAlignment="Left" Margin="-40,0,0,0"/>
                            <StackPanel x:Name="panelOsX" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,-25"/>
                        </Grid>

                        <!-- Legenda -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                                <Ellipse Fill="#3B82F6" Width="10" Height="10" VerticalAlignment="Center"/>
                                <TextBlock Text="Telefony" FontSize="11" Foreground="#64748B" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                                <Ellipse Fill="#8B5CF6" Width="10" Height="10" VerticalAlignment="Center"/>
                                <TextBlock Text="Statusy" FontSize="11" Foreground="#64748B" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Fill="#F59E0B" Width="10" Height="10" VerticalAlignment="Center"/>
                                <TextBlock Text="Notatki" FontSize="11" Foreground="#64748B" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- SŁUPKI OSTATNICH DNI -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="OSTATNIE 14 DNI" FontSize="11" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,8"/>

                        <ItemsControl Grid.Row="1" x:Name="listaAktywnoscDni">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding TloKarty}" CornerRadius="6" Padding="4,6" Margin="2,0">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding DzienSkrot}" FontSize="9" FontWeight="SemiBold" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                                            <Grid Height="60" Width="30" Margin="0,4,0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Background="#3B82F6" CornerRadius="2,2,0,0" Height="{Binding WysokoscTelefony}" VerticalAlignment="Bottom" Margin="0,0,1,0" ToolTip="{Binding TooltipTelefony}"/>
                                                <Border Grid.Column="1" Background="#8B5CF6" CornerRadius="2,2,0,0" Height="{Binding WysokoscStatusy}" VerticalAlignment="Bottom" Margin="0,0,1,0" ToolTip="{Binding TooltipStatusy}"/>
                                                <Border Grid.Column="2" Background="#F59E0B" CornerRadius="2,2,0,0" Height="{Binding WysokoscNotatki}" VerticalAlignment="Bottom" ToolTip="{Binding TooltipNotatki}"/>
                                            </Grid>
                                            <TextBlock Text="{Binding Suma}" FontSize="11" FontWeight="Bold" Foreground="#2563EB" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
