<Window x:Class="Kalendarz1.Kontrakty.Views.KontraktyRankingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ranking hodowców" Height="720" Width="780"
        WindowStartupLocation="CenterOwner" Background="#F1F5F9"
        UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Kontrakty/Views/KontraktyTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" Padding="22,16">
            <StackPanel>
                <TextBlock Text="🏆 Ranking hodowców — wolumen 12 mies." Foreground="White" FontSize="19" FontWeight="Bold"/>
                <TextBlock x:Name="txtPodtytul" Foreground="#C8E6C9" FontSize="12" Margin="0,3,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" Padding="18,10" BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1">
            <TextBlock Text="Dwuklik: hodowca bez umowy → kreator (prefill) · z umową → karta kontraktu. 🎯 = liczy się do ARiMR."
                       Foreground="{StaticResource Muted}" FontSize="11.5"/>
        </Border>

        <DataGrid x:Name="dgRanking" Grid.Row="2" Margin="18,12,18,16" BorderThickness="0" RowHeight="40"
                  MouseDoubleClick="Dg_DoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Pozycja}" Width="44"/>
                <DataGridTextColumn Header="HODOWCA" Binding="{Binding Nazwa}" Width="*"/>
                <DataGridTextColumn Header="WOLUMEN 12M" Binding="{Binding WagaTonyLabel}" Width="120"/>
                <DataGridTextColumn Header="DOSTAW" Binding="{Binding LiczbaDostaw}" Width="80"/>
                <DataGridTextColumn Header="OSTATNIA" Binding="{Binding OstatniaLabel}" Width="90"/>
                <DataGridTemplateColumn Header="STATUS" Width="125">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10" Padding="9,3" HorizontalAlignment="Left"
                                    Background="{Binding StatusTlo}">
                                <TextBlock Text="{Binding StatusLabel}" FontSize="11.5" FontWeight="SemiBold"
                                           Foreground="{Binding StatusKolor}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
