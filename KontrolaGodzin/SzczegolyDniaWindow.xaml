<Window x:Class="Kalendarz1.KontrolaGodzin.SzczegolyDniaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Szczegóły dnia pracy" 
        Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA"
        FontFamily="Segoe UI"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F7FAFC"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E2E8F0"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="RowHeight" Value="28"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EDF2F7"/>
            <Setter Property="Foreground" Value="#1A202C"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="0,0,1,2"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#BEE3F8"/>
                    <Setter Property="Foreground" Value="#1A202C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <StackPanel>
                    <TextBlock x:Name="txtPracownik" Text="Jan Kowalski" FontSize="18" FontWeight="Bold" Foreground="#1A202C"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock x:Name="txtDzial" Text="PRODUKCJA" FontSize="12" Foreground="#3182CE" FontWeight="SemiBold"/>
                        <TextBlock Text="  •  " FontSize="12" Foreground="#A0AEC0"/>
                        <TextBlock x:Name="txtData" Text="20.12.2025 (sobota)" FontSize="12" Foreground="#718096"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Podsumowanie -->
        <Border Grid.Row="1" Background="#FAFBFC" Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Border Background="#F0FFF4" CornerRadius="8" Padding="20,10" Margin="0,0,15,0" BorderBrush="#C6F6D5" BorderThickness="1">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="WEJŚCIE" FontSize="10" Foreground="#276749" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="txtWejscie" Text="05:47" FontSize="24" FontWeight="Bold" Foreground="#276749" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFF5F5" CornerRadius="8" Padding="20,10" Margin="0,0,15,0" BorderBrush="#FED7D7" BorderThickness="1">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="WYJŚCIE" FontSize="10" Foreground="#C53030" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="txtWyjscie" Text="14:32" FontSize="24" FontWeight="Bold" Foreground="#C53030" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Background="#EBF8FF" CornerRadius="8" Padding="20,10" Margin="0,0,15,0" BorderBrush="#BEE3F8" BorderThickness="1">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="CZAS PRACY" FontSize="10" Foreground="#2B6CB0" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="txtCzasPracy" Text="8:45" FontSize="24" FontWeight="Bold" Foreground="#2B6CB0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Background="#FFFAF0" CornerRadius="8" Padding="20,10" BorderBrush="#FEEBC8" BorderThickness="1">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="PRZERWY" FontSize="10" Foreground="#C05621" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="txtPrzerwy" Text="0:30" FontSize="24" FontWeight="Bold" Foreground="#C05621" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Lista rejestracji -->
        <Border Grid.Row="2" Background="White" Margin="15" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" Background="#EDF2F7" Padding="15,10">
                    <TextBlock Text="📋 WSZYSTKIE REJESTRACJE Z TEGO DNIA" FontWeight="Bold" Foreground="#4A5568" FontSize="11"/>
                </Border>
                
                <DataGrid x:Name="gridRejestracje" Grid.Row="1" Margin="1" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="GODZINA" Binding="{Binding Godzina}" Width="80"/>
                        <DataGridTextColumn Header="TYP" Binding="{Binding Typ}" Width="90"/>
                        <DataGridTextColumn Header="PUNKT DOSTĘPU" Binding="{Binding PunktDostepu}" Width="180"/>
                        <DataGridTextColumn Header="URZĄDZENIE" Binding="{Binding Urzadzenie}" Width="150"/>
                        <DataGridTextColumn Header="UWAGI" Binding="{Binding Uwagi}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Stopka -->
        <Border Grid.Row="3" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="15,10">
            <Grid>
                <TextBlock x:Name="txtPodsumowanie" Text="Łącznie: 5 rejestracji" FontSize="11" Foreground="#718096" VerticalAlignment="Center"/>
                <Button Content="Zamknij" HorizontalAlignment="Right" Padding="20,8" Click="BtnZamknij_Click"
                        Background="#E8EDF3" Foreground="#2D3748" BorderBrush="#CBD5E0" BorderThickness="1" Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</Window>
