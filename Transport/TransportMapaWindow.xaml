<Window x:Class="Kalendarz1.Transport.TransportMapaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Mapa Zamówień Transportowych"
        Width="1400" Height="900"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC"
        FontFamily="Segoe UI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Padding="16,12">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#312E81" Offset="0"/>
                    <GradientStop Color="#6366F1" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#x1F69A;" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Mapa Zamówień" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="txtSubtitle" Text="" FontSize="11" Foreground="#A5B4FC" Margin="0,2,0,0" FontStyle="Italic"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Background="#15FFFFFF" CornerRadius="8" Padding="14,8" Margin="0,0,8,0" BorderBrush="#30FFFFFF" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x1F4E6;" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <StackPanel>
                                <TextBlock Text="ZAMÓWIENIA" FontSize="9" Foreground="#A5B4FC" FontWeight="SemiBold"/>
                                <TextBlock x:Name="txtLiczbaZam" Text="0" FontSize="16" FontWeight="Bold" Foreground="White"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Border Background="#15FFFFFF" CornerRadius="8" Padding="14,8" Margin="0,0,8,0" BorderBrush="#30FFFFFF" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x1F4CD;" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <StackPanel>
                                <TextBlock Text="NA MAPIE" FontSize="9" Foreground="#A5B4FC" FontWeight="SemiBold"/>
                                <TextBlock x:Name="txtNaMapie" Text="0" FontSize="16" FontWeight="Bold" Foreground="White"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Border Background="#15FFFFFF" CornerRadius="8" Padding="14,8" BorderBrush="#30FFFFFF" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x1F3D7;" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <StackPanel>
                                <TextBlock Text="POJEMNIKI" FontSize="9" Foreground="#A5B4FC" FontWeight="SemiBold"/>
                                <TextBlock x:Name="txtSumaPoj" Text="0" FontSize="16" FontWeight="Bold" Foreground="White"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="btnDebug" Content="&#x1F41E; DEBUG" Margin="0,0,8,0" Cursor="Hand" Click="BtnDebug_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="#92400E" CornerRadius="8" Padding="10,6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#B45309"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Foreground>
                            <SolidColorBrush Color="White"/>
                        </Button.Foreground>
                    </Button>
                    <Button x:Name="btnZamknij" Content="&#x2715;" Width="32" Padding="0" Cursor="Hand" Click="BtnZamknij_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="#334155" CornerRadius="8" Padding="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#475569"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Foreground>
                            <SolidColorBrush Color="White"/>
                        </Button.Foreground>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- MAP + DEBUG PANEL -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition x:Name="colDebug" Width="0"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FFFFFF" Margin="8" CornerRadius="12" ClipToBounds="True" BorderBrush="#E2E8F0" BorderThickness="1">
                <Grid>
                    <wv2:WebView2 x:Name="webView"/>

                    <Border x:Name="loadingOverlay" Background="#E6F8FAFC" Visibility="Visible">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ProgressBar IsIndeterminate="True" Width="200" Height="6" Foreground="#6366F1" Background="#E2E8F0" BorderThickness="0"/>
                            <TextBlock x:Name="txtLoading" Text="Ladowanie mapy..." FontSize="12" Foreground="#475569" Margin="0,12,0,0" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="400"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- DEBUG PANEL -->
            <Border Grid.Column="1" x:Name="debugPanel" Background="#1A1A2E" Margin="0,8,8,8" CornerRadius="12" BorderBrush="#F59E0B" BorderThickness="1" Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#92400E" CornerRadius="12,12,0,0" Padding="10,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="&#x1F41E; DEBUGGER" FontSize="13" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="&#x1F4CB; Kopiuj" Cursor="Hand" Click="BtnCopyDebug_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bd" Background="#78350F" CornerRadius="6" Padding="10,4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#A16207"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                                <Button.Foreground>
                                    <SolidColorBrush Color="White"/>
                                </Button.Foreground>
                            </Button>
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" x:Name="debugScroll" VerticalScrollBarVisibility="Auto" Margin="0">
                        <TextBlock x:Name="txtDebugLog" Text="" FontSize="11" Foreground="#D1D5DB" FontFamily="Consolas" Padding="10" TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
