<Window x:Class="Kalendarz1.Monitoring.LiveViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        Title="Podgląd na żywo"
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        Closing="Window_Closing">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2D2D2D" Padding="15">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📹" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel>
                        <TextBlock x:Name="CameraNameText" Text="Kamera" FontSize="16" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="CameraInfoText" Text="" FontSize="11" Foreground="#888"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Ellipse x:Name="StatusIndicator" Width="10" Height="10" Fill="#27AE60" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="StatusText" Text="Na żywo" FontSize="11" Foreground="#27AE60" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Video Player -->
        <Grid Grid.Row="1" Background="Black">
            <vlc:VideoView x:Name="VideoPlayer" MediaPlayer="{Binding MediaPlayer}"/>

            <!-- Loading overlay -->
            <Border x:Name="LoadingOverlay" Background="#CC000000" Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="48" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="LoadingText" Text="Łączenie ze strumieniem..."
                               FontSize="14" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Controls -->
        <Border Grid.Row="2" Background="#2D2D2D" Padding="10">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="PlayPauseButton" Content="⏸ Pauza" Click="PlayPauseButton_Click"
                            Background="#3498DB" Foreground="White" BorderThickness="0"
                            Padding="15,8" Margin="0,0,10,0" Cursor="Hand"/>
                    <Button Content="📷 Snapshot" Click="SnapshotButton_Click"
                            Background="#27AE60" Foreground="White" BorderThickness="0"
                            Padding="15,8" Margin="0,0,10,0" Cursor="Hand"/>
                    <Button Content="🔄 Odśwież" Click="RefreshButton_Click"
                            Background="#F39C12" Foreground="White" BorderThickness="0"
                            Padding="15,8" Cursor="Hand"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Jakość:" Foreground="#888" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="QualityCombo" Width="100" SelectionChanged="QualityCombo_SelectionChanged">
                        <ComboBoxItem Content="Główny (HD)" IsSelected="True" Tag="01"/>
                        <ComboBoxItem Content="Substream" Tag="02"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
