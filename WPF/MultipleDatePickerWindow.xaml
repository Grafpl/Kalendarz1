<Window x:Class="Kalendarz1.WPF.MultipleDatePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wybierz dni dla duplikatu"
        Height="550" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F5F7FA"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Color="#000000" Opacity="0.15" BlurRadius="8"/>
                            </Border.Effect>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#95A5A6"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#5C8A3A"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="15,8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Od dnia:"
                   FontSize="13"
                   Foreground="#2C3E50"
                   Margin="0,0,0,5"/>

        <DatePicker x:Name="dpStartDate" 
                    Grid.Row="1"
                    SelectedDateChanged="DateRange_Changed"
                    Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" 
                   Text="Do dnia:"
                   FontSize="13"
                   Foreground="#2C3E50"
                   Margin="0,0,0,5"/>

        <DatePicker x:Name="dpEndDate" 
                    Grid.Row="3"
                    SelectedDateChanged="DateRange_Changed"
                    Margin="0,0,0,10"/>

        <TextBlock x:Name="lblInfo" 
                   Grid.Row="4"
                   Text="Wybierz dni do dublowania:"
                   FontSize="13"
                   Foreground="#2C3E50"
                   Margin="0,0,0,5"
                   VerticalAlignment="Top"/>

        <Border Grid.Row="4" 
                Background="White" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1"
                CornerRadius="4"
                Margin="0,25,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="icDays" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding DisplayText}"
                                      IsChecked="{Binding IsSelected}"
                                      Margin="0,2"
                                      FontSize="12"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Left"
                    Margin="0,10,0,0">
            <Button Content="Zaznacz wszystkie" 
                    Style="{StaticResource ActionButton}"
                    Click="BtnSelectAll_Click"
                    Margin="0,0,5,0"/>
            <Button Content="Odznacz wszystkie" 
                    Style="{StaticResource ActionButton}"
                    Click="BtnDeselectAll_Click"/>
        </StackPanel>

        <CheckBox x:Name="chkCopyNotes" 
                  Grid.Row="6"
                  Content="Kopiuj notatkę z oryginalnego zamówienia"
                  FontSize="12"
                  Margin="0,15,0,0"/>

        <StackPanel Grid.Row="7" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button Content="Dubluj" 
                    Style="{StaticResource PrimaryButton}"
                    Width="100" Height="35"
                    Click="BtnOk_Click"
                    Margin="0,0,10,0"/>
            <Button Content="Anuluj" 
                    Style="{StaticResource SecondaryButton}"
                    Width="100" Height="35"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>