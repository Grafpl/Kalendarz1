<Window x:Class="Kalendarz1.WPF.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dashboard - Bilans produktow"
        Width="1400" Height="800"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFBFC"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E8E8E8"/>
            <Setter Property="RowHeight" Value="35"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <Style x:Key="RightAlignedCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Padding" Value="0,0,8,0"/>
        </Style>

        <Style x:Key="CenterAlignedCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Naglowek -->
        <Border Grid.Row="0" Margin="0,0,0,15">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#2c3e50" Offset="0"/>
                    <GradientStop Color="#3498db" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border CornerRadius="8" Padding="20,15">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Dashboard - Bilans produktow" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="txtSelectedDate" Text="Data: -" FontSize="12" Foreground="#B0C4DE" Margin="0,5,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Border Background="#27AE60" CornerRadius="12" Padding="12,5" Margin="5,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="OK " FontSize="12" Foreground="White" FontWeight="Bold"/>
                                <TextBlock x:Name="txtBilansOk" Text="0" FontSize="12" Foreground="White" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#F39C12" CornerRadius="12" Padding="12,5" Margin="5,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Uwaga " FontSize="12" Foreground="White" FontWeight="Bold"/>
                                <TextBlock x:Name="txtBilansUwaga" Text="0" FontSize="12" Foreground="White" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#E74C3C" CornerRadius="12" Padding="12,5" Margin="5,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Brak " FontSize="12" Foreground="White" FontWeight="Bold"/>
                                <TextBlock x:Name="txtBilansBrak" Text="0" FontSize="12" Foreground="White" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>

        <!-- Panel wyboru daty i odswiezania -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Opacity="0.1" BlurRadius="5"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Wybierz date:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                    <DatePicker x:Name="dpData" Width="130" SelectedDateChanged="DpData_SelectedDateChanged"/>
                </StackPanel>

                <!-- Wydajnosc -->
                <Border Grid.Column="1" Margin="30,0,0,0" Background="#F8F9FA" CornerRadius="8" Padding="15,8">
                    <StackPanel Orientation="Horizontal">
                        <StackPanel Margin="0,0,20,0">
                            <TextBlock Text="Wydajnosc" FontSize="10" Foreground="#7f8c8d" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="txtWydajnoscGlowna" Text="78%" FontSize="24" Foreground="#2c3e50" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <Border Background="#E8F8E8" CornerRadius="5" Padding="10,5" Margin="0,0,10,0">
                            <StackPanel>
                                <TextBlock Text="Tuszka A" FontSize="9" Foreground="#27AE60" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="txtTuszkaA" Text="85%" FontSize="14" Foreground="#27AE60" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#E8F0F8" CornerRadius="5" Padding="10,5">
                            <StackPanel>
                                <TextBlock Text="Tuszka B" FontSize="9" Foreground="#3498DB" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="txtTuszkaB" Text="15%" FontSize="14" Foreground="#3498DB" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <Button x:Name="btnRefresh" Content="Odswiez" Padding="15,8"
                            Background="#3498DB" Foreground="White" BorderThickness="0"
                            Click="BtnRefresh_Click" Cursor="Hand" Margin="0,0,10,0"/>
                    <Button Content="Zamknij" Padding="15,8"
                            Background="#95A5A6" Foreground="White" BorderThickness="0"
                            Click="BtnClose_Click" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Zawartosc - dwie kolumny -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!-- Lewa kolumna - Bilans produktow -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" Opacity="0.1" BlurRadius="10"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Naglowek tabeli -->
                    <Border Grid.Row="0" CornerRadius="8,8,0,0" Padding="15,10">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#2c3e50" Offset="0"/>
                                <GradientStop Color="#3498db" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="BILANS PRODUKTOW" FontSize="14" Foreground="White" FontWeight="Bold"/>
                    </Border>

                    <DataGrid Grid.Row="1" x:Name="dgDashboardProdukty"
                              Style="{StaticResource ModernDataGridStyle}"
                              AutoGenerateColumns="False"
                              CanUserSortColumns="True"
                              Margin="5"/>
                </Grid>
            </Border>

            <!-- Prawa kolumna - Dostepnosc dla handlowcow -->
            <Border Grid.Column="1" Background="White" CornerRadius="8">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" Opacity="0.1" BlurRadius="10"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Naglowek -->
                    <Border Grid.Row="0" CornerRadius="8,8,0,0" Padding="15,10">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#00b09b" Offset="0"/>
                                <GradientStop Color="#96c93d" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="DOSTEPNOSC PRODUKTOW" FontSize="14" Foreground="White" FontWeight="Bold"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="icDostepnoscProduktow" Margin="10">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="5" Padding="10" CornerRadius="8" Width="170"
                                            BorderThickness="2" BorderBrush="{Binding KolorRamki}">
                                        <Border.Background>
                                            <SolidColorBrush Color="{Binding KolorTla}"/>
                                        </Border.Background>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Nazwa produktu -->
                                            <TextBlock Grid.Row="0" Text="{Binding Nazwa}"
                                                       FontWeight="Bold" FontSize="11"
                                                       Foreground="#2c3e50" TextWrapping="Wrap"/>

                                            <!-- Pasek postepu -->
                                            <Grid Grid.Row="1" Margin="0,8,0,6">
                                                <Border Background="#E0E0E0" CornerRadius="4" Height="12"/>
                                                <Border Background="{Binding KolorPaska}" CornerRadius="4" Height="12"
                                                        HorizontalAlignment="Left" Width="{Binding SzerokoscPaska}"/>
                                                <TextBlock Text="{Binding ProcentText}" FontSize="8" FontWeight="Bold"
                                                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>

                                            <!-- Bilans -->
                                            <Border Grid.Row="2" Background="{Binding KolorRamki}" CornerRadius="4" Padding="6,3" Margin="0,0,0,6">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBlock Text="Bilans: " FontSize="10" Foreground="White"/>
                                                    <TextBlock Text="{Binding BilansText}" FontSize="11" Foreground="White" FontWeight="Bold"/>
                                                </StackPanel>
                                            </Border>

                                            <!-- Szczegoly -->
                                            <Grid Grid.Row="3">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Plan/Fakt:" FontSize="8" Foreground="#7f8c8d"/>
                                                    <TextBlock Text="{Binding PlanFaktText}" FontSize="9" FontWeight="SemiBold" Foreground="#2c3e50"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Zamow.:" FontSize="8" Foreground="#7f8c8d"/>
                                                    <TextBlock Text="{Binding ZamowioneText}" FontSize="9" FontWeight="SemiBold" Foreground="#e74c3c"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Stopka -->
        <Border Grid.Row="3" Margin="0,10,0,0" Background="White" CornerRadius="8" Padding="15,10">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Opacity="0.1" BlurRadius="5"/>
            </Border.Effect>
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Suma zamowien: " FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="txtSumaZamowien" Text="0 kg" FontSize="16" FontWeight="Bold" Foreground="#E74C3C" VerticalAlignment="Center"/>
                    <TextBlock Text="  |  Plan produkcji: " FontWeight="SemiBold" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    <TextBlock x:Name="txtPlanProdukcji" Text="0 kg" FontSize="16" FontWeight="Bold" Foreground="#27AE60" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
