<Window x:Class="Kalendarz1.SkrzynkaZakupu.Views.OknoNowaWiadomosc"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nowa wiadomość" Height="600" Width="740" MinHeight="460" MinWidth="540"
        WindowStartupLocation="CenterScreen" Background="#F1F5F9"
        FontFamily="Segoe UI" UseLayoutRounding="True">
    <Window.Resources>
        <SolidColorBrush x:Key="Brand" Color="#2E7D32"/>
        <SolidColorBrush x:Key="BrandDark" Color="#1B5E20"/>
        <SolidColorBrush x:Key="Line" Color="#E5E9EF"/>
        <SolidColorBrush x:Key="TextMute" Color="#94A3B8"/>

        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="b" Background="White" CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Brand}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Lbl" TargetType="TextBlock">
            <Setter Property="Width" Value="60"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Foreground" Value="#475569"/>
        </Style>

        <Style x:Key="Primary" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Brand}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="b" Property="Background" Value="{StaticResource BrandDark}"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Ghost" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="{StaticResource Line}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="b" Property="Background" Value="#F1F5F9"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- nagłówek -->
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="20,14">
            <TextBlock x:Name="TxtNaglowek" Text="✏️  Nowa wiadomość" FontSize="16" FontWeight="Bold" Foreground="#0F172A"/>
        </Border>

        <!-- formularz -->
        <Grid Grid.Row="1" Margin="20,16,20,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="Do:" Style="{StaticResource Lbl}"/>
                <TextBox x:Name="TxtTo"/>
            </DockPanel>
            <DockPanel Grid.Row="1" Margin="0,0,0,8">
                <TextBlock Text="DW:" Style="{StaticResource Lbl}"/>
                <TextBox x:Name="TxtCc"/>
            </DockPanel>
            <DockPanel Grid.Row="2" Margin="0,0,0,8">
                <TextBlock Text="Temat:" Style="{StaticResource Lbl}"/>
                <TextBox x:Name="TxtSubject"/>
            </DockPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,2,0,8">
                <Button Style="{StaticResource Ghost}" Padding="11,6" Click="BtnZalacznik_Click" Content="📎  Dodaj załącznik"/>
                <TextBlock x:Name="TxtZalaczniki" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{StaticResource TextMute}" FontSize="12"/>
            </StackPanel>

            <TextBox x:Name="TxtBody" Grid.Row="4" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto" FontSize="13"/>

            <TextBlock x:Name="TxtStatus" Grid.Row="5" Foreground="#C62828" TextWrapping="Wrap" Margin="2,8,0,0" FontSize="12"/>
        </Grid>

        <!-- stopka -->
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource Line}" BorderThickness="0,1,0,0" Padding="20,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource Ghost}" Width="96" Height="38" Margin="0,0,8,0" Click="BtnAnuluj_Click" Content="Anuluj"/>
                <Button x:Name="BtnWyslij" Style="{StaticResource Primary}" Width="130" Height="38" Click="BtnWyslij_Click" Content="📨  Wyślij"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
