<Window x:Class="Kalendarz1.SkrzynkaZakupu.Views.HasloSkrzynkiDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Konfiguracja skrzynki" Height="640" Width="480"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F1F5F9" FontFamily="Segoe UI" UseLayoutRounding="True">
    <Window.Resources>
        <SolidColorBrush x:Key="Brand" Color="#2E7D32"/>
        <SolidColorBrush x:Key="BrandDark" Color="#1B5E20"/>
        <SolidColorBrush x:Key="Line" Color="#E5E9EF"/>

        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="b" Background="White" CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Brand}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="b" Background="White" CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Brand}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Primary" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Brand}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="b" Property="Background" Value="{StaticResource BrandDark}"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.5"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Ghost" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="{StaticResource Line}" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="b" Property="Background" Value="#F1F5F9"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,18">
            <Border Width="44" Height="44" CornerRadius="12" Background="#E8F5E9">
                <TextBlock Text="📧" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Margin="13,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Skrzynka Zakupu" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                <TextBlock Text="Dane dostępowe — hasło zapisuje się tylko na tym komputerze"
                           TextWrapping="Wrap" Foreground="#94A3B8" FontSize="11.5" Width="330"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Adres e-mail / login" FontWeight="SemiBold" FontSize="12.5" Foreground="#475569" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtEmail"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Hasło" FontWeight="SemiBold" FontSize="12.5" Foreground="#475569" Margin="0,0,0,4"/>
            <PasswordBox x:Name="TxtPass"/>
        </StackPanel>

        <Border Grid.Row="3" Background="White" BorderBrush="{StaticResource Line}" BorderThickness="1"
                CornerRadius="10" Padding="14" VerticalAlignment="Top" Margin="0,4,0,0">
            <StackPanel>
                <TextBlock Text="Serwery (dpoczta) — zwykle bez zmian" FontWeight="SemiBold" FontSize="12" Foreground="#475569" Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="66"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBox x:Name="TxtImap" Grid.Row="0" Grid.Column="0" Margin="0,0,6,6"/>
                    <TextBox x:Name="TxtImapPort" Grid.Row="0" Grid.Column="1" Margin="0,0,0,6"/>
                    <TextBox x:Name="TxtSmtp" Grid.Row="1" Grid.Column="0" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtSmtpPort" Grid.Row="1" Grid.Column="1"/>
                </Grid>
                <TextBlock x:Name="TxtStatus" Foreground="#C62828" TextWrapping="Wrap" Margin="0,8,0,0" FontSize="12"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="4" Margin="0,12,0,0">
            <TextBlock Text="Podpis (dodawany przy pisaniu)" FontWeight="SemiBold" FontSize="12.5" Foreground="#475569" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtPodpis" Height="70" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button x:Name="BtnTest" Style="{StaticResource Ghost}" Content="Testuj połączenie" Width="140" Height="38" Margin="0,0,8,0" Click="BtnTest_Click"/>
            <Button x:Name="BtnZapisz" Style="{StaticResource Primary}" Content="Zapisz" Width="104" Height="38" Click="BtnZapisz_Click"/>
            <Button Style="{StaticResource Ghost}" Content="Anuluj" Width="84" Height="38" Margin="8,0,0,0" Click="BtnAnuluj_Click"/>
        </StackPanel>
    </Grid>
</Window>
