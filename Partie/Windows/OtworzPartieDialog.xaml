<Window x:Class="Kalendarz1.Partie.Windows.OtworzPartieDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ponowne otwarcie partii"
        Width="420" Height="300"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="BgPrimaryBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="BgSecondaryBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="BgTertiaryBrush" Color="#334155"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#94A3B8"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#334155"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BgPrimaryBrush}" CornerRadius="10" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" Color="Black" Opacity="0.4" BlurRadius="15"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#8E44AD" CornerRadius="10,10,0,0" Padding="16,14">
                <StackPanel>
                    <TextBlock x:Name="txtHeader" Text="PONOWNE OTWARCIE PARTII" FontSize="16" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="txtSubHeader" FontSize="12" Foreground="#D7BDE2" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Content -->
            <Border Grid.Row="1" Background="{StaticResource BgSecondaryBrush}" Padding="20,14">
                <StackPanel>
                    <!-- Warning -->
                    <Border Background="#7D2C0A" CornerRadius="6" Padding="12,10" Margin="0,0,0,12">
                        <TextBlock Foreground="#F5B041" FontSize="12" TextWrapping="Wrap"
                                   Text="OPERACJA RYZYKOWNA! Ponowne otwarcie zamknietej partii moze wplynac na rozliczenia i raporty."/>
                    </Border>

                    <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,2">
                        <Run Text="Zamknieta: "/>
                        <Run x:Name="runZamknieta" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,12">
                        <Run Text="Przez: "/>
                        <Run x:Name="runPrzez" FontWeight="SemiBold"/>
                    </TextBlock>

                    <TextBlock Text="Powod otwarcia *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                    <TextBox x:Name="txtPowod" Background="{StaticResource BgTertiaryBrush}"
                             Foreground="{StaticResource TextPrimaryBrush}" BorderBrush="{StaticResource BorderBrush}"
                             BorderThickness="1" Padding="8,6" FontSize="12" Height="60"
                             TextWrapping="Wrap" AcceptsReturn="True" CaretBrush="White"/>
                </StackPanel>
            </Border>

            <!-- Buttons -->
            <Border Grid.Row="2" Background="{StaticResource BgSecondaryBrush}" CornerRadius="0,0,10,10" Padding="20,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="Anuluj" Background="#475569" Style="{StaticResource ActionButton}" Click="BtnCancel_Click" Margin="0,0,6,0"/>
                    <Button Grid.Column="1" x:Name="BtnOtworz" Content="OTWORZ PARTIE"
                            Background="#8E44AD" Style="{StaticResource ActionButton}" Click="BtnOtworz_Click" Margin="6,0,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
