<Window x:Class="Kalendarz1.WidokMatrycaWPF"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Kalendarz1"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Matryca Transport - Planowanie dostaw żywca"
        Height="900" Width="1900"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#ECEFF1"
        FontFamily="Segoe UI"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Konwerter skrótów godzin -->
        <local:TimeShortcutConverter x:Key="TimeShortcutConverter"/>
        <!-- Styl dla przycisków głównych -->
        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#5C8A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Color="#000000" Opacity="0.15" BlurRadius="8"/>
                            </Border.Effect>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4B732F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A5C24"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl dla przycisków pomocniczych -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource MainButtonStyle}">
            <Setter Property="Background" Value="#607D8B"/>
        </Style>

        <!-- Styl dla przycisków nawigacji -->
        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource MainButtonStyle}">
            <Setter Property="Background" Value="#34495E"/>
            <Setter Property="Width" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Styl dla przycisków akcji -->
        <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource MainButtonStyle}">
            <Setter Property="Background" Value="#3498DB"/>
        </Style>

        <!-- Styl dla przycisków niebezpiecznych -->
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource MainButtonStyle}">
            <Setter Property="Background" Value="#E74C3C"/>
        </Style>

        <!-- Styl dla nagłówków DataGrid -->
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#34495E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="8,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
        </Style>

        <!-- Style dla nagłówków kolorowych grup -->
        <Style x:Key="HodowcaHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridHeaderStyle}">
            <Setter Property="Background" Value="#F57C00"/>
        </Style>

        <Style x:Key="TransportHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridHeaderStyle}">
            <Setter Property="Background" Value="#1976D2"/>
        </Style>

        <Style x:Key="CzasHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridHeaderStyle}">
            <Setter Property="Background" Value="#7B1FA2"/>
        </Style>

        <Style x:Key="DaneHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridHeaderStyle}">
            <Setter Property="Background" Value="#388E3C"/>
        </Style>

        <Style x:Key="CenaHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridHeaderStyle}">
            <Setter Property="Background" Value="#1976D2"/>
        </Style>

        <!-- Style dla komórek -->
        <Style x:Key="CellStyleOrange" TargetType="DataGridCell">
            <Setter Property="Background" Value="#FFF3E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FFE0B2"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CellStyleBlue" TargetType="DataGridCell">
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#BBDEFB"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CellStylePurple" TargetType="DataGridCell">
            <Setter Property="Background" Value="#F3E5F5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E1BEE7"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CellStyleGreen" TargetType="DataGridCell">
            <Setter Property="Background" Value="#E8F5E9"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#C8E6C9"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Styl dla komórek DataGrid -->
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#C8E6C9"/>
                    <Setter Property="Foreground" Value="#2C3E50"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Styl dla DataGrid -->
        <Style x:Key="MainDataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ColumnHeaderStyle" Value="{StaticResource DataGridHeaderStyle}"/>
            <Setter Property="CellStyle" Value="{StaticResource DataGridCellStyle}"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
        </Style>

        <!-- Style dla kolorowanych kolumn matrycy -->
        <Style x:Key="LpColumnStyle" TargetType="TextBlock">
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="HodowcaColumnStyle" TargetType="TextBlock">
            <Setter Property="Background" Value="#FFF8E1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="TransportColumnStyle" TargetType="TextBlock">
            <Setter Property="Background" Value="#E8F5E9"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CzasColumnStyle" TargetType="TextBlock">
            <Setter Property="Background" Value="#FCE4EC"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <!-- Styl dla GroupBox -->
        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" FontSize="13" Foreground="#34495E"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- NAGŁÓWEK -->
        <Border Grid.Row="0" Background="#5C8A3A" Padding="20,15">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="3" Color="#000000" Opacity="0.2" BlurRadius="10"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Tytuł -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="MATRYCA TRANSPORT - PLANOWANIE DOSTAW"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock Text="System planowania odbioru żywca od hodowców"
                               FontSize="12"
                               Foreground="#E0F0D6"
                               Margin="0,3,0,0"/>
                </StackPanel>

                <!-- Wybór daty -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Data:"
                               Foreground="White"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>
                    <Button Content="◀"
                            Click="BtnPreviousDay_Click"
                            Style="{StaticResource NavButtonStyle}"/>
                    <DatePicker x:Name="dateTimePicker1"
                                SelectedDateChanged="DateTimePicker1_SelectedDateChanged"
                                Width="160"
                                FontSize="13"
                                Padding="10,8"
                                Margin="5,0"
                                VerticalContentAlignment="Center"/>
                    <Button Content="▶"
                            Click="BtnNextDay_Click"
                            Style="{StaticResource NavButtonStyle}"/>
                    <TextBlock x:Name="lblDayOfWeek"
                               Text=""
                               Foreground="White"
                               FontWeight="SemiBold"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="15,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- GŁÓWNA ZAWARTOŚĆ -->
        <Grid Grid.Row="1" Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- LEWA STRONA - MATRYCA -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" Color="#000000" Opacity="0.1" BlurRadius="10"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Pasek narzędzi matrycy -->
                    <Border Grid.Row="0" Background="#F8F9FA" Padding="12" CornerRadius="10,10,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Przyciski kolejności -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <!-- ComboBox z dostawami z harmonogramu -->
                                <Border Background="#E3F2FD" CornerRadius="4" Padding="8,4" Margin="4" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Dostawa:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                        <ComboBox x:Name="cmbHarmonogramDostawy"
                                                  Width="280"
                                                  DisplayMemberPath="DisplayText"
                                                  SelectedValuePath="LP"
                                                  SelectionChanged="CmbHarmonogramDostawy_SelectionChanged"
                                                  ToolTip="Wybierz dostawę z harmonogramu aby skopiować cenę, ubytek i typ ceny do wierszy tego dostawcy"/>
                                        <Button x:Name="btnApplyHarmonogramPrice"
                                                Content="Zastosuj"
                                                Click="BtnApplyHarmonogramPrice_Click"
                                                Background="#4CAF50"
                                                Foreground="White"
                                                FontWeight="SemiBold"
                                                Padding="10,4"
                                                Margin="8,0,0,0"
                                                Cursor="Hand"
                                                BorderThickness="0"
                                                ToolTip="Automatycznie zastosuj cenę do wierszy z tym samym dostawcą (po nazwie)">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#388E3C"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                        <Button x:Name="btnApplyHarmonogramPriceManual"
                                                Content="Wklej ręcznie"
                                                Click="BtnApplyHarmonogramPriceManual_Click"
                                                Background="#FF9800"
                                                Foreground="White"
                                                FontWeight="SemiBold"
                                                Padding="10,4"
                                                Margin="4,0,0,0"
                                                Cursor="Hand"
                                                BorderThickness="0"
                                                ToolTip="Ręcznie wklej cenę do zaznaczonego wiersza (gdy nazwy się różnią)">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#F57C00"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </StackPanel>
                                </Border>
                                <Border Width="1" Background="#E0E0E0" Margin="8,5"/>
                                <Button x:Name="btnImportAvilog"
                                        Content="📄 IMPORT PDF"
                                        Click="BtnImportAvilog_Click"
                                        Background="#E65100"
                                        Foreground="White"
                                        FontWeight="Bold"
                                        FontSize="11"
                                        Padding="14,10"
                                        Margin="4"
                                        Cursor="Hand"
                                        ToolTip="Importuj plan transportu z pliku PDF AVILOG">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                <Border.Effect>
                                                    <DropShadowEffect ShadowDepth="2" Color="#000000" Opacity="0.15" BlurRadius="8"/>
                                                </Border.Effect>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#BF360C"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <Button x:Name="btnImportExcel"
                                        Content="📊 IMPORT EXCEL"
                                        Click="BtnImportExcel_Click"
                                        Background="#217346"
                                        Foreground="White"
                                        FontWeight="Bold"
                                        FontSize="11"
                                        Padding="14,10"
                                        Margin="4"
                                        Cursor="Hand"
                                        ToolTip="Importuj plan transportu z pliku Excel AVILOG (.xls/.xlsx)">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                <Border.Effect>
                                                    <DropShadowEffect ShadowDepth="2" Color="#000000" Opacity="0.15" BlurRadius="8"/>
                                                </Border.Effect>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#1D5C33"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <Button x:Name="btnLoadFromDatabase"
                                        Content="🗄️ WCZYTAJ Z BAZY"
                                        Click="BtnLoadFromDatabase_Click"
                                        Background="#1976D2"
                                        Foreground="White"
                                        FontWeight="Bold"
                                        FontSize="11"
                                        Padding="14,10"
                                        Margin="4"
                                        Cursor="Hand"
                                        ToolTip="Wczytaj zapisane dane z bazy dla wybranej daty">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                <Border.Effect>
                                                    <DropShadowEffect ShadowDepth="2" Color="#000000" Opacity="0.15" BlurRadius="8"/>
                                                </Border.Effect>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#1565C0"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <Border Width="1" Background="#E0E0E0" Margin="8,5"/>
                                <Button Content="Przesuń w górę"
                                        Click="BtnMoveUp_Click"
                                        Style="{StaticResource ActionButtonStyle}"
                                        FontSize="11"/>
                                <Button Content="Przesuń w dół"
                                        Click="BtnMoveDown_Click"
                                        Style="{StaticResource ActionButtonStyle}"
                                        FontSize="11"/>
                                <Button Content="Sugeruj kolejność"
                                        Click="BtnSuggestOrder_Click"
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        FontSize="11"
                                        ToolTip="Sortuj wg odległości - najbliższe rano"/>
                            </StackPanel>

                            <!-- Przyciski edycji -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <Button Content="+ Dodaj"
                                        Click="BtnAddRow_Click"
                                        Style="{StaticResource MainButtonStyle}"
                                        FontSize="11"/>
                                <Button Content="Usuń"
                                        Click="BtnDeleteRow_Click"
                                        Style="{StaticResource DangerButtonStyle}"
                                        FontSize="11"/>
                                <Button Content="Odśwież"
                                        Click="BtnRefresh_Click"
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        FontSize="11"/>
                                <Button Content="ZAPISZ DO BAZY"
                                        Click="BtnSaveToDatabase_Click"
                                        Style="{StaticResource MainButtonStyle}"
                                        FontSize="12"
                                        FontWeight="Bold"
                                        Padding="20,10"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- DataGrid Matrycy -->
                    <DataGrid x:Name="dataGridMatryca"
                              Grid.Row="1"
                              Style="{StaticResource MainDataGridStyle}"
                              AllowDrop="True"
                              PreviewMouseLeftButtonDown="DataGrid_PreviewMouseLeftButtonDown"
                              PreviewMouseMove="DataGrid_PreviewMouseMove"
                              CellEditEnding="DataGridMatryca_CellEditEnding"
                              Drop="DataGrid_Drop"
                              DragOver="DataGrid_DragOver"
                              SelectionChanged="DataGridMatryca_SelectionChanged"
                              Margin="5">

                        <DataGrid.Columns>
                            <!-- LP Dostawy -->
                            <DataGridTextColumn Header="LP" Binding="{Binding LpDostawy}" Width="50" IsReadOnly="True"
                                                HeaderStyle="{StaticResource DataGridHeaderStyle}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="14"/>
                                        <Setter Property="Foreground" Value="#1976D2"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Numer Specyfikacji -->
                            <DataGridTextColumn Header="Nr Spec" Binding="{Binding Number, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="70"
                                                HeaderStyle="{StaticResource DataGridHeaderStyle}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="13"/>
                                        <Setter Property="Foreground" Value="#D32F2F"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                                <DataGridTextColumn.EditingElementStyle>
                                    <Style TargetType="TextBox">
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="13"/>
                                    </Style>
                                </DataGridTextColumn.EditingElementStyle>
                            </DataGridTextColumn>

                            <!-- Hodowca - ComboBox -->
                            <DataGridComboBoxColumn x:Name="colHodowca"
                                                    Header="Hodowca"
                                                    SelectedValueBinding="{Binding HodowcaNazwa, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    DisplayMemberPath="Name"
                                                    SelectedValuePath="Name"
                                                    Width="180"
                                                    HeaderStyle="{StaticResource HodowcaHeaderStyle}"/>

                            <!-- Waga deklarowana -->
                            <DataGridTextColumn Header="Waga (kg)" Binding="{Binding WagaDek, StringFormat={}{0:N2}}" Width="90"
                                                HeaderStyle="{StaticResource DaneHeaderStyle}"
                                                CellStyle="{StaticResource CellStyleGreen}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="0,0,8,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Sztuki -->
                            <DataGridTextColumn Header="Sztuk" Binding="{Binding SztPoj}" Width="70"
                                                HeaderStyle="{StaticResource DaneHeaderStyle}"
                                                CellStyle="{StaticResource CellStyleGreen}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Kierowca - ComboBox -->
                            <DataGridComboBoxColumn x:Name="colKierowca"
                                                    Header="Kierowca"
                                                    SelectedValueBinding="{Binding DriverGID}"
                                                    DisplayMemberPath="Name"
                                                    SelectedValuePath="GID"
                                                    Width="150"
                                                    HeaderStyle="{StaticResource TransportHeaderStyle}"/>

                            <!-- Ciągnik - ComboBox -->
                            <DataGridComboBoxColumn x:Name="colCiagnik"
                                                    Header="Ciągnik"
                                                    SelectedValueBinding="{Binding CarID}"
                                                    DisplayMemberPath="ID"
                                                    SelectedValuePath="ID"
                                                    Width="100"
                                                    HeaderStyle="{StaticResource TransportHeaderStyle}"/>

                            <!-- Naczepa - ComboBox -->
                            <DataGridComboBoxColumn x:Name="colNaczepa"
                                                    Header="Naczepa"
                                                    SelectedValueBinding="{Binding TrailerID}"
                                                    DisplayMemberPath="ID"
                                                    SelectedValuePath="ID"
                                                    Width="100"
                                                    HeaderStyle="{StaticResource TransportHeaderStyle}"/>

                            <!-- Godziny - z konwerterem obsługującym skróty -->
                            <DataGridTemplateColumn Header="Wyjazd" Width="70"
                                                    HeaderStyle="{StaticResource CzasHeaderStyle}"
                                                    CellStyle="{StaticResource CellStylePurple}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Wyjazd, Converter={StaticResource TimeShortcutConverter}}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Wyjazd, Converter={StaticResource TimeShortcutConverter}, UpdateSourceTrigger=LostFocus, Mode=TwoWay}"
                                                 HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                                                 BorderThickness="0" Background="White"
                                                 PreviewKeyDown="TimeTextBox_PreviewKeyDown"
                                                 Loaded="TimeTextBox_Loaded"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Załadunek" Width="85"
                                                    HeaderStyle="{StaticResource CzasHeaderStyle}"
                                                    CellStyle="{StaticResource CellStylePurple}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Zaladunek, Converter={StaticResource TimeShortcutConverter}}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="#7B1FA2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Zaladunek, Converter={StaticResource TimeShortcutConverter}, UpdateSourceTrigger=LostFocus, Mode=TwoWay}"
                                                 HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                                                 BorderThickness="0" Background="White"
                                                 PreviewKeyDown="TimeTextBox_PreviewKeyDown"
                                                 Loaded="TimeTextBox_Loaded"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Przyjazd" Width="75"
                                                    HeaderStyle="{StaticResource CzasHeaderStyle}"
                                                    CellStyle="{StaticResource CellStylePurple}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Przyjazd, Converter={StaticResource TimeShortcutConverter}}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Przyjazd, Converter={StaticResource TimeShortcutConverter}, UpdateSourceTrigger=LostFocus, Mode=TwoWay}"
                                                 HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                                                 BorderThickness="0" Background="White"
                                                 PreviewKeyDown="TimeTextBox_PreviewKeyDown"
                                                 Loaded="TimeTextBox_Loaded"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- Wózek - ComboBox -->
                            <DataGridComboBoxColumn x:Name="colWozek"
                                                    Header="Wózek"
                                                    SelectedValueBinding="{Binding NotkaWozek}"
                                                    Width="140"
                                                    HeaderStyle="{StaticResource TransportHeaderStyle}"/>

                            <!-- Cena -->
                            <DataGridTextColumn Header="Cena" Binding="{Binding Price, StringFormat={}{0:N2}}" Width="70"
                                                HeaderStyle="{StaticResource CenaHeaderStyle}"
                                                CellStyle="{StaticResource CellStyleBlue}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="0,0,8,0"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#1565C0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Ubytek (Loss) -->
                            <DataGridTextColumn Header="Ubytek%" Binding="{Binding Loss, StringFormat={}{0:N2}}" Width="70"
                                                HeaderStyle="{StaticResource CenaHeaderStyle}"
                                                CellStyle="{StaticResource CellStyleBlue}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="#C62828"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Typ Ceny -->
                            <DataGridTemplateColumn Header="Typ Ceny" Width="100"
                                                    HeaderStyle="{StaticResource CenaHeaderStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox SelectedValue="{Binding PriceTypeID, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                  SelectedValuePath="Tag"
                                                  BorderThickness="0"
                                                  Background="Transparent"
                                                  HorizontalAlignment="Stretch">
                                            <ComboBoxItem Content="">
                                                <ComboBoxItem.Tag>
                                                    <sys:Int32>-1</sys:Int32>
                                                </ComboBoxItem.Tag>
                                            </ComboBoxItem>
                                            <ComboBoxItem Content="wolnyrynek">
                                                <ComboBoxItem.Tag>
                                                    <sys:Int32>1</sys:Int32>
                                                </ComboBoxItem.Tag>
                                            </ComboBoxItem>
                                            <ComboBoxItem Content="rolnicza">
                                                <ComboBoxItem.Tag>
                                                    <sys:Int32>2</sys:Int32>
                                                </ComboBoxItem.Tag>
                                            </ComboBoxItem>
                                            <ComboBoxItem Content="łączona">
                                                <ComboBoxItem.Tag>
                                                    <sys:Int32>3</sys:Int32>
                                                </ComboBoxItem.Tag>
                                            </ComboBoxItem>
                                            <ComboBoxItem Content="ministerialna">
                                                <ComboBoxItem.Tag>
                                                    <sys:Int32>4</sys:Int32>
                                                </ComboBoxItem.Tag>
                                            </ComboBoxItem>
                                        </ComboBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Status SMS -->
                            <DataGridTemplateColumn Header="SMS" Width="130" IsReadOnly="True"
                                                    HeaderStyle="{StaticResource DataGridHeaderStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="4,2" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SmsSent}" Value="True">
                                                            <Setter Property="Background" Value="#C8E6C9"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding SmsStatus}" VerticalAlignment="Center" FontSize="10">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#888888"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding SmsSent}" Value="True">
                                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Notatka ze skrótami godzin -->
                    <Border Grid.Row="2" Background="#FFF8E1" Padding="10,8" CornerRadius="0,0,10,10" BorderBrush="#FFE082" BorderThickness="0,1,0,0">
                        <StackPanel>
                            <TextBlock FontWeight="Bold" FontSize="11" Foreground="#E65100" Margin="0,0,0,4">
                                Skróty wprowadzania godzin:
                            </TextBlock>
                            <TextBlock FontSize="10" Foreground="#5D4037" TextWrapping="Wrap">
                                8 = 08:00 | 915 = 09:15 | 1015 = 10:15 | 1430 = 14:30
                            </TextBlock>
                            <TextBlock FontSize="10" Foreground="#5D4037" TextWrapping="Wrap" Margin="0,3,0,0">
                                <Run FontWeight="SemiBold">Zapis dnia:</Run> godz. 00:00-17:00 = wybrany dzień | godz. 17:01-23:59 = poprzedni dzień
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- PRAWA STRONA - PANEL INFORMACYJNY -->
            <Border Grid.Column="1" Background="White" CornerRadius="10">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" Color="#000000" Opacity="0.1" BlurRadius="10"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Nagłówek panelu -->
                    <Border Grid.Row="0" Background="#34495E" Padding="15,12" CornerRadius="10,10,0,0">
                        <TextBlock Text="INFORMACJE O DOSTAWIE"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="White"/>
                    </Border>

                    <!-- Zawartość panelu -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15">
                        <StackPanel>
                            <!-- Dane Hodowcy -->
                            <GroupBox Header="Dane Hodowcy" Style="{StaticResource ModernGroupBox}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Nazwa:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="lblHodowcaNazwa" Text="-"
                                               FontWeight="Bold" Foreground="#F57C00" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Adres:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="lblHodowcaAdres" Text="-"
                                               TextWrapping="Wrap" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Miasto:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="lblHodowcaMiejscowosc" Text="-" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Odległość:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="lblHodowcaOdleglosc" Text="-"
                                               FontWeight="Bold" Foreground="#1976D2" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Telefon:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="4" Grid.Column="1" x:Name="lblHodowcaTelefon" Text="-"
                                               Foreground="#388E3C" FontWeight="SemiBold" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Email:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="5" Grid.Column="1" x:Name="lblHodowcaEmail" Text="-"
                                               Foreground="#1976D2" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                </Grid>
                            </GroupBox>

                            <!-- Dane Transportowe -->
                            <GroupBox Header="Dane Transportowe" Style="{StaticResource ModernGroupBox}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Kierowca:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="lblKierowca" Text="-"
                                               FontWeight="Bold" Foreground="#1976D2" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Tel. kier.:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="lblKierowcaTelefon" Text="-"
                                               Foreground="#388E3C" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Ciągnik:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="lblCiagnik" Text="-"
                                               FontWeight="Bold" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Naczepa:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="lblNaczepa" Text="-" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Wózek:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="4" Grid.Column="1" x:Name="lblWozek" Text="-" VerticalAlignment="Center"/>
                                </Grid>
                            </GroupBox>

                            <!-- Dane Ilościowe -->
                            <GroupBox Header="Dane Ilościowe" Style="{StaticResource ModernGroupBox}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Sztuki:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="lblSztukiDek" Text="-"
                                               FontWeight="Bold" FontSize="16" Foreground="#7B1FA2" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Waga dek.:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="lblWagaDek" Text="-"
                                               FontWeight="Bold" FontSize="14" Foreground="#388E3C" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Waga całk.:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="lblWagaCalkowita" Text="-"
                                               FontWeight="Bold" FontSize="16" Foreground="#E74C3C" VerticalAlignment="Center"/>
                                </Grid>
                            </GroupBox>

                            <!-- Harmonogram -->
                            <GroupBox Header="Harmonogram" Style="{StaticResource ModernGroupBox}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Wyjazd:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="lblWyjazd" Text="-"
                                               FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Załadunek:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="lblZaladunek" Text="-"
                                               FontWeight="Bold" FontSize="16" Foreground="#7B1FA2" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Przyjazd:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="lblPrzyjazd" Text="-"
                                               FontWeight="Bold" FontSize="14" Foreground="#E74C3C" VerticalAlignment="Center"/>
                                </Grid>
                            </GroupBox>

                            <!-- Status SMS -->
                            <GroupBox Header="Status SMS" Style="{StaticResource ModernGroupBox}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                        <RowDefinition Height="28"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Status:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="lblSmsStatus" Text="Nie wysłano"
                                               FontWeight="Bold" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Wysłał:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="lblSmsUser" Text="-"
                                               Foreground="#1976D2" VerticalAlignment="Center"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Kiedy:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="lblSmsData" Text="-"
                                               Foreground="#7B1FA2" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </Grid>
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Przyciski akcji w panelu -->
                    <Border Grid.Row="2" Background="#F8F9FA" Padding="10" CornerRadius="0,0,10,10">
                        <StackPanel>
                            <TextBlock Text="Akcje SMS" FontWeight="Bold" FontSize="12" Margin="0,0,0,8" Foreground="#34495E"/>

                            <Button x:Name="btnSmsZaladunekAll"
                                    Content="🚛 SMS WSZYSTKIE AUTA"
                                    Click="BtnSmsZaladunekAll_Click"
                                    Style="{StaticResource MainButtonStyle}"
                                    Background="#3498DB"
                                    FontSize="12"
                                    HorizontalAlignment="Stretch"
                                    ToolTip="SMS zbiorczy - wszystkie auta dla tego hodowcy"/>

                            <Button x:Name="btnSmsZaladunekOne"
                                    Content="🚚 SMS TO AUTO"
                                    Click="BtnSmsZaladunekOne_Click"
                                    Style="{StaticResource MainButtonStyle}"
                                    Background="#2980B9"
                                    FontSize="12"
                                    HorizontalAlignment="Stretch"
                                    ToolTip="SMS tylko dla wybranego auta"/>

                            <Button x:Name="btnEdytujHodowce"
                                    Content="✏️ EDYTUJ HODOWCĘ"
                                    Click="BtnEdytujHodowce_Click"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    FontSize="12"
                                    HorizontalAlignment="Stretch"
                                    ToolTip="Otwórz formularz edycji danych hodowcy"/>

                            <Separator Margin="0,8"/>

                            <Button x:Name="btnStatystyki"
                                    Content="📊 STATYSTYKI PRACOWNIKÓW"
                                    Click="BtnStatystyki_Click"
                                    Style="{StaticResource MainButtonStyle}"
                                    Background="#E67E22"
                                    FontSize="12"
                                    HorizontalAlignment="Stretch"
                                    ToolTip="Zobacz kto wysyła najwięcej SMS"/>

                            <Button x:Name="btnHistoriaZmian"
                                    Content="📜 HISTORIA ZMIAN"
                                    Click="BtnHistoriaZmian_Click"
                                    Style="{StaticResource MainButtonStyle}"
                                    Background="#607D8B"
                                    FontSize="12"
                                    HorizontalAlignment="Stretch"
                                    ToolTip="Zobacz historię zmian w specyfikacji (audit log)"/>

                            <Button x:Name="btnShowSpecyfikacja"
                                    Content="📋 OTWÓRZ SPECYFIKACJĘ"
                                    Click="BtnShowFullSpecyfikacja_Click"
                                    Style="{StaticResource MainButtonStyle}"
                                    Background="#9B59B6"
                                    FontSize="12"
                                    HorizontalAlignment="Stretch"
                                    ToolTip="Otwórz pełną specyfikację dostaw"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- STOPKA ZE STATYSTYKAMI -->
        <Border Grid.Row="2" Background="White" Padding="15" Margin="15,0,15,15">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" Color="#000000" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Statystyki -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Background="#E3F2FD" CornerRadius="6" Padding="18,10" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🚚 Dostaw: " FontWeight="SemiBold" Foreground="#1976D2" FontSize="13"/>
                            <TextBlock x:Name="lblRecordCount" Text="0" FontWeight="Bold" Foreground="#1976D2" FontSize="14"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#E8F5E9" CornerRadius="6" Padding="18,10" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚖️ Suma wagi: " FontWeight="SemiBold" Foreground="#388E3C" FontSize="13"/>
                            <TextBlock x:Name="lblTotalWeight" Text="0 kg" FontWeight="Bold" Foreground="#388E3C" FontSize="14"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FFF3E0" CornerRadius="6" Padding="18,10" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🐔 Suma sztuk: " FontWeight="SemiBold" Foreground="#F57C00" FontSize="13"/>
                            <TextBlock x:Name="lblTotalPieces" Text="0" FontWeight="Bold" Foreground="#F57C00" FontSize="14"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#F3E5F5" CornerRadius="6" Padding="18,10" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👤 Hodowców: " FontWeight="SemiBold" Foreground="#7B1FA2" FontSize="13"/>
                            <TextBlock x:Name="lblHodowcyCount" Text="0" FontWeight="Bold" Foreground="#7B1FA2" FontSize="14"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FCE4EC" CornerRadius="6" Padding="18,10" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📍 Suma km: " FontWeight="SemiBold" Foreground="#C2185B" FontSize="13"/>
                            <TextBlock x:Name="lblTotalKm" Text="0" FontWeight="Bold" Foreground="#C2185B" FontSize="14"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Źródło danych -->
                <Border Grid.Column="1" Background="#ECEFF1" CornerRadius="6" Padding="15,8" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Źródło: " Foreground="#7F8C8D"/>
                        <TextBlock x:Name="lblDataSource" Text="-" FontWeight="SemiBold" Foreground="#34495E"/>
                    </StackPanel>
                </Border>

                <!-- Status -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Status: " Foreground="#7F8C8D"/>
                    <TextBlock x:Name="lblStatus" Text="Gotowy" FontWeight="SemiBold" Foreground="#5C8A3A"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
