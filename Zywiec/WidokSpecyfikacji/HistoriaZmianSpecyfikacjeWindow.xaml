<Window x:Class="Kalendarz1.HistoriaZmianSpecyfikacjeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Historia zmian - Specyfikacje"
        Height="850" Width="1300"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Style dla przycisków -->
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#757575"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#616161"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ExportButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#388E3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style dla TextBox -->
        <Style x:Key="FilterTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style dla ComboBox -->
        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <!-- Style dla DatePicker -->
        <Style x:Key="FilterDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Padding" Value="4"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <!-- Style dla Label -->
        <Style x:Key="FilterLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Setter Property="Margin" Value="0,0,5,3"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <Border Grid.Row="0" Background="#1976D2" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Historia zmian specyfikacji"
                               FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="txtSubtitle" Text="Przeglądaj i filtruj historię wszystkich zmian"
                               FontSize="13" Foreground="#BBDEFB" Margin="0,3,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="txtRecordCount" Text="0 rekordów"
                               FontSize="16" Foreground="White" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="Odśwież" Click="BtnRefresh_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Panel filtrów -->
        <Border Grid.Row="1" Background="White" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Rząd 1: Daty i wyszukiwanie -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Data od:" Style="{StaticResource FilterLabelStyle}"/>
                        <DatePicker x:Name="dateFrom" Width="130" Style="{StaticResource FilterDatePickerStyle}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Data do:" Style="{StaticResource FilterLabelStyle}"/>
                        <DatePicker x:Name="dateTo" Width="130" Style="{StaticResource FilterDatePickerStyle}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Pole:" Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox x:Name="cboField" Width="150" Style="{StaticResource FilterComboBoxStyle}"
                                  SelectionChanged="Filter_Changed">
                            <ComboBoxItem Content="-- Wszystkie --" IsSelected="True"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Użytkownik:" Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox x:Name="cboUser" Width="130" Style="{StaticResource FilterComboBoxStyle}"
                                  SelectionChanged="Filter_Changed">
                            <ComboBoxItem Content="-- Wszyscy --" IsSelected="True"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Szukaj dostawcy:" Style="{StaticResource FilterLabelStyle}"/>
                        <TextBox x:Name="txtSearchSupplier" Width="180" Style="{StaticResource FilterTextBoxStyle}"
                                 TextChanged="Filter_Changed"/>
                    </StackPanel>

                    <StackPanel VerticalAlignment="Bottom" Margin="10,0,0,0">
                        <Button Content="Filtruj" Click="BtnFilter_Click" Style="{StaticResource FilterButtonStyle}" Width="80"/>
                    </StackPanel>

                    <StackPanel VerticalAlignment="Bottom" Margin="5,0,0,0">
                        <Button Content="Wyczyść" Click="BtnClearFilters_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Rząd 2: Szybkie filtry i eksport -->
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBlock Text="Szybki filtr:" VerticalAlignment="Center" Margin="0,0,10,0"
                               FontWeight="Medium" Foreground="#616616"/>
                    <Button Content="Dzisiaj" Click="BtnToday_Click" Style="{StaticResource SecondaryButtonStyle}" Padding="10,5"/>
                    <Button Content="Ostatnie 7 dni" Click="BtnLast7Days_Click" Style="{StaticResource SecondaryButtonStyle}" Padding="10,5"/>
                    <Button Content="Ten miesiąc" Click="BtnThisMonth_Click" Style="{StaticResource SecondaryButtonStyle}" Padding="10,5"/>
                    <Button Content="Wszystko" Click="BtnAll_Click" Style="{StaticResource SecondaryButtonStyle}" Padding="10,5"/>

                    <Separator Margin="20,0" Width="1" Background="#E0E0E0"/>

                    <Button Content="Eksport CSV" Click="BtnExportCsv_Click" Style="{StaticResource ExportButtonStyle}"/>
                    <Button Content="Kopiuj" Click="BtnCopy_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid z historią -->
        <DataGrid x:Name="dgHistory" Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  CanUserSortColumns="True"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#FAFAFA"
                  RowBackground="White"
                  BorderBrush="#E0E0E0"
                  BorderThickness="0"
                  Margin="10">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#ECEFF1"/>
                    <Setter Property="Foreground" Value="#37474F"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="BorderBrush" Value="#CFD8DC"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E3F2FD"/>
                            <Setter Property="Foreground" Value="#1565C0"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Data zmiany" Binding="{Binding ChangeDate, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss}'}" Width="145"/>
                <DataGridTextColumn Header="LP" Binding="{Binding Nr}" Width="50"/>
                <DataGridTextColumn Header="Nr auta" Binding="{Binding CarID}" Width="100"/>
                <DataGridTextColumn Header="Dostawca" Binding="{Binding Dostawca}" Width="180"/>
                <DataGridTextColumn Header="Pole" Binding="{Binding FieldDisplayName}" Width="120"/>
                <DataGridTemplateColumn Header="Zmiana" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding OldValue}" Foreground="#D32F2F" FontWeight="Medium"/>
                                <TextBlock Text=" → " Foreground="#757575"/>
                                <TextBlock Text="{Binding NewValue}" Foreground="#388E3C" FontWeight="Medium"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Użytkownik" Binding="{Binding ChangedBy}" Width="100"/>
                <DataGridTextColumn Header="UserID" Binding="{Binding UserID}" Width="70"/>
                <DataGridTextColumn Header="Data specyfikacji" Binding="{Binding CalcDate, StringFormat='{}{0:dd.MM.yyyy}'}" Width="110"/>
                <DataGridTextColumn Header="ID" Binding="{Binding FarmerCalcID}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pasek statusu -->
        <Border Grid.Row="3" Background="#ECEFF1" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="txtStatus" Text="Gotowe" VerticalAlignment="Center" Foreground="#616161"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0">
                    <TextBlock Text="Wyświetlono: " Foreground="#757575"/>
                    <TextBlock x:Name="txtDisplayed" Text="0" FontWeight="Bold" Foreground="#1976D2"/>
                    <TextBlock Text=" z " Foreground="#757575"/>
                    <TextBlock x:Name="txtTotal" Text="0" FontWeight="Bold" Foreground="#1976D2"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="20,0">
                    <TextBlock Text="Zaznaczono: " Foreground="#757575"/>
                    <TextBlock x:Name="txtSelected" Text="0" FontWeight="Bold" Foreground="#1976D2"/>
                </StackPanel>

                <Button Grid.Column="3" Content="Zamknij" Click="BtnClose_Click"
                        Style="{StaticResource SecondaryButtonStyle}" Width="80"/>
            </Grid>
        </Border>
    </Grid>
</Window>
