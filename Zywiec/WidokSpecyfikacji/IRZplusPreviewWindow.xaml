<Window x:Class="Kalendarz1.IRZplusPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IRZplus - Podglad i wyslanie zgloszenia"
        WindowState="Maximized"
        MinHeight="700" MinWidth="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SendButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#388E3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="25,12"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2E7D32"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1B5E20"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#757575"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#616161"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WarningButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F57C00"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EF6C00"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl dla przycisków toolbara z animacją hover (jak w Specyfikacjach) -->
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                 To="1.05" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                 To="1.05" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                 To="1" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                 To="1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Naglowek -->
        <Border Grid.Row="0" Background="#1976D2" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="IRZplus - Zgloszenie uboju drobiu"
                               FontSize="22" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="txtSubtitle" Text="Podglad danych przed wyslaniem do ARiMR"
                               FontSize="13" Foreground="#BBDEFB" Margin="0,3,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border x:Name="borderEnv" Background="#FF9800" CornerRadius="3" Padding="10,5" Margin="0,0,10,0">
                        <TextBlock x:Name="txtEnvironment" Text="SRODOWISKO TESTOWE" FontSize="11" FontWeight="Bold" Foreground="White"/>
                    </Border>
                    <Button Content="Ustawienia" Click="BtnSettings_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Toolbar z przyciskami akcji -->
        <Border Grid.Row="1" Background="#ECEFF1" Padding="8,6" BorderBrush="#CFD8DC" BorderThickness="0,0,0,1" Margin="10,5,10,0">
            <StackPanel Orientation="Horizontal">
                <!-- Przyciski akcji po lewej stronie (styl jak w Specyfikacjach) -->
                <Button Content="Zapisz dane IRZ" Click="BtnSaveIRZFields_Click"
                        Style="{StaticResource ToolbarButton}" Background="#1976D2"
                        ToolTip="Zapisz nr.dok.Arimr, przybycie i padniecia do bazy"/>
                <Button Content="Zapisz lokalna kopie" Click="BtnSaveLocal_Click"
                        Style="{StaticResource ToolbarButton}" Background="#F57C00"/>
                <Button x:Name="btnExportDialog" Content="Eksport (XML/CSV)" Click="BtnExportDialog_Click"
                        Style="{StaticResource ToolbarButton}" Background="#FF5722"
                        ToolTip="Eksportuj dane do pliku XML lub CSV"/>
                <Button x:Name="btnSend" Content="Wyslij do IRZplus" Click="BtnSend_Click"
                        Style="{StaticResource ToolbarButton}" Background="#388E3C"/>

                <Border Width="1" Background="#B0BEC5" Margin="8,2"/>

                <Button Content="Zaznacz wszystkie" Click="BtnSelectAll_Click"
                        Style="{StaticResource ToolbarButton}" Background="#607D8B"/>
                <Button Content="Odznacz wszystkie" Click="BtnDeselectAll_Click"
                        Style="{StaticResource ToolbarButton}" Background="#607D8B"/>
                <Button Content="Odswiez" Click="BtnRefresh_Click"
                        Style="{StaticResource ToolbarButton}" Background="#2196F3"/>

                <Border Width="1" Background="#B0BEC5" Margin="8,2"/>

                <Button Content="Anuluj" Click="BtnCancel_Click"
                        Style="{StaticResource ToolbarButton}" Background="#757575"/>
            </StackPanel>
        </Border>

        <!-- DataGrid ze specyfikacjami -->
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="2" Margin="10">
        <DataGrid x:Name="dgSpecyfikacje"
                  AutoGenerateColumns="False"
                  IsReadOnly="False"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  CanUserSortColumns="True"
                  GridLinesVisibility="All"
                  HorizontalGridLinesBrush="Black"
                  VerticalGridLinesBrush="Black"
                  AlternatingRowBackground="#F5F5F5"
                  RowBackground="White"
                  BorderThickness="0"
                  FontSize="12"
                  RowHeight="28"
                  ColumnHeaderHeight="32">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#37474F"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontSize" Value="11"/>
                    <Setter Property="Padding" Value="6,4"/>
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="4,2"/>
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#BBDEFB"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <!-- Kolumna wyboru -->
                <DataGridCheckBoxColumn Header="" Binding="{Binding Wybrana, UpdateSourceTrigger=PropertyChanged}" Width="30"/>
                <!-- A: Kolejnosc auta -->
                <DataGridTextColumn Header="Nr" Binding="{Binding KolejnoscAuta}" Width="30" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="#1976D2"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <!-- B: Hodowca -->
                <DataGridTextColumn Header="Hodowca" Binding="{Binding Hodowca}" Width="130" IsReadOnly="True"/>
                <!-- C: Id na produkcji -->
                <DataGridTextColumn Header="Id" Binding="{Binding IdHodowcy}" Width="45" IsReadOnly="True"/>
                <!-- D: IRZ PLUS - edytowalne z mozliwoscia zapisu do bazy -->
                <DataGridTemplateColumn Header="IRZ PLUS" Width="130">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="30"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"
                                         Text="{Binding IRZPlus, UpdateSourceTrigger=PropertyChanged}"
                                         FontWeight="Bold"
                                         Foreground="#1565C0"
                                         BorderThickness="1"
                                         BorderBrush="#BBDEFB"
                                         Padding="4,2"
                                         VerticalContentAlignment="Center"/>
                                <Button Grid.Column="1"
                                        Content="&#x2714;"
                                        ToolTip="Zapisz IRZPlus do bazy dla tego hodowcy"
                                        Click="BtnSaveIRZPlus_Click"
                                        Tag="{Binding}"
                                        Background="#4CAF50"
                                        Foreground="White"
                                        FontWeight="Bold"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Width="28"
                                        Margin="2,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <!-- E: Numer Partii z przyciskiem Kopiuj -->
                <DataGridTemplateColumn Header="Numer Partii" Width="140" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding NumerPartii}" VerticalAlignment="Center" Margin="2,0"/>
                                <Button x:Name="btnKopiuj" Grid.Column="1" Content="Kopiuj"
                                        Click="BtnKopiujPartie_Click"
                                        Tag="{Binding NumerPartii}"
                                        Background="#E3F2FD"
                                        Foreground="#1976D2"
                                        FontSize="10"
                                        Padding="6,2"
                                        Margin="4,0"
                                        Cursor="Hand"
                                        ToolTip="Kopiuj numer partii do schowka">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Setter Property="BorderBrush" Value="#1976D2"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#1976D2"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <!-- F: Liczba Sztuk Drobiu (zdatne) -->
                <DataGridTextColumn Header="Szt" Binding="{Binding LiczbaSztukDrobiu, StringFormat=N0}" Width="60" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <!-- KG Do zapl. (PayWgt) -->
                <DataGridTextColumn Header="KG" Binding="{Binding KgDoZaplaty, StringFormat=N0}" Width="70" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#1976D2"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <!-- Szt Konfiskat -->
                <DataGridTextColumn Header="Konf" Binding="{Binding KgKonfiskat, StringFormat=N0}" Width="45" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#FF9800"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <!-- Szt Padlych -->
                <DataGridTextColumn Header="Padl" Binding="{Binding KgPadlych, StringFormat=N0}" Width="45" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Foreground" Value="#D32F2F"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <!-- G: Typ Zdarzenia -->
                <DataGridTextColumn Header="Typ Zdarzenia" Binding="{Binding TypZdarzenia}" Width="140" IsReadOnly="True"/>
                <!-- H: Data zdarzenia -->
                <DataGridTextColumn Header="Data" Binding="{Binding DataZdarzenia, StringFormat='{}{0:dd-MM-yyyy}'}" Width="80" IsReadOnly="True"/>
                <!-- I: Kraj Wywozu -->
                <DataGridTextColumn Header="Kraj" Binding="{Binding KrajWywozu}" Width="40" IsReadOnly="True"/>
                <!-- J: Przyjete z dzialalnosci (obliczane) -->
                <DataGridTextColumn Header="Przyjete z dzial." Binding="{Binding PrzyjetaZDzialalnosci}" Width="120" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#388E3C"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <!-- K: nr.dok.Arimr (edytowalne) -->
                <DataGridTextColumn Header="nr.dok.Arimr" Binding="{Binding NrDokArimr, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                <!-- L: przybycie (edytowalne) -->
                <DataGridTextColumn Header="przybycie" Binding="{Binding Przybycie, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                <!-- M: padniecia (edytowalne) -->
                <DataGridTextColumn Header="padniecia" Binding="{Binding Padniecia, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
        </Border>

        <!-- Panel walidacji/ostrzezen -->
        <Border x:Name="borderWarnings" Grid.Row="3" Background="#FFF3E0" Padding="15" Margin="10,0,10,10"
                BorderBrush="#FFB74D" BorderThickness="1" CornerRadius="4" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="Ostrzezenia:" FontWeight="Bold" Foreground="#E65100" Margin="0,0,0,5"/>
                <TextBlock x:Name="txtWarnings" TextWrapping="Wrap" Foreground="#E65100"/>
            </StackPanel>
        </Border>

        <!-- Panel statusu i podsumowania (sumy na dole) -->
        <Border Grid.Row="4" Background="#ECEFF1" Padding="8,5" BorderBrush="#B0BEC5" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Status i progress bar -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="txtStatus" Text="Gotowe do wyslania" VerticalAlignment="Center" FontSize="12" Foreground="#616161"/>
                    <ProgressBar x:Name="progressBar" Width="150" Height="16" Margin="15,0,0,0" Visibility="Collapsed"/>
                </StackPanel>

                <Border Grid.Column="1" Width="1" Background="#B0BEC5" Margin="15,2"/>

                <!-- Podsumowania (sumy) w kompaktowej formie -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Data:" FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,3,0"/>
                    <TextBlock x:Name="txtDataUboju" Text="-" FontSize="12" FontWeight="Bold" Foreground="#1976D2" VerticalAlignment="Center" Margin="0,0,12,0"/>

                    <TextBlock Text="Dostawcy:" FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,3,0"/>
                    <TextBlock x:Name="txtLiczbaDostawcow" Text="0" FontSize="12" FontWeight="Bold" Foreground="#424242" VerticalAlignment="Center" Margin="0,0,12,0"/>

                    <TextBlock Text="Szt:" FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,3,0"/>
                    <TextBlock x:Name="txtSumaSztuk" Text="0" FontSize="12" FontWeight="Bold" Foreground="#424242" VerticalAlignment="Center" Margin="0,0,12,0"/>

                    <TextBlock Text="Waga:" FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,3,0"/>
                    <TextBlock x:Name="txtSumaWagi" Text="0 kg" FontSize="12" FontWeight="Bold" Foreground="#424242" VerticalAlignment="Center" Margin="0,0,12,0"/>

                    <TextBlock Text="Padle:" FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,3,0"/>
                    <TextBlock x:Name="txtSumaPadlych" Text="0" FontSize="12" FontWeight="Bold" Foreground="#D32F2F" VerticalAlignment="Center" Margin="0,0,12,0"/>

                    <TextBlock Text="KG zapl:" FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,3,0"/>
                    <TextBlock x:Name="txtSumaKgDoZapl" Text="0" FontSize="12" FontWeight="Bold" Foreground="#1976D2" VerticalAlignment="Center" Margin="0,0,12,0"/>

                    <TextBlock Text="Konfi:" FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,3,0"/>
                    <TextBlock x:Name="txtSumaKgKonfiskat" Text="0" FontSize="12" FontWeight="Bold" Foreground="#FF9800" VerticalAlignment="Center" Margin="0,0,12,0"/>

                    <TextBlock Text="Padle szt:" FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,3,0"/>
                    <TextBlock x:Name="txtSumaKgPadlych" Text="0" FontSize="12" FontWeight="Bold" Foreground="#D32F2F" VerticalAlignment="Center" Margin="0,0,12,0"/>

                    <TextBlock Text="Padle+Konf:" FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,3,0"/>
                    <TextBlock x:Name="txtSumaPadleKonfiskaty" Text="0" FontSize="12" FontWeight="Bold" Foreground="#9C27B0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Konfetti Canvas (overlay na calym oknie) -->
        <Canvas x:Name="confettiCanvas" Grid.Row="0" Grid.RowSpan="5" IsHitTestVisible="False"/>
    </Grid>
</Window>
