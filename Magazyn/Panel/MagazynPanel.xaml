<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Kalendarz1.MagazynPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Panel Magazynier" 
        WindowState="Maximized" 
        WindowStyle="None"
        Background="#1C1E26"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Style for the top buttons - made larger for touch screens -->
        <Style x:Key="TopButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="70"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#787878"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Property=Background}"
                                BorderBrush="{TemplateBinding Property=BorderBrush}"
                                BorderThickness="{TemplateBinding Property=BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#969696"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- General Style for DataGrids -->
        <Style x:Key="CustomDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#2D2F3E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowBackground" Value="#373946"/>
            <Setter Property="AlternatingRowBackground" Value="#32343F"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#464858"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Grid Background="{TemplateBinding Property=Background}">
                            <ContentPresenter VerticalAlignment="Center" 
                                              Margin="5,2"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- Style for TabControl Tabs - made larger for touch -->
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#282A32"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="25,15"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="{TemplateBinding Property=Background}" 
                                BorderThickness="0" CornerRadius="8,8,0,0" Margin="2,0">
                            <ContentPresenter x:Name="ContentSite" 
                                            ContentSource="Header"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Property=Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1C1E26"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- PRZYCISKI TOWARÓW - NAJWYŻSZY RZĄD -->
        <Border Grid.Row="0" Background="#1A3A5A" Padding="8">
            <WrapPanel x:Name="pnlProductButtons" Orientation="Horizontal" HorizontalAlignment="Center">
                <!-- Przyciski towarów zostaną dodane dynamicznie -->
            </WrapPanel>
        </Border>

        <!-- TOP BAR -->
        <Border Grid.Row="1" Background="#24262F" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Data po lewej stronie -->
                <TextBlock x:Name="lblData" Grid.Column="0" Text="2025-10-23 Środa"
                           Foreground="White" FontSize="32" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0,0,40,0"/>

                <!-- Navigation buttons w środku -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="btnPrev" Content="◀ Wczoraj" Style="{StaticResource TopButtonStyle}"
                            Background="#3A5A7A" Click="btnPrev_Click" Margin="0,0,10,0"/>
                    <Button x:Name="btnToday" Content="📅 Dziś" Style="{StaticResource TopButtonStyle}"
                            Background="#4A7A5A" Click="btnToday_Click" Margin="0,0,10,0"/>
                    <Button x:Name="btnNext" Content="Jutro ▶" Style="{StaticResource TopButtonStyle}"
                            Background="#3A5A7A" Click="btnNext_Click" Margin="0,0,10,0"/>
                    <Button x:Name="btnRefresh" Content="🔄 Odśwież" Style="{StaticResource TopButtonStyle}"
                            Background="#5A6A7A" Click="btnRefresh_Click" Margin="20,0,0,0"/>
                </StackPanel>

                <!-- Checkbox i przycisk zamknij po prawej -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox x:Name="chkShowShipmentsOnly"
                              Content="Pokaż wydania bez zamówień"
                              Foreground="White"
                              FontSize="16"
                              FontWeight="SemiBold"
                              VerticalAlignment="Center"
                              Margin="0,0,20,0"
                              Checked="chkShowShipmentsOnly_CheckedChanged"
                              Unchecked="chkShowShipmentsOnly_CheckedChanged"/>

                    <Button x:Name="btnClose" Content="❌ Zamknij" Style="{StaticResource TopButtonStyle}"
                            Background="#8A3A3A" Click="btnClose_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- MAIN CONTENT AREA -->
        <TabControl Grid.Row="2" Background="#1C1E26" BorderThickness="0">
            <!-- TAB 1: ZAMÓWIENIA DO WYDANIA -->
            <TabItem Header="📦 Zamówienia">
                <Grid Background="#1C1E26">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <!-- LEFT SIDE: Pierwsza kolumna zamówień -->
                    <Border Grid.Column="0" Background="#262832" CornerRadius="5" Margin="5,5,0,5">
                        <DataGrid x:Name="dgvZamowienia1" 
                                  ItemsSource="{Binding ZamowieniaList1}" 
                                  Style="{StaticResource CustomDataGridStyle}" 
                                  SelectionChanged="dgvZamowienia_SelectionChanged" 
                                  IsReadOnly="True" 
                                  RowHeight="50" 
                                  Margin="5">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Wydany">
                                            <Setter Property="BorderBrush" Value="LimeGreen"/>
                                            <Setter Property="BorderThickness" Value="3"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Klient" Width="2*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Klient}" Foreground="{Binding KlientColor}" FontWeight="SemiBold"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Kg" Binding="{Binding TotalIlosc, StringFormat=N0}" Width="0.6*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Margin" Value="0,0,5,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Wyjazd" Binding="{Binding CzasWyjazdDisplay}" Width="1*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="Orange"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Pojazd" Binding="{Binding PojazdDisplay}" Width="0.9*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="LightSkyBlue"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="1.1*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{Binding StatusColor}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="FontSize" Value="12"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- MIDDLE: Druga kolumna zamówień -->
                    <Border Grid.Column="1" Background="#22242E" CornerRadius="5" Margin="5,5,0,5">
                        <DataGrid x:Name="dgvZamowienia2"
                                  ItemsSource="{Binding ZamowieniaList2}"
                                  Style="{StaticResource CustomDataGridStyle}"
                                  SelectionChanged="dgvZamowienia_SelectionChanged"
                                  IsReadOnly="True"
                                  RowHeight="50"
                                  Margin="5">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Wydany">
                                            <Setter Property="BorderBrush" Value="LimeGreen"/>
                                            <Setter Property="BorderThickness" Value="3"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Klient" Width="2*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Klient}" Foreground="{Binding KlientColor}" FontWeight="SemiBold"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Kg" Binding="{Binding TotalIlosc, StringFormat=N0}" Width="0.6*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Margin" Value="0,0,5,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Wyjazd" Binding="{Binding CzasWyjazdDisplay}" Width="1*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="Orange"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Pojazd" Binding="{Binding PojazdDisplay}" Width="0.9*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="LightSkyBlue"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="1.1*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{Binding StatusColor}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="FontSize" Value="12"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- RIGHT SIDE: Szczegóły zamówienia -->
                    <Grid Grid.Column="2" Margin="5,5,5,5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="60*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20*"/>
                        </Grid.RowDefinitions>

                        <!-- Informacje o kliencie i transporcie -->
                        <Border Grid.Row="0" Background="#2A2C38" CornerRadius="5" Padding="15" Margin="0,0,0,5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="🏢 Klient:" Foreground="LightGray" FontSize="16" Margin="0,5"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="txtKlient" Text="--" Foreground="White" FontSize="16" FontWeight="Bold" Margin="10,5"/>

                                <TextBlock Grid.Row="0" Grid.Column="2" Text="💼 Handlowiec:" Foreground="LightGray" FontSize="16" Margin="20,5,0,5"/>
                                <TextBlock Grid.Row="0" Grid.Column="3" x:Name="txtHandlowiec" Text="--" Foreground="#90CAF9" FontSize="16" FontWeight="Bold" Margin="10,5"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="⏰ Wyjazd:" Foreground="LightGray" FontSize="16" Margin="0,5"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="txtCzasWyjazdu" Text="--" Foreground="Orange" FontSize="16" FontWeight="Bold" Margin="10,5"/>

                                <TextBlock Grid.Row="1" Grid.Column="2" Text="🚗 Pojazd:" Foreground="LightGray" FontSize="16" Margin="20,5,0,5"/>
                                <TextBlock Grid.Row="1" Grid.Column="3" x:Name="txtPojazd" Text="--" Foreground="LightSkyBlue" FontSize="16" FontWeight="Bold" Margin="10,5"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="👤 Kierowca:" Foreground="LightGray" FontSize="16" Margin="0,5"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" x:Name="txtKierowca" Text="--" Foreground="White" FontSize="16" FontWeight="Bold" Margin="10,5"/>

                                <!-- Informacja o wydaniu -->
                                <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4"
                                           x:Name="txtShipmentInfo"
                                           Text=""
                                           Foreground="LimeGreen"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           Margin="0,5,0,5"/>

                                <!-- Przyciski do zmiany statusu -->
                                <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4"
                                        x:Name="btnMarkAsShipped" 
                                        Content="✅ Oznacz zamówienie jako WYDANE" 
                                        Click="btnMarkAsShipped_Click"
                                        Background="#2A7A3A" 
                                        Foreground="White" 
                                        FontSize="18" 
                                        FontWeight="Bold"
                                        Height="50" 
                                        Margin="0,5,0,0"
                                        Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="8" 
                                                                BorderBrush="LimeGreen" 
                                                                BorderThickness="2">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#3A9A4A"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4"
                                        x:Name="btnUndoShipment"
                                        Content="↩️ COFNIJ - Przywróć status 'Nowe'"
                                        Click="btnUndoShipment_Click"
                                        Background="#8A3A3A"
                                        Foreground="White"
                                        FontSize="18"
                                        FontWeight="Bold"
                                        Height="50"
                                        Margin="0,5,0,0"
                                        Cursor="Hand"
                                        Visibility="Collapsed">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="8"
                                                                BorderBrush="OrangeRed"
                                                                BorderThickness="2">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#AA4A4A"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Przycisk przyjęcia zmiany -->
                                <Button Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="4"
                                        x:Name="btnAcceptChange"
                                        Content="✅ Przyjmuję zmianę - zaktualizuj snapshot"
                                        Click="btnAcceptChange_Click"
                                        Background="#B45309"
                                        Foreground="White"
                                        FontSize="14"
                                        FontWeight="Bold"
                                        Height="40"
                                        Margin="0,5,0,0"
                                        Cursor="Hand"
                                        Visibility="Collapsed">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="8"
                                                                BorderBrush="Orange"
                                                                BorderThickness="2">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#D97706"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- Lista towarów do wydania -->
                        <Border Grid.Row="1" Background="#262832" CornerRadius="5" Margin="0,5">
                            <DataGrid x:Name="dgvPozycje"
                                      Style="{StaticResource CustomDataGridStyle}"
                                      IsReadOnly="True"
                                      RowHeight="32"
                                      Margin="5">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Produkt" Width="2*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="{Binding ProduktImg}" Width="24" Height="24" Margin="2,0,4,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="{Binding Produkt}" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Zamówiono (kg)" Binding="{Binding [Zamówiono (kg)], StringFormat=N0}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Margin" Value="0,0,5,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Wydano (kg)" Binding="{Binding [Wydano (kg)], StringFormat=N0}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Margin" Value="0,0,5,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Różnica (kg)" Binding="{Binding [Różnica (kg)], StringFormat=N0}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Margin" Value="0,0,5,0"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="Zmiana" Binding="{Binding Zmiana}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="Foreground" Value="Orange"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridCheckBoxColumn Header="Halal" Binding="{Binding Halal}" Width="45" IsReadOnly="True">
                                        <DataGridCheckBoxColumn.ElementStyle>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="IsHitTestVisible" Value="False"/>
                                            </Style>
                                        </DataGridCheckBoxColumn.ElementStyle>
                                    </DataGridCheckBoxColumn>
                                    <DataGridCheckBoxColumn Header="Folia" Binding="{Binding Folia}" Width="45" IsReadOnly="True">
                                        <DataGridCheckBoxColumn.ElementStyle>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="IsHitTestVisible" Value="False"/>
                                            </Style>
                                        </DataGridCheckBoxColumn.ElementStyle>
                                    </DataGridCheckBoxColumn>
                                    <DataGridCheckBoxColumn Header="E2" Binding="{Binding E2}" Width="35" IsReadOnly="True">
                                        <DataGridCheckBoxColumn.ElementStyle>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="IsHitTestVisible" Value="False"/>
                                            </Style>
                                        </DataGridCheckBoxColumn.ElementStyle>
                                    </DataGridCheckBoxColumn>
                                    <DataGridCheckBoxColumn Header="Strefa" Binding="{Binding Strefa}" Width="50" IsReadOnly="True">
                                        <DataGridCheckBoxColumn.ElementStyle>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="IsHitTestVisible" Value="False"/>
                                            </Style>
                                        </DataGridCheckBoxColumn.ElementStyle>
                                    </DataGridCheckBoxColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                        <!-- Legenda ikon -->
                        <StackPanel Orientation="Horizontal" Margin="5,2">
                            <TextBlock Text="Legenda:" Foreground="#8899AA" FontSize="10" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="&#x26A0;&#xFE0F; Strefa" Foreground="#FF6B6B" FontSize="10" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="&#x1F52A; Halal" Foreground="#4DB6AC" FontSize="10" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="&#x1F39E;&#xFE0F; Folia" Foreground="#81C784" FontSize="10" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="&#x1F4E6; E2" Foreground="#64B5F6" FontSize="10" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="&#x1F69A; Transport" Foreground="#FFB74D" FontSize="10" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Uwagi do zamówienia -->
                        <TextBlock Grid.Row="2" Text="Notatka zamówienia" Background="#323440" Foreground="Yellow" FontWeight="Bold" FontSize="14" Padding="5"/>

                        <Border Grid.Row="3" Background="#343642" CornerRadius="5" Margin="0,5,0,0">
                            <TextBox x:Name="txtUwagi" Background="Transparent"
                                     Foreground="White" BorderThickness="0"
                                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                     FontSize="28" Margin="5" IsReadOnly="True"/>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- TAB 2: HISTORIA WYDAŃ -->
            <TabItem Header="📋 Historia wydań">
                <Grid Background="#1C1E26">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Nagłówek z filtrem dat -->
                    <Border Grid.Row="0" Background="#262832" Padding="10" Margin="5">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="Zakres dat:" Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <DatePicker x:Name="dpHistoriaOd" Width="130" Background="#3A3C48" Foreground="White"/>
                            <TextBlock Text=" — " Foreground="White" FontSize="14" VerticalAlignment="Center"/>
                            <DatePicker x:Name="dpHistoriaDo" Width="130" Background="#3A3C48" Foreground="White"/>
                            <Button x:Name="btnLoadHistoria" Content="🔍 Pokaż"
                                    Style="{StaticResource TopButtonStyle}"
                                    Background="#4A7A5A"
                                    Margin="15,0,0,0"
                                    Click="btnLoadHistoria_Click"/>
                            <Button x:Name="btnHistoriaToday" Content="📅 Dziś"
                                    Style="{StaticResource TopButtonStyle}"
                                    Background="#3A5A7A"
                                    Margin="10,0,0,0"
                                    Click="btnHistoriaToday_Click"/>
                            <Button x:Name="btnHistoriaWeek" Content="📆 Tydzień"
                                    Style="{StaticResource TopButtonStyle}"
                                    Background="#3A5A7A"
                                    Margin="10,0,0,0"
                                    Click="btnHistoriaWeek_Click"/>
                            <Button x:Name="btnHistoriaMonth" Content="📆 Miesiąc"
                                    Style="{StaticResource TopButtonStyle}"
                                    Background="#3A5A7A"
                                    Margin="10,0,0,0"
                                    Click="btnHistoriaMonth_Click"/>
                        </StackPanel>
                    </Border>

                    <!-- Filtry dodatkowe -->
                    <Border Grid.Row="1" Background="#2A2C38" Padding="10" Margin="5,0,5,5">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="Kto wydał:" Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox x:Name="cmbHistoriaUzytkownik" Width="180" Height="35" FontSize="14"
                                      SelectionChanged="cmbHistoriaFilter_SelectionChanged"/>

                            <TextBlock Text="Klient:" Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="25,0,8,0"/>
                            <ComboBox x:Name="cmbHistoriaKlient" Width="200" Height="35" FontSize="14"
                                      SelectionChanged="cmbHistoriaFilter_SelectionChanged"/>

                            <TextBlock Text="Status:" Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="25,0,8,0"/>
                            <ComboBox x:Name="cmbHistoriaStatus" Width="140" Height="35" FontSize="14"
                                      SelectionChanged="cmbHistoriaFilter_SelectionChanged">
                                <ComboBoxItem Content="Wszystkie" IsSelected="True"/>
                                <ComboBoxItem Content="✅ Pełne"/>
                                <ComboBoxItem Content="⚠️ Z różnicami"/>
                            </ComboBox>

                            <Button x:Name="btnHistoriaClearFilters" Content="✖ Wyczyść filtry"
                                    Style="{StaticResource TopButtonStyle}"
                                    Background="#5A4A4A"
                                    Margin="25,0,0,0"
                                    Click="btnHistoriaClearFilters_Click"/>
                        </StackPanel>
                    </Border>

                    <!-- Tabela historii wydań -->
                    <Border Grid.Row="2" Background="#262832" CornerRadius="5" Margin="5">
                        <DataGrid x:Name="dgvHistoriaWydan"
                                  Style="{StaticResource CustomDataGridStyle}"
                                  IsReadOnly="True"
                                  RowHeight="45"
                                  Margin="5"
                                  AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Data wydania" Binding="{Binding DataWydania, StringFormat=yyyy-MM-dd HH:mm}" Width="140">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#90CAF9"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Klient" Binding="{Binding Klient}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Ilość (kg)" Binding="{Binding IloscKg, StringFormat=N0}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Margin" Value="0,0,10,0"/>
                                            <Setter Property="Foreground" Value="#FFD700"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Kto wydał" Binding="{Binding KtoWydal}" Width="150">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#81C784"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Status" Binding="{Binding StatusWydania}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Uwagi" Binding="{Binding Uwagi}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#B0BEC5"/>
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- Podsumowanie -->
                    <Border Grid.Row="3" Background="#1E3A2A" Padding="15" Margin="5">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="Łącznie wydań: " Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock x:Name="lblHistoriaCount" Text="0" Foreground="#FFD700" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Text="  |  Suma kg: " Foreground="White" FontSize="16" VerticalAlignment="Center" Margin="20,0,0,0"/>
                            <TextBlock x:Name="lblHistoriaSumaKg" Text="0" Foreground="#4CAF50" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Text="  |  Pełne wydania: " Foreground="White" FontSize="16" VerticalAlignment="Center" Margin="20,0,0,0"/>
                            <TextBlock x:Name="lblHistoriaPelne" Text="0" Foreground="#81C784" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Text="  |  Z różnicami: " Foreground="White" FontSize="16" VerticalAlignment="Center" Margin="20,0,0,0"/>
                            <TextBlock x:Name="lblHistoriaRoznice" Text="0" Foreground="#FFCC80" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
