<Window x:Class="Kalendarz1.Spotkania.Views.PodgladSpotkania"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Szczegóły spotkania" Height="650" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <Border x:Name="HeaderBorder" Grid.Row="0" Background="#1976D2" Padding="25,20">
            <StackPanel>
                <TextBlock x:Name="TxtTytul" Text="Tytuł spotkania" FontSize="22" FontWeight="Bold"
                           Foreground="White" TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock x:Name="TxtStatus" Text="Zaplanowane" FontSize="14" Foreground="White"
                               Padding="10,4" Background="#FFFFFF30"/>
                    <TextBlock x:Name="TxtTyp" Text="Zespół" FontSize="14" Foreground="White"
                               Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Zawartość -->
        <ScrollViewer Grid.Row="1" Padding="25">
            <StackPanel>
                <!-- Data i czas -->
                <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="📅" FontSize="40" VerticalAlignment="Center"
                                   Margin="0,0,20,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtData" Text="15 stycznia 2026" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TxtCzas" Text="10:00 - 11:00 (1 godzina)" FontSize="14"
                                       Foreground="#666" Margin="0,4,0,0"/>
                            <TextBlock x:Name="TxtCzasDoSpotkania" Text="Za 2 dni" FontSize="13"
                                       Foreground="#1976D2" FontWeight="Medium" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Lokalizacja / Link -->
                <Border x:Name="PanelLokalizacja" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📍 Lokalizacja / Link" FontSize="14" FontWeight="SemiBold"
                                   Foreground="#666" Margin="0,0,0,10"/>
                        <TextBlock x:Name="TxtLokalizacja" Text="Sala konferencyjna A" FontSize="15"/>
                        <TextBlock x:Name="TxtLink" Text="" Foreground="#1976D2" Cursor="Hand"
                                   MouseDown="TxtLink_MouseDown" TextDecorations="Underline" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Kontrahent -->
                <Border x:Name="PanelKontrahent" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="LblKontrahent" Text="🏢 Odbiorca" FontSize="14" FontWeight="SemiBold"
                                   Foreground="#666" Margin="0,0,0,10"/>
                        <TextBlock x:Name="TxtKontrahent" Text="Nazwa kontrahenta" FontSize="15"/>
                    </StackPanel>
                </Border>

                <!-- Opis -->
                <Border x:Name="PanelOpis" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📝 Opis / Agenda" FontSize="14" FontWeight="SemiBold"
                                   Foreground="#666" Margin="0,0,0,10"/>
                        <TextBlock x:Name="TxtOpis" Text="Brak opisu" FontSize="14" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Uczestnicy -->
                <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="👥 Uczestnicy" FontSize="14" FontWeight="SemiBold"
                                   Foreground="#666" Margin="0,0,0,10"/>
                        <ItemsControl x:Name="ListaUczestnikow">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F5F5F5" CornerRadius="6" Padding="12,8" Margin="0,4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding OperatorNazwa}" FontSize="14"/>
                                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                <TextBlock Text="{Binding IkonaStatusu}" FontSize="14" Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding StatusDisplay}" FontSize="12" Foreground="#666"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Organizator -->
                <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="👤 Organizator" FontSize="14" FontWeight="SemiBold"
                                   Foreground="#666" Margin="0,0,0,10"/>
                        <TextBlock x:Name="TxtOrganizator" Text="Jan Kowalski" FontSize="14"/>
                    </StackPanel>
                </Border>

                <!-- Powiązania -->
                <Border x:Name="PanelPowiazania" Background="White" CornerRadius="8" Padding="20" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="🔗 Powiązania" FontSize="14" FontWeight="SemiBold"
                                   Foreground="#666" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="BtnNotatka" Content="📝 Otwórz notatkę" Padding="15,8" Margin="0,0,10,0"
                                    Visibility="Collapsed" Click="BtnNotatka_Click"/>
                            <Button x:Name="BtnTranskrypcja" Content="🎙️ Otwórz transkrypcję" Padding="15,8"
                                    Visibility="Collapsed" Click="BtnTranskrypcja_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Przyciski -->
        <Border Grid.Row="2" Background="White" Padding="20,15" BorderThickness="0,1,0,0" BorderBrush="#E0E0E0">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="BtnAkceptuj" Content="✓ Zaakceptuj zaproszenie" Padding="15,10"
                            Background="#4CAF50" Foreground="White" BorderThickness="0"
                            Visibility="Collapsed" Click="BtnAkceptuj_Click"/>
                    <Button x:Name="BtnOdrzuc" Content="✗ Odrzuć" Padding="15,10"
                            Background="#F44336" Foreground="White" BorderThickness="0"
                            Margin="10,0,0,0" Visibility="Collapsed" Click="BtnOdrzuc_Click"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnEdytuj" Content="✏️ Edytuj" Padding="15,10" Margin="0,0,10,0"
                            Click="BtnEdytuj_Click"/>
                    <Button Content="Zamknij" Padding="15,10" Click="BtnZamknij_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
