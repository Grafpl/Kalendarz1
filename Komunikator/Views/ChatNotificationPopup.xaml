<Window x:Class="Kalendarz1.Komunikator.Views.ChatNotificationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nowa wiadomość"
        Width="340" Height="95"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
            <ThicknessAnimation Storyboard.TargetName="MainBorder" Storyboard.TargetProperty="Margin"
                               From="15,15,0,0" To="0,0,0,0" Duration="0:0:0.2">
                <ThicknessAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.2"/>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="MainBorder" Background="#1F2C33" CornerRadius="12"
            BorderBrush="#00A884" BorderThickness="1" Margin="8" Cursor="Hand"
            MouseLeftButtonUp="Border_MouseLeftButtonUp">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="18" ShadowDepth="4" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="14,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid Width="48" Height="48" Margin="0,0,12,0">
                <Ellipse>
                    <Ellipse.Fill>
                        <ImageBrush x:Name="SenderAvatar" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>
            </Grid>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="SenderName" Foreground="#E9EDEF" FontSize="14" FontWeight="SemiBold"/>
                <TextBlock x:Name="MessagePreview" Foreground="#8696A0" FontSize="13"
                           TextTrimming="CharacterEllipsis" MaxWidth="190" Margin="0,3,0,0"/>
            </StackPanel>

            <Button Grid.Column="2" Content="✕" FontSize="14"
                    Background="Transparent" Foreground="#8696A0"
                    BorderThickness="0" Cursor="Hand" VerticalAlignment="Top"
                    Click="CloseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#E9EDEF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
