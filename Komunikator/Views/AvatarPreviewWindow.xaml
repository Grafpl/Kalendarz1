<Window x:Class="Kalendarz1.Komunikator.Views.AvatarPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Podgląd"
        Width="320" Height="380"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetName="AvatarScale" Storyboard.TargetProperty="ScaleX"
                           From="0.8" To="1" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="AvatarScale" Storyboard.TargetProperty="ScaleY"
                           From="0.8" To="1" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetName="AvatarScale" Storyboard.TargetProperty="ScaleX"
                           From="1" To="0.8" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetName="AvatarScale" Storyboard.TargetProperty="ScaleY"
                           From="1" To="0.8" Duration="0:0:0.15"/>
        </Storyboard>
    </Window.Resources>

    <Border Background="#111B21" CornerRadius="16" Padding="20">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="5" Opacity="0.6"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Avatar -->
            <Grid Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.RenderTransform>
                    <ScaleTransform x:Name="AvatarScale" ScaleX="1" ScaleY="1" CenterX="120" CenterY="120"/>
                </Grid.RenderTransform>

                <!-- Zielona poświata -->
                <Ellipse x:Name="GlowEffect" Width="250" Height="250" Opacity="0">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#25D366" Offset="0.7"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>

                <!-- Avatar border -->
                <Ellipse x:Name="AvatarBorder" Width="240" Height="240" StrokeThickness="3">
                    <Ellipse.Stroke>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#2A3942" Offset="0"/>
                            <GradientStop Color="#1F2C33" Offset="1"/>
                        </LinearGradientBrush>
                    </Ellipse.Stroke>
                    <Ellipse.Fill>
                        <ImageBrush x:Name="AvatarImage" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>

                <!-- Online indicator -->
                <Ellipse x:Name="OnlineIndicator" Width="36" Height="36"
                         HorizontalAlignment="Right" VerticalAlignment="Bottom"
                         Margin="0,0,15,15" Visibility="Collapsed"
                         Stroke="#111B21" StrokeThickness="4">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#4ADE80" Offset="0"/>
                            <GradientStop Color="#25D366" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
            </Grid>

            <!-- Nazwa użytkownika -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,16,0,0">
                <TextBlock x:Name="UserName" Text="" Foreground="#E9EDEF"
                           FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                <StackPanel x:Name="StatusPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,0">
                    <Ellipse x:Name="StatusDot" Width="10" Height="10" Fill="#8696A0" Margin="0,0,8,0"/>
                    <TextBlock x:Name="UserStatus" Text="Offline" Foreground="#8696A0" FontSize="14"/>
                </StackPanel>
            </StackPanel>

            <!-- Przycisk zamknij -->
            <Button Grid.Row="0" Content="✕" FontSize="16"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Background="Transparent" Foreground="#8696A0"
                    BorderThickness="0" Cursor="Hand" Margin="0,-5,-5,0"
                    Width="32" Height="32" Click="CloseButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="Transparent" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2A3942"/>
                                <Setter Property="Foreground" Value="#E9EDEF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>
