<Window x:Class="Kalendarz1.Zamowienia.WyborTowaruWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wybór towaru"
        Width="640" Height="600" MinWidth="460" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        Background="#F4F6F8" ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek + szukajka -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="12,9" Margin="0,0,0,10"
                BorderBrush="#D5DBE0" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍 Wybierz towar" FontSize="15" FontWeight="Bold"
                           Foreground="#2C3E50" VerticalAlignment="Center" Margin="0,0,14,0"/>
                <TextBox Grid.Column="1" x:Name="txtSzukaj" Height="30" FontSize="13"
                         VerticalContentAlignment="Center" TextChanged="TxtSzukaj_TextChanged"
                         ToolTip="Szukaj towaru po nazwie"/>
                <Button Grid.Column="2" Content="✕" Width="30" Height="30" Margin="8,0,0,0"
                        FontSize="13" Background="#95A5A6" Foreground="White" BorderThickness="0"
                        Cursor="Hand" Click="BtnZamknij_Click" ToolTip="Zamknij"/>
            </Grid>
        </Border>

        <!-- Przycisk: wszystkie produkty -->
        <Button Grid.Row="1" x:Name="btnWszystkie" Height="38" Margin="0,0,0,10"
                Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand"
                FontSize="13" FontWeight="SemiBold" Content="📋  Wszystkie produkty (zdejmij filtr)"
                Click="BtnWszystkie_Click"/>

        <!-- Lista kafelków towarów -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="spTowary"/>
        </ScrollViewer>
    </Grid>
</Window>
