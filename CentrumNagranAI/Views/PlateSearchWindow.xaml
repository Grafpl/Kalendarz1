<Window x:Class="Kalendarz1.CentrumNagranAI.Views.PlateSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🚗 Wyszukiwarka tablic rejestracyjnych"
        Width="900" Height="640"
        WindowStartupLocation="CenterOwner"
        Background="#0F172A">

    <Window.Resources>
        <SolidColorBrush x:Key="Surface" Color="#1E293B"/>
        <SolidColorBrush x:Key="Surface2" Color="#243349"/>
        <SolidColorBrush x:Key="Border" Color="#334155"/>
        <SolidColorBrush x:Key="Text" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextDim" Color="#94A3B8"/>
        <SolidColorBrush x:Key="Accent" Color="#EF4444"/>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource Surface2}"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="RowBackground" Value="{StaticResource Surface2}"/>
            <Setter Property="AlternatingRowBackground" Value="#1E293B"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🚗 Wyszukiwarka tablic rejestracyjnych"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="11"
                   Foreground="{StaticResource TextDim}" Margin="0,0,0,12"
                   Text="Wpisz fragment numeru rejestracyjnego (np. 'WK', 'WK 12345', '12345'). Wyszukiwarka przeszukuje wszystkie tablice które zostały wykryte automatycznie z kamer rampy/bramy."/>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QueryBox" Grid.Column="0" KeyDown="QueryBox_KeyDown"/>
            <Button Grid.Column="1" Content="🔍 Szukaj" Padding="20,8" Margin="8,0,0,0"
                    Background="{StaticResource Accent}" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand" Click="Search_Click"/>
        </Grid>

        <DataGrid x:Name="ResultsGrid" Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tablica" Binding="{Binding Plate}" Width="160" FontFamily="Consolas"/>
                <DataGridTextColumn Header="Czas" Binding="{Binding TimeLabel}" Width="160"/>
                <DataGridTextColumn Header="Kamera" Binding="{Binding CameraDisplayName}" Width="200"/>
                <DataGridTextColumn Header="Plik klatki" Binding="{Binding FilePath}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
