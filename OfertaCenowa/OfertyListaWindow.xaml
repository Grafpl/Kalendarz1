<Window x:Class="Kalendarz1.OfertaCenowa.OfertyListaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📋 Lista Ofert Handlowych" 
        Height="900" Width="1600"
        MinHeight="700" MinWidth="1300"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#F0F2F5">

    <Window.Resources>
        <!-- Kolory -->
        <SolidColorBrush x:Key="PrimaryGreen" Color="#4B833C"/>
        <SolidColorBrush x:Key="PrimaryGreenDark" Color="#3D6B31"/>
        <SolidColorBrush x:Key="PrimaryGreenLight" Color="#E8F5E9"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentBlueLight" Color="#EFF6FF"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="AccentRed" Color="#EF4444"/>
        <SolidColorBrush x:Key="AccentRedLight" Color="#FEE2E2"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#F97316"/>
        <SolidColorBrush x:Key="AccentGray" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderGray" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextDark" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextGray" Color="#6B7280"/>

        <!-- Style przycisku głównego -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryGreenDark}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style przycisku drugorzędnego -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F9FAFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style przycisku niebezpiecznego (usuwanie) -->
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentRed}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#DC2626"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style akcji w tabeli -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBlueLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style usuwania w tabeli -->
        <Style x:Key="DeleteButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentRed}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentRedLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style karty -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.08" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- NAGŁÓWEK -->
        <Border Grid.Row="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#4B833C" Offset="0"/>
                    <GradientStop Color="#3D6B31" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid Margin="30,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel>
                    <TextBlock Text="📋 Lista Ofert Handlowych" FontSize="26" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="txtPodtytul" Text="Zarządzaj ofertami handlowymi" FontSize="13" Foreground="#B8E6B0" Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="➕ Nowa oferta" Style="{StaticResource SecondaryButton}" 
                            Click="BtnNowaOferta_Click" Margin="0,0,10,0" FontWeight="SemiBold"/>
                    <Button Content="🔄 Odśwież" Style="{StaticResource SecondaryButton}" Click="BtnOdswiez_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- GŁÓWNA ZAWARTOŚĆ -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="900"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <!-- LEWA STRONA - Lista ofert -->
            <Grid Grid.Column="0" Margin="0,0,15,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Pasek wyszukiwania i filtry -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="20" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Wyszukiwarka -->
                        <Border Grid.Column="0" Background="#F9FAFB" BorderBrush="{StaticResource BorderGray}" 
                                BorderThickness="1" CornerRadius="8" Padding="12,0" Margin="0,0,15,0">
                            <Grid>
                                <TextBlock x:Name="txtPlaceholder" Text="🔍 Szukaj klienta, numeru oferty, handlowca..." 
                                           FontSize="13" Foreground="{StaticResource TextGray}" VerticalAlignment="Center"
                                           Visibility="Visible"/>
                                <TextBox x:Name="txtSzukaj" Background="Transparent" BorderThickness="0" 
                                         FontSize="13" VerticalAlignment="Center" Padding="0,10"
                                         TextChanged="TxtSzukaj_TextChanged"/>
                            </Grid>
                        </Border>

                        <!-- Filtr statusu -->
                        <ComboBox x:Name="cboStatus" Grid.Column="1" Width="150" Margin="0,0,10,0"
                                  FontSize="12" Padding="10,8" SelectionChanged="CboStatus_SelectionChanged">
                            <ComboBoxItem Content="📋 Wszystkie" Tag="" IsSelected="True"/>
                            <ComboBoxItem Content="📝 Nowe" Tag="Nowa"/>
                            <ComboBoxItem Content="📧 Wysłane" Tag="Wyslana"/>
                            <ComboBoxItem Content="✅ Zaakceptowane" Tag="Zaakceptowana"/>
                            <ComboBoxItem Content="❌ Odrzucone" Tag="Odrzucona"/>
                        </ComboBox>

                        <!-- Daty -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,10,0">
                            <DatePicker x:Name="dpDataOd" Width="120" FontSize="11" 
                                        SelectedDateChanged="DpData_SelectedDateChanged"/>
                            <TextBlock Text=" - " VerticalAlignment="Center" Foreground="{StaticResource TextGray}"/>
                            <DatePicker x:Name="dpDataDo" Width="120" FontSize="11"
                                        SelectedDateChanged="DpData_SelectedDateChanged"/>
                        </StackPanel>

                        <!-- Szybkie filtry -->
                        <StackPanel Grid.Column="3" Orientation="Horizontal">
                            <Button Content="Dziś" Style="{StaticResource SecondaryButton}" Padding="10,8" 
                                    Click="BtnFiltrDzisiaj_Click" FontSize="11"/>
                            <Button Content="Tydzień" Style="{StaticResource SecondaryButton}" Padding="10,8" 
                                    Click="BtnFiltrTydzien_Click" Margin="5,0" FontSize="11"/>
                            <Button Content="Miesiąc" Style="{StaticResource SecondaryButton}" Padding="10,8" 
                                    Click="BtnFiltrMiesiac_Click" FontSize="11"/>
                            <Button Content="✕" Style="{StaticResource SecondaryButton}" Padding="10,8" 
                                    Click="BtnFiltrWyczysc_Click" Margin="5,0,0,0" FontSize="11" ToolTip="Wyczyść filtry"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Statystyki -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="15" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock x:Name="txtStatWszystkie" Text="0" FontSize="24" FontWeight="Bold" 
                                       Foreground="{StaticResource TextDark}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Wszystkie" FontSize="11" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <TextBlock x:Name="txtStatNowe" Text="0" FontSize="24" FontWeight="Bold" 
                                       Foreground="{StaticResource AccentBlue}" HorizontalAlignment="Center"/>
                            <TextBlock Text="📝 Nowe" FontSize="11" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <TextBlock x:Name="txtStatWyslane" Text="0" FontSize="24" FontWeight="Bold" 
                                       Foreground="{StaticResource AccentPurple}" HorizontalAlignment="Center"/>
                            <TextBlock Text="📧 Wysłane" FontSize="11" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                            <TextBlock x:Name="txtStatZaakceptowane" Text="0" FontSize="24" FontWeight="Bold" 
                                       Foreground="{StaticResource PrimaryGreen}" HorizontalAlignment="Center"/>
                            <TextBlock Text="✅ Zaakceptowane" FontSize="11" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="4" HorizontalAlignment="Center">
                            <TextBlock x:Name="txtStatWartosc" Text="0 zł" FontSize="20" FontWeight="Bold" 
                                       Foreground="{StaticResource AccentOrange}" HorizontalAlignment="Center"/>
                            <TextBlock Text="💰 Suma wartości" FontSize="11" Foreground="{StaticResource TextGray}" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Przycisk usuń zaznaczone -->
                        <Button x:Name="btnUsunZaznaczone" Grid.Column="5" Content="🗑️ Usuń zaznaczone" 
                                Style="{StaticResource DangerButton}" Click="BtnUsunZaznaczone_Click"
                                IsEnabled="False" Visibility="Collapsed" Margin="15,0,0,0"/>
                    </Grid>
                </Border>

                <!-- Tabela ofert -->
                <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
                    <DataGrid x:Name="dgOferty" AutoGenerateColumns="False" IsReadOnly="True"
                              SelectionMode="Extended" SelectionUnit="FullRow"
                              GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                              BorderThickness="0" Background="Transparent"
                              RowHeight="56" CanUserResizeRows="False"
                              SelectionChanged="DgOferty_SelectionChanged"
                              MouseDoubleClick="DgOferty_MouseDoubleClick">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F8FAFC"/>
                                <Setter Property="Foreground" Value="#64748B"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="Padding" Value="12,14"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#F1F5F9"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F8FAFC"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#EFF6FF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="12,0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <!-- Status ikona -->
                            <DataGridTemplateColumn Header="" Width="45">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StatusIkona}" FontSize="20" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Numer oferty -->
                            <DataGridTemplateColumn Header="NUMER OFERTY" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding NumerOferty}" FontWeight="SemiBold" 
                                                   Foreground="{StaticResource PrimaryGreen}" FontSize="12"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Data -->
                            <DataGridTemplateColumn Header="DATA" Width="95">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DataWystawieniaFormatowana}" FontSize="12"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Klient -->
                            <DataGridTemplateColumn Header="KLIENT" Width="*" MinWidth="180">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding KlientNazwa}" FontWeight="SemiBold" FontSize="12" 
                                                       TextTrimming="CharacterEllipsis"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding KlientMiejscowosc}" FontSize="10" 
                                                           Foreground="{StaticResource TextGray}"/>
                                                <TextBlock Text=" • " FontSize="10" Foreground="{StaticResource TextGray}"
                                                           Visibility="{Binding KlientMiejscowosc, Converter={x:Null}}"/>
                                                <TextBlock Text="{Binding LiczbaPozycji, StringFormat={}{0} poz.}" 
                                                           FontSize="10" Foreground="{StaticResource TextGray}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Utworzył -->
                            <DataGridTemplateColumn Header="👤 UTWORZYŁ" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#F0FDF4" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding HandlowiecNazwa}" FontSize="11" 
                                                       Foreground="{StaticResource PrimaryGreen}" TextTrimming="CharacterEllipsis"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Wartość -->
                            <DataGridTemplateColumn Header="WARTOŚĆ" Width="115">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding WartoscFormatowana}" FontWeight="Bold" FontSize="13" 
                                                   HorizontalAlignment="Right" Foreground="{StaticResource TextDark}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Status -->
                            <DataGridTemplateColumn Header="STATUS" Width="115">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding StatusBackground}" CornerRadius="12" 
                                                Padding="10,5" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Status}" FontSize="10" FontWeight="SemiBold"
                                                       Foreground="{Binding StatusForeground}"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Akcje -->
                            <DataGridTemplateColumn Header="AKCJE" Width="130">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="📄" Style="{StaticResource ActionButton}" 
                                                    ToolTip="Otwórz PDF" Click="BtnOtworzPDF_Click" Tag="{Binding}"/>
                                            <Button Content="📧" Style="{StaticResource ActionButton}" 
                                                    ToolTip="Wyślij email" Click="BtnWyslijEmail_Click" Tag="{Binding}" Margin="4,0"/>
                                            <Button Content="🗑️" Style="{StaticResource DeleteButton}" 
                                                    ToolTip="Usuń ofertę" Click="BtnUsunOferte_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>

            <!-- PRAWA STRONA - Szczegóły -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="20">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- Placeholder gdy brak wyboru -->
                            <StackPanel x:Name="placeholderBrakWyboru" HorizontalAlignment="Center" 
                                        VerticalAlignment="Center" Margin="0,50,0,0">
                                <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center" Opacity="0.3"/>
                                <TextBlock Text="Wybierz ofertę z listy" FontSize="14" Foreground="{StaticResource TextGray}" 
                                           HorizontalAlignment="Center" Margin="0,15,0,0"/>
                                <TextBlock Text="aby zobaczyć szczegóły" FontSize="12" Foreground="{StaticResource TextGray}" 
                                           HorizontalAlignment="Center" Opacity="0.7"/>
                            </StackPanel>

                            <!-- Panel szczegółów -->
                            <StackPanel x:Name="panelSzczegolyOferty" Visibility="Collapsed">
                                <!-- Nagłówek oferty -->
                                <Grid Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="txtSzczegolyIkona" Text="📝" FontSize="32" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                        <TextBlock x:Name="txtSzczegolyNumer" Text="OF/202511/001" FontSize="18" FontWeight="Bold" 
                                                   Foreground="{StaticResource PrimaryGreen}"/>
                                        <TextBlock x:Name="txtSzczegolyData" Text="27.11.2025" FontSize="12" Foreground="{StaticResource TextGray}"/>
                                    </StackPanel>
                                    <!-- Przycisk usuwania -->
                                    <Button Grid.Column="2" x:Name="btnUsunWybrana" Content="🗑️" 
                                            Style="{StaticResource DeleteButton}" ToolTip="Usuń tę ofertę"
                                            Click="BtnUsunWybranaOferte_Click" FontSize="18" Padding="8,6"/>
                                </Grid>

                                <Separator Background="{StaticResource BorderGray}" Margin="0,0,0,20"/>

                                <!-- DANE KLIENTA -->
                                <TextBlock Text="👤 DANE KLIENTA" FontSize="11" FontWeight="Bold" 
                                           Foreground="{StaticResource PrimaryGreen}" Margin="0,0,0,12"/>

                                <TextBlock Text="Nazwa firmy" FontSize="10" Foreground="{StaticResource TextGray}" Margin="0,0,0,3"/>
                                <TextBox x:Name="txtKlientNazwa" FontSize="12" Padding="10,8" Margin="0,0,0,10" IsReadOnly="True"
                                         Background="#F9FAFB" BorderBrush="{StaticResource BorderGray}" BorderThickness="1"/>

                                <TextBlock Text="Adres" FontSize="10" Foreground="{StaticResource TextGray}" Margin="0,0,0,3"/>
                                <TextBox x:Name="txtKlientAdres" FontSize="12" Padding="10,8" Margin="0,0,0,10" IsReadOnly="True"
                                         Background="#F9FAFB" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" TextWrapping="Wrap"/>

                                <Separator Background="{StaticResource BorderGray}" Margin="0,10,0,20"/>

                                <!-- DANE KONTAKTOWE - EDYTOWALNE -->
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📞 DANE KONTAKTOWE" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentBlue}"/>
                                        <Border Background="{StaticResource AccentBlueLight}" CornerRadius="3" Padding="6,2" Margin="8,0,0,0">
                                            <TextBlock Text="edytowalne" FontSize="9" Foreground="{StaticResource AccentBlue}"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" x:Name="txtZmianyNiezapisane" Text="● niezapisane" FontSize="10" 
                                               Foreground="{StaticResource AccentOrange}" Visibility="Collapsed"/>
                                </Grid>

                                <TextBlock Text="Osoba kontaktowa" FontSize="10" Foreground="{StaticResource TextGray}" Margin="0,0,0,3"/>
                                <TextBox x:Name="txtKlientOsoba" FontSize="12" Padding="10,8" Margin="0,0,0,10"
                                         BorderBrush="{StaticResource BorderGray}" BorderThickness="1" TextChanged="DaneKontaktowe_Changed"/>

                                <TextBlock Text="📧 Email (do wysyłki oferty)" FontSize="10" Foreground="{StaticResource AccentBlue}" 
                                           FontWeight="SemiBold" Margin="0,0,0,3"/>
                                <TextBox x:Name="txtKlientEmail" FontSize="12" Padding="10,8" Margin="0,0,0,10"
                                         BorderBrush="{StaticResource AccentBlue}" BorderThickness="2"
                                         TextChanged="DaneKontaktowe_Changed"/>

                                <TextBlock Text="Telefon" FontSize="10" Foreground="{StaticResource TextGray}" Margin="0,0,0,3"/>
                                <TextBox x:Name="txtKlientTelefon" FontSize="12" Padding="10,8" Margin="0,0,0,15"
                                         BorderBrush="{StaticResource BorderGray}" BorderThickness="1" TextChanged="DaneKontaktowe_Changed"/>

                                <!-- Przycisk zapisu -->
                                <Button x:Name="btnZapiszKontakt" Content="💾 Zapisz dane kontaktowe" 
                                        Style="{StaticResource PrimaryButton}" Click="BtnZapiszKontakt_Click"
                                        IsEnabled="False"/>

                                <Separator Background="{StaticResource BorderGray}" Margin="0,20,0,20"/>

                                <!-- WARTOŚĆ -->
                                <Border Background="{StaticResource PrimaryGreenLight}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="💰 WARTOŚĆ OFERTY" FontSize="11" FontWeight="Bold" 
                                                   Foreground="{StaticResource PrimaryGreen}" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" x:Name="txtSzczegolyWartosc" Text="0,00 zł" 
                                                   FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryGreen}"/>
                                    </Grid>
                                </Border>

                                <Separator Background="{StaticResource BorderGray}" Margin="0,5,0,20"/>

                                <!-- ZMIANA STATUSU -->
                                <TextBlock Text="📋 ZMIEŃ STATUS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextGray}" Margin="0,0,0,12"/>

                                <WrapPanel>
                                    <Button Content="📝 Nowa" Style="{StaticResource SecondaryButton}" 
                                            Click="BtnZmienStatus_Click" Tag="Nowa" Margin="0,0,6,6" FontSize="11"/>
                                    <Button Content="📧 Wysłana" Style="{StaticResource SecondaryButton}" 
                                            Click="BtnZmienStatus_Click" Tag="Wyslana" Margin="0,0,6,6" FontSize="11"/>
                                    <Button Content="✅ Zaakceptowana" Style="{StaticResource SecondaryButton}" 
                                            Click="BtnZmienStatus_Click" Tag="Zaakceptowana" Margin="0,0,6,6" FontSize="11"/>
                                    <Button Content="❌ Odrzucona" Style="{StaticResource SecondaryButton}" 
                                            Click="BtnZmienStatus_Click" Tag="Odrzucona" Margin="0,0,6,6" FontSize="11"/>
                                </WrapPanel>

                                <Separator Background="{StaticResource BorderGray}" Margin="0,15,0,20"/>

                                <!-- UTWORZYŁ -->
                                <Border Background="#F0FDF4" CornerRadius="8" Padding="15">
                                    <StackPanel>
                                        <TextBlock Text="👤 UTWORZYŁ OFERTĘ" FontSize="11" FontWeight="Bold" Foreground="{StaticResource PrimaryGreen}" Margin="0,0,0,6"/>
                                        <TextBlock x:Name="txtSzczegolyHandlowiec" Text="-" FontSize="14" FontWeight="SemiBold" 
                                                   Foreground="{StaticResource TextDark}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <!-- Przyciski dolne -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="15" Margin="0,15,0,0">
                    <StackPanel>
                        <Button x:Name="btnWyslijEmailZOferta" Content="📧 Wyślij ofertę emailem" Style="{StaticResource PrimaryButton}"
                                Click="BtnWyslijEmailZOferta_Click" Margin="0,0,0,8" IsEnabled="False"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" x:Name="btnOtworzPDFSzczegoly" Content="📄 Otwórz PDF" 
                                    Style="{StaticResource SecondaryButton}"
                                    Click="BtnOtworzPDFSzczegoly_Click" Margin="0,0,4,0" IsEnabled="False"/>
                            <Button Grid.Column="1" Content="Zamknij" Style="{StaticResource SecondaryButton}" 
                                    Click="BtnZamknij_Click" Margin="4,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
