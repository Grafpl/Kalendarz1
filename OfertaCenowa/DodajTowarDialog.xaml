<Window x:Class="Kalendarz1.OfertaCenowa.DodajTowarDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodaj produkt do oferty" 
        Height="350" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#F9FAFB"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Wybierz produkt" 
                   FontSize="16" 
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Produkt:" FontSize="11" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbTowar" 
                  Grid.Row="2"
                  Height="35"
                  FontSize="11"
                  Margin="0,0,0,15"
                  DisplayMemberPath="Nazwa"/>

        <Grid Grid.Row="3" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" 
                       Text="Ilość (kg):" 
                       FontSize="11" 
                       Margin="0,0,0,5"/>
            <TextBox x:Name="txtIlosc" 
                     Grid.Row="1" Grid.Column="0"
                     Height="35"
                     FontSize="11"
                     Text="0"/>

            <TextBlock Grid.Row="0" Grid.Column="2" 
                       Text="Cena jedn. (zł/kg):" 
                       FontSize="11" 
                       Margin="0,0,0,5"/>
            <TextBox x:Name="txtCena" 
                     Grid.Row="1" Grid.Column="2"
                     Height="35"
                     FontSize="11"
                     Text="0"/>
        </Grid>

        <Border Grid.Row="4" 
                Background="#EFF6FF" 
                BorderBrush="#BFDBFE" 
                BorderThickness="1" 
                CornerRadius="6" 
                Padding="15">
            <StackPanel>
                <TextBlock Text="Wartość pozycji:" 
                           FontSize="11" 
                           Foreground="#1E40AF"
                           Margin="0,0,0,5"/>
                <TextBlock x:Name="txtWartoscPozycji" 
                           Text="0,00 zł" 
                           FontSize="20" 
                           FontWeight="Bold"
                           Foreground="#1E40AF"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Dodaj" 
                    Width="100" 
                    Height="35"
                    Background="#10B981"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold"
                    Margin="0,0,10,0"
                    Click="BtnDodaj_Click"/>
            <Button Content="Anuluj" 
                    Width="100" 
                    Height="35"
                    Background="#F3F4F6"
                    Foreground="#4B5563"
                    BorderThickness="0"
                    FontWeight="SemiBold"
                    Click="BtnAnuluj_Click"/>
        </StackPanel>
    </Grid>
</Window>