<Window x:Class="Kalendarz1.OfertaCenowa.WyborSzablonuTowarowWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wybierz szablon towarów" 
        Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#F4F6F9">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryGreenBrush" Color="#4B833C"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#D1D5DB"/>
        <SolidColorBrush x:Key="LightTextBrush" Color="#7F8C8D"/>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryGreenBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="📁 Wybierz szablon towarów" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="Kliknij dwukrotnie aby wczytać lub wybierz i kliknij 'Wczytaj'" 
                       FontSize="12" Foreground="{StaticResource LightTextBrush}" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Lista szablonów -->
        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
            <Grid>
                <ListBox x:Name="lstSzablony" BorderThickness="0" SelectionChanged="LstSzablony_SelectionChanged" MouseDoubleClick="LstSzablony_MouseDoubleClick">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="15,12" BorderBrush="#F3F4F6" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Nazwa}" FontSize="14" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Opis}" FontSize="11" Foreground="{StaticResource LightTextBrush}" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                                    </StackPanel>
                                    <Border Grid.Column="1" Background="#E8F5E9" CornerRadius="4" Padding="10,5" VerticalAlignment="Center">
                                        <TextBlock FontSize="12" Foreground="{StaticResource PrimaryGreenBrush}">
                                            <Run Text="{Binding LiczbaTowrow, Mode=OneWay}"/><Run Text=" produktów"/>
                                        </TextBlock>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Placeholder gdy brak -->
                <StackPanel x:Name="placeholderBrak" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="📭" FontSize="40" HorizontalAlignment="Center" Opacity="0.3"/>
                    <TextBlock Text="Brak zapisanych szablonów" FontSize="14" Foreground="{StaticResource LightTextBrush}" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <TextBlock Text="Kliknij 'Edytuj szablony' aby utworzyć nowy" FontSize="12" Foreground="{StaticResource LightTextBrush}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Przyciski -->
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="✏️ Edytuj szablony" Style="{StaticResource SecondaryButtonStyle}" Click="BtnEdytujSzablony_Click"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="❌ Anuluj" Style="{StaticResource SecondaryButtonStyle}" Click="BtnAnuluj_Click" Margin="0,0,10,0"/>
                <Button x:Name="btnWczytaj" Content="✅ Wczytaj szablon" Style="{StaticResource PrimaryButtonStyle}" Click="BtnWczytaj_Click" IsEnabled="False"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
