<UserControl x:Class="Kalendarz1.MagazynLiczenie.Formularze.SimpleDatePickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="320" Height="420">
    <UserControl.Resources>
        <Style x:Key="CalendarButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EBF5FB"/>
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3498DB"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TodayButtonStyle" TargetType="Button" BasedOn="{StaticResource CalendarButtonStyle}">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#229954"/>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#34495E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#34495E" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- Month/Year Display -->
        <Border Grid.Row="0" Background="#2C3E50" CornerRadius="8,8,0,0" Margin="5,5,5,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Name="btnPrevMonth" Content="◀" Style="{StaticResource NavButtonStyle}"
                        Click="BtnPrevMonth_Click" Margin="5"/>
                <TextBlock x:Name="txtMonthYear" 
                           Text="Styczeń 2025" 
                           FontSize="20" 
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"
                           MinWidth="150"
                           TextAlignment="Center"
                           Margin="10,0"/>
                <Button x:Name="btnNextMonth" Content="▶" Style="{StaticResource NavButtonStyle}"
                        Click="BtnNextMonth_Click" Margin="5"/>
            </StackPanel>
        </Border>

        <!-- Day Headers -->
        <UniformGrid Grid.Row="1" Rows="1" Columns="7" Margin="5,0">
            <TextBlock Text="Pn" FontSize="14" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="Wt" FontSize="14" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="Śr" FontSize="14" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="Cz" FontSize="14" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="Pt" FontSize="14" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="So" FontSize="14" FontWeight="Bold" Foreground="#E74C3C" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="Nd" FontSize="14" FontWeight="Bold" Foreground="#E74C3C" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </UniformGrid>

        <!-- Calendar Grid -->
        <UniformGrid x:Name="gridCalendar" Grid.Row="2" Rows="6" Columns="7" Margin="5"/>

        <!-- Today Button -->
        <Button Grid.Row="3" Content="📅 DZISIAJ" 
                Style="{StaticResource TodayButtonStyle}"
                Height="50" Margin="10,5"
                FontSize="20"
                Click="BtnToday_Click"/>
    </Grid>
</UserControl>