<Window x:Class="Kalendarz1.Kartoteka.Features.Przypomnienia.PrzypomnieniaCentrum"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Centrum przypomnień" Width="750" Height="600"
        WindowStartupLocation="CenterScreen" Background="#F3F4F6">
    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="🔔 Centrum przypomnień" FontSize="18" FontWeight="Bold" Foreground="#166534"/>
            <Border x:Name="badgeLicznik" Background="#EF4444" CornerRadius="10" Padding="8,2" Margin="12,0,0,0"
                    VerticalAlignment="Center" Visibility="Collapsed">
                <TextBlock x:Name="txtBadge" Text="0" FontSize="11" FontWeight="Bold" Foreground="White"/>
            </Border>
        </StackPanel>

        <!-- Przyciski akcji -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <Button Content="➕ Nowe przypomnienie" Click="BtnDodaj_Click" Margin="0,0,8,0"
                    Background="#059669" Style="{StaticResource ActionButton}"/>
            <Button Content="🔄 Odśwież" Click="BtnOdswiez_Click" Margin="0,0,8,0"
                    Background="#3B82F6" Style="{StaticResource ActionButton}"/>
        </WrapPanel>

        <!-- Lista przypomnień -->
        <Border Grid.Row="2" Background="White" CornerRadius="6" BorderBrush="#E5E7EB" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="panelPrzypomnienia" Margin="4"/>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" x:Name="txtStatus" FontSize="11" Foreground="#6B7280" Margin="0,8,0,0"/>
    </Grid>
</Window>
