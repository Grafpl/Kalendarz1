<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:Kalendarz1.Kartoteka.Converters">

    <!-- ============================================== -->
    <!-- KONWERTERY -->
    <!-- ============================================== -->
    <converters:KategoriaToBackgroundConverter x:Key="KategoriaToBackgroundConverter"/>
    <converters:KategoriaToForegroundConverter x:Key="KategoriaToForegroundConverter"/>
    <converters:AlertToBackgroundConverter x:Key="AlertToBackgroundConverter"/>
    <converters:AlertToBorderConverter x:Key="AlertToBorderConverter"/>
    <converters:AlertToIconConverter x:Key="AlertToIconConverter"/>
    <converters:ProcentToProgressColorConverter x:Key="ProcentToProgressColorConverter"/>
    <converters:DecimalToFormattedConverter x:Key="DecimalToFormattedConverter"/>
    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    <converters:FakturaStatusToColorConverter x:Key="FakturaStatusToColorConverter"/>
    <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>

    <!-- ============================================== -->
    <!-- KOLORY FOREST -->
    <!-- ============================================== -->
    <Color x:Key="HeaderGradientStart">#166534</Color>
    <Color x:Key="HeaderGradientEnd">#059669</Color>
    <Color x:Key="ButtonPrimary">#059669</Color>
    <Color x:Key="ButtonPrimaryHover">#047857</Color>
    <Color x:Key="TableHeader">#DCFCE7</Color>
    <Color x:Key="TableBorder">#BBF7D0</Color>
    <Color x:Key="TableRowHover">#F0FDF4</Color>
    <Color x:Key="TabActive">#059669</Color>
    <Color x:Key="TabInactive">#6B7280</Color>
    <Color x:Key="TabHover">#F0FDF4</Color>
    <Color x:Key="DetailBackground">#F0FDF4</Color>
    <Color x:Key="DetailCardBorder">#DCFCE7</Color>
    <Color x:Key="FooterBackground">#14532D</Color>
    <Color x:Key="FooterText">#BBF7D0</Color>

    <SolidColorBrush x:Key="ButtonPrimaryBrush" Color="{StaticResource ButtonPrimary}"/>
    <SolidColorBrush x:Key="ButtonPrimaryHoverBrush" Color="{StaticResource ButtonPrimaryHover}"/>
    <SolidColorBrush x:Key="TableHeaderBrush" Color="{StaticResource TableHeader}"/>
    <SolidColorBrush x:Key="TableBorderBrush" Color="{StaticResource TableBorder}"/>
    <SolidColorBrush x:Key="DetailBackgroundBrush" Color="{StaticResource DetailBackground}"/>
    <SolidColorBrush x:Key="DetailCardBorderBrush" Color="{StaticResource DetailCardBorder}"/>
    <SolidColorBrush x:Key="FooterBackgroundBrush" Color="{StaticResource FooterBackground}"/>
    <SolidColorBrush x:Key="FooterTextBrush" Color="{StaticResource FooterText}"/>

    <!-- ============================================== -->
    <!-- STYL PRZYCISKU GŁÓWNEGO -->
    <!-- ============================================== -->
    <Style x:Key="KartotekaPrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ButtonPrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonPrimaryHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="KartotekaSecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="#33FFFFFF"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#55FFFFFF"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#55FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================== -->
    <!-- STYL ZAKŁADEK -->
    <!-- ============================================== -->
    <Style x:Key="KartotekaTabItem" TargetType="TabItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#6B7280"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            CornerRadius="6,6,0,0" Padding="{TemplateBinding Padding}"
                            Margin="2,0,2,0" Cursor="Hand">
                        <ContentPresenter x:Name="content" ContentSource="Header"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#059669"/>
                            <Setter TargetName="content" Property="TextElement.Foreground" Value="White"/>
                            <Setter TargetName="content" Property="TextElement.FontWeight" Value="SemiBold"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="border" Property="Background" Value="#F0FDF4"/>
                            <Setter TargetName="content" Property="TextElement.Foreground" Value="#059669"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================== -->
    <!-- STYL DATAGRID -->
    <!-- ============================================== -->
    <Style x:Key="KartotekaDataGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#E5E7EB"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="RowHeight" Value="38"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="#1F2937"/>
    </Style>

    <Style x:Key="KartotekaColumnHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource TableHeaderBrush}"/>
        <Setter Property="Foreground" Value="#166534"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="BorderBrush" Value="{StaticResource TableBorderBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
    </Style>

    <Style x:Key="KartotekaDataGridCell" TargetType="DataGridCell">
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}" BorderThickness="0" Padding="8,4">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#D1FAE5"/>
                <Setter Property="Foreground" Value="#1F2937"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="KartotekaDataGridRow" TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="#F3F4F6"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#F0FDF4"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#D1FAE5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ============================================== -->
    <!-- STYL TEXTBOX SZUKAJ -->
    <!-- ============================================== -->
    <Style x:Key="KartotekaSearchBox" TargetType="TextBox">
        <Setter Property="Background" Value="#20FFFFFF"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="CaretBrush" Value="White"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#40FFFFFF"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#80FFFFFF"/>
                            <Setter TargetName="border" Property="Background" Value="#30FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================== -->
    <!-- STYL COMBOBOX W NAGŁÓWKU -->
    <!-- ============================================== -->
    <Style x:Key="KartotekaHeaderComboBox" TargetType="ComboBox">
        <Setter Property="Background" Value="#20FFFFFF"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#40FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="MinHeight" Value="30"/>
    </Style>

    <!-- ============================================== -->
    <!-- STYL KARTY KONTAKTU -->
    <!-- ============================================== -->
    <Style x:Key="KontaktCard" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#DCFCE7"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Width" Value="180"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="0" Color="#000000" Opacity="0.05" BlurRadius="8"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================== -->
    <!-- STYL KARTY STATYSTYKI -->
    <!-- ============================================== -->
    <Style x:Key="StatCard" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#DCFCE7"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect ShadowDepth="0" Color="#000000" Opacity="0.05" BlurRadius="8"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================== -->
    <!-- STYL HYPERLINK -->
    <!-- ============================================== -->
    <Style x:Key="KartotekaHyperlink" TargetType="Hyperlink">
        <Setter Property="Foreground" Value="#2563EB"/>
        <Setter Property="TextDecorations" Value="None"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="TextDecorations" Value="Underline"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
